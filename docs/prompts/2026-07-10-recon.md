# BNOW.NET — State Reconciliation & Reprioritization (READ-MOSTLY, $0 LLM)

> Provenance: this is the verbatim recon prompt, authored 2026-07-10 from outside the
> repo, executed in-repo 2026-07-11. Saved here per its own TASK 0 so the repo carries
> its provenance chain. The deliverable it produced is `docs/reviews/STATE-2026-07-10.md`.

**Run in a NEW Claude Code session** in `~/code/bnow.net`. Orient from `CLAUDE.md` / `AGENTS.md` (note: if the AGENTS/CLAUDE restructure session has landed, AGENTS.md is a snapshot + standing rulings; if it still reads as a journal, that is itself a TASK 1 finding), `docs/OPEN-TASKS.md`, and the tail of `docs/PROGRESS.md`. Do not rely on prior-session memory.

**Provenance caveat:** this prompt was written 2026-07-10 from outside the repo. Every factual claim below is a hypothesis — verify from git history, the filesystem, and the database before acting on it. Where this prompt is wrong about the state, the disk is right.

## MISSION

Nine-plus sessions ran between July 5 and July 10 (weekend build, hardening, coverage sprint, nav restructure, i18n, source-mix quota, pipeline audit, MR sprints 1–2, and possibly MR sprint 3 and the AGENTS/CLAUDE restructure). Produce one self-contained state document answering: **what actually shipped, what is live and healthy, what is broken or drifting, and what should run next** — so the next implementation session starts from verified ground truth instead of prompt-corpus archaeology.

## GROUND RULES

- **$0 LLM. Zero paid API calls of any kind.** Read-only database queries are fine. If a task would require spend, skip it and note the gap.
- Docs-only commits (the state doc, OPEN-TASKS updates, AGENTS.md corrections per its in-place-correction rule). No src/ changes — if you find a bug, it goes in OPEN-TASKS, not in a patch.
- Every number in the state doc states its provenance (SQL, file, git ref) so it can be reproduced.
- Commit hygiene per CLAUDE.md (no vendor trailers or model names in commits).
- Copy this prompt into `docs/prompts/2026-07-10-recon.md` in your first commit, so the repo carries its own provenance chain from here on.

## TASK 1 — Ship ledger

For each item below, record **SHIPPED (evidence: commit/file/DB row) / PARTIAL (what remains) / NOT RUN**:

1. **MR Sprint 3** (reduce, A/B, cutover): does `src/lib/analysis/reduce.ts` exist with tests; `docs/reviews/MR3-CHECKPOINT.md` state; the A/B report and its gate verdict; `DIGEST_ENGINE` flag existence and **deployed value** (legacy vs mapreduce); the new synthesis cadence (04:00/10:00/19:30 UTC + 02:00 D+1 finalization) in `vercel.json`; whether the disposable Neon branch was deleted; whether OPEN-TASKS #29 (Lebanese channels → ir retag) executed and the ~586-doc map catch-up completed.
2. **AGENTS/CLAUDE restructure**: `docs/DECISIONS.md` archive exists; AGENTS.md line count and shape (snapshot + standing rulings vs journal); CLAUDE.md commands block present; the filename-case question resolved.
3. **July-6 hardening-era debt**, item by item: CI workflow (`.github/workflows/` or pre-push hook), /ask rate limit + per-user usage logging, entity canonicalization (rules pass, LLM-assisted pass, extraction-prompt lexicon rules), integration tests (trigger invariant, digest path, scoreDigest), per-theater takeaway filtering, Iran military prompt variant, reliability weighting in ranking/retrieval, ME source materialization (are the 1,574 zero-citation ME rows still zombie?).
4. Anything in git log since July 7 that no prompt in `docs/prompts/` (or this ledger) accounts for — list it; unexplained commits are a finding.

## TASK 2 — Live-system health

From `cron_runs`, `provider_usage`, and the tables, for the last 48h under whatever cadence is actually deployed:

1. Every cron job: fired on schedule? success rate? runtime vs `maxDuration` (use the killed-vs-running distinction from the cron-audit fix)?
2. Digests current for ru/ua/ir (and gulf theaters on whatever engine serves them); validation runs current; X ingest still hourly and under its spend guard; map coverage still ≥95% of canonical eligible docs.
3. Any symptom of the silent-failure class (empty digests over good ones, refusal/truncation retries spiking) — the guards should make this impossible; confirm they're actually firing as guards, not silently absent.

## TASK 3 — Spend audit

`provider_usage` by provider for the last 7 days vs each cap: `openai_digest`, `openai_map`, `openai_reduce` (if live), X, OpenSanctions. Specifically follow up the OpenSanctions $33-in-3-days overrun flagged in MR1's operator checklist: what is the current burn rate, was the enrich cron budget fixed, and is the monthly trajectory inside the 2,000-call quota? Flag anything trending toward its cap within 7 days.

## TASK 4 — Scoreboard truth

Current coverage mean/variance for ru/ua/ir under the majority matcher; unsupported-claim rate; x-share of citations; info-lead. State the honest distance to the product brief's §8.7 targets (≥80% event coverage, <2% unsupported, ±6h timeliness) — one short table, no spin.

## TASK 5 — OPEN-TASKS reconciliation

Walk every open item: close what TASK 1 proved shipped (with pointers), re-rank the remainder into the existing tier structure, add everything this recon surfaced. The output should let a fresh session trust OPEN-TASKS again without cross-checking git.

## DELIVERABLE

`docs/reviews/STATE-2026-07-10.md` — self-contained, every number sourced — plus the updated OPEN-TASKS.md, committed (docs only). Paste an executive summary (≤20 lines) into the session output, ending with a **single recommended next session** from: (a) MR Sprint 3 itself if unrun (its prompt exists and is current), (b) the MTProto ingest sprint (prompt provided alongside this one), (c) the hardening sweep (prompt provided alongside this one), (d) something this recon surfaced as more urgent than all three — with a one-line justification tied to what you found.

## DEFINITION OF DONE

- Every ship-ledger entry has evidence or an explicit NOT RUN.
- Health, spend, and scoreboard sections carry sourced numbers.
- OPEN-TASKS is trustworthy again; state doc committed; exec summary + next-session pick pasted back.
- Actual LLM spend at session end: $0.00 — state it.
