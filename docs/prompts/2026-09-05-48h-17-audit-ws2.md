# Step 17 — Adversarial audit of the WS-2 diffs (steps 09, 11, 12) — SKETCH (Wave 3)

| | |
|---|---|
| Model / effort / mode | **Fable / xhigh / ultracode** — parallel finders per lens + refuters per finding; read-only |
| Worktree | `48h-audit-ws2-20260905` — read-only; reset to the audited SHA (branches or `main` after CP2) |
| Window | H22 → H26 |
| Depends on | 11, 12 complete (merged or PR-ready); 09 merged |
| Rewrite from | `docs/reviews/WS-2-1-ASK-PARITY-2026-09-06.md`, `WS-2-2-PROVIDER-CORE-2026-09-06.md`, `ANTHROPIC-SEAM-HARDENING-2026-09-05.md` — paste their Built/Tests/Handoff sections and the PR numbers below before use |
| Spend | $0 |
| Closing report | `docs/reviews/WS-2-AUDIT-FINDING-REGISTER-2026-09-06.md` |

Read `docs/prompts/2026-09-05-48h-COMMON.md` first. Precedent for the format:
`docs/reviews/CONFLICT-EVALUATION-FABLE-AUDIT-FINDING-REGISTER-2026-08-18.md` and
`docs/prompts/2026-08-18-quality-foundation-fable-final-audit.md`.

## Prompt shape (fill in at CP2)

You are auditing, not fixing. Diffs under audit: `<PR list from the reports>` against base
`<SHA>`. Lenses, each a separate finder that reads the whole diff plus the files it touches:
(1) **ruling 4** — is there any path where a provider/model/effort not in the registry
reaches `tryReserve` or a client constructor? any new row/cap without the ruling-4 ordering
note? (2) **ruling 8/9** — metering still inside `analyze()`; per-site degradation
unchanged (digest throws typed; llm-match degrades to keyword; /ask degrades); (3) **ruling
13** — map lock predicate untouched; extractor-version basis untouched; (4) **identity** —
every persisted output that carried `openai:`/`registryVersion` still does, byte-identical
for provider `openai`; (5) **eval-plane isolation** — `docs/evals/analysis/` byte-untouched;
offline results resume; `isolation.test.ts`/`import-graph.test.ts` still meaningful; (6)
**tests as evidence** — does each claimed pin actually fail if the guard is removed (mutate
and run)? (7) **secret/env leakage** and **docs truth** (OPEN-TASKS/AGENTS text matches the
code). For every finding, three independent refuters try to disprove it; a finding survives
with ≥2 non-refutations. Output the register: id, severity (blocker/major/minor), file:line,
claim, refutation summary, remediation (exact), and a per-PR verdict (merge / merge-after-fix
/ do-not-merge). No code changes. Step 23 remediates.
