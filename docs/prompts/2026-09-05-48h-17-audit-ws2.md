# Step 17 — Adversarial audit of the WS-2 + WS-4 code landed at CP2/CP2b (Wave 3)

| | |
|---|---|
| Model / effort / mode | **Fable / xhigh / ultracode** — parallel finders per lens, independent refuters per finding; read-only; attended, normal permissions |
| Worktree | `/Users/go/code/bnow-net-worktrees/48h-audit-ws2-20260905`, reset to the audited SHA: `git fetch origin && git checkout -B 48h/audit-ws2-20260905 98294c5` (`main` after CP2b). Read-only: no commits except the register. |
| Window | now → +4h; runs in parallel with Wave 3 |
| Depends on | CP2b done (#52, #57, #59, #60, #61, #62, #64, #65 on `main`) |
| Decisions | none. This step decides nothing and fixes nothing; step 23 remediates. |
| Spend | $0. No DB, no fork, no `.env.local` in this worktree. Mutation runs are `npm test` only. |
| Closing report | `docs/reviews/WS-2-AUDIT-FINDING-REGISTER-2026-09-06.md` on branch `48h/audit-ws2-20260905-finding-register`, PR opened |

Read `docs/prompts/2026-09-05-48h-COMMON.md` first (§3 Never; §4.10). Precedent for the
register format: `docs/reviews/CONFLICT-EVALUATION-FABLE-AUDIT-FINDING-REGISTER-2026-08-18.md`
and `docs/prompts/2026-08-18-quality-foundation-fable-final-audit.md`. **ultracode**: fan out
one finder per lens below and three independent refuters per finding; a finding survives
with ≥2 non-refutations; every surviving finding carries a file:line the refuters
re-verified at `98294c5`.

## Under audit (base `98294c5`; audit the merged diffs, `git log --merges` gives the SHAs)

| PR | Step | Merge | What |
|---|---|---|---|
| #52 | 09 | `d74d588` | Anthropic seam hardening — `ANTHROPIC_NOT_REGISTERED` typed refusal, key-alone branch deleted, well-formed slice |
| #57 | 11 | `8ac41d2` | read-only Ask attribution report over `ask_usage` + hygiene #44/#82 |
| #59 | 11 | `8a00ea2` | model-aware embedding pricing; unpriced embed model refused before reservation |
| #60 | 12 | `fed1d03` | provider dimension on analysis dispatch; allowlist `{openai}`; `pricedFor`; identity stamped with provider |
| #61 | 12 | `ef0bba8` | offline eval results identity decoupled from the live registry constant |
| #62 | 16 | `c75bd99` | log-drain design (docs — audit its claims against #64) |
| #64 | 16 | `1e06112` | `/api/logs/drain` receiver, HMAC, `runtime_logs` migration 0029, retention sweep |
| #65 | 22 | `7f267bd` | `scripts/map-remap.ts` `--base-ack` fail-closed guard + runbook |

If 11b's PR (`48h/ws2-routing-20260905-auto-scorecard-gate`) is open when you start, audit
its branch diff as a ninth item under lenses 1, 2 and 6; if not, step 26 covers it.

Reports whose claims you are testing: `docs/reviews/ANTHROPIC-SEAM-HARDENING-2026-09-05.md`,
`WS-2-1-ASK-PARITY-2026-09-06.md`, `WS-2-2-PROVIDER-CORE-2026-09-06.md`, `LOG-DRAIN-2026-09-06.md`,
`MAP-REMAP-RUNBOOK-2026-09-06.md`, `docs/designs/LOG-DRAIN.md`, `PLAN-WS-2-routing-matrix-2026-09-05.md`.

## Lenses (one finder each; every finder reads the whole diff plus every file it touches)

1. **Ruling 4 — fail-closed spend.** Any path where a provider/model/effort outside the
   registry reaches `tryReserve` or a client constructor? `pricedFor` with an absent
   `provider` field must mean `openai`, never "any" (WS-2-2 attack item 4). `EMBED_UNKNOWN_PRICE_PER_MTOK`
   ≥ every priced entry, and `estimateEmbedCostUsd` bit-identical for the default model. Any
   new row/cap without the ruling-4 ordering note.
2. **Rulings 8/9 — metering inside `analyze()`; per-site degradation unchanged** (digest
   throws typed; llm-match degrades to keyword; /ask degrades to `v2-lexical-only`). The step
   09 contract: refusal class and message unchanged; the "never reserved" assertions intact;
   no key-presence auto-selection anywhere (`git grep -n "ANTHROPIC_API_KEY" -- src`).
3. **Ruling 13 — map lock.** `model-config.ts` lock predicate and `map-prompts.ts` extractor
   basis untouched; WS-2-2 attack item 1: the `provider === "openai" || !providerAllowed`
   model-blanking scope — can a non-openai provider slip past the lock with a blank model?
   `scripts/map-remap.ts`: default target still refused without `--base-ack`; the ack compares
   the HOST, not the URL (the report's mutation table) — try `http://localhost:3000@evil`,
   `http://127.0.0.1.evil`, IPv6 loopback, a trailing dot, uppercase.
4. **Identity.** Every persisted output that carried `openai:` / `registryVersion` still does,
   byte-identical for provider `openai`: `openai-provider.ts`, `synthesize.ts`,
   `embeddings/client.ts`, `schema.ts:615`, `map-worker.ts`, `digest.ts`, `llm-match.ts`,
   `validation/run.ts`, `entity-audit/route.ts`; JSON key order vs `quality-funnel.ts:474-475`
   (WS-2-2 item 6); `analysisApproval` arity at every caller (item 5).
5. **Eval-plane isolation.** `git diff 2203150 98294c5 --stat -- docs/evals/analysis/` must be
   EMPTY except what PR #58 (step 07, not under audit) added; #61: can an offline file be
   misclassified as live, or a live file skip its version check (item 8)? `isolation.test.ts`
   and `import-graph.test.ts` still meaningful after #60's SDK-scan change (WS-2-1 item 5).
6. **Tests as evidence — mutate and run.** For every pin the reports claim: remove the guard,
   run `npm test`, record the failing test names, restore. Minimum set: #59's
   refuse-before-reserve (move the price check after `openaiEmbedBatches`); #60's allowlist
   check (expect the report's 13 failures across six files — count them); #57's read-only
   source scan (write verb in code, not a comment); #64's HMAC verification (flip one byte of
   the signature; skip the timestamp); #65's `assertBaseAck` (delete the call → exactly one
   failure, per the report). Then WS-2-1 item 2 and WS-2-2 item 7: **no existing pin was
   deleted rather than extended** — diff the assertions in every modified test file.
7. **Log-drain receiver as an attack surface** (#64): signature check constant-time and
   before any parse; body size bound; a request with a valid signature but a forged
   `deployment_id`/`request_path`; the self-ingestion loop actually dropped at insert;
   retention sweep bounded and never touching another table; `LOG_DRAIN_SECRET` unset → 503
   and no row; nothing written but the documented columns; `message` cannot carry a secret
   the route logs itself; `request_path` normalization (`?which=fast` loss — is any audit
   query wrong because of it?).
8. **Secrets, env, docs truth.** `git diff 2203150 98294c5 | grep -iE 'key|secret|token|postgres://'`
   reviewed line by line; OPEN-TASKS / AGENTS standing text matches the code after these PRs
   (list every stale line for step 25); every "Proposed AGENTS.md changes" block in the five
   reports checked for accuracy before step 25 applies them.

## Output — the register

`docs/reviews/WS-2-AUDIT-FINDING-REGISTER-2026-09-06.md`: for each finding — id, severity
(blocker / major / minor), PR, file:line, claim, how it was reproduced, refutation summary
(3 refuters, verdict), exact remediation (diff-level, for step 23). Then a per-PR verdict:
merge-stands / fix-before-deploy / revert — the deploy in step 27 reads this column. Then
the "stale standing text" list for step 25 and the mutation log (which pins were mutated,
what failed). Close with COMMON §5 sections. No code changes anywhere; the only commit is
the register.
