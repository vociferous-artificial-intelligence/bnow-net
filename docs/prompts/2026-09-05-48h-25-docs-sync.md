# Step 25 — Docs sync: statuses, snapshot, cross-references, program log — SKETCH (Wave 4)

| | |
|---|---|
| Model / effort / mode | Sonnet / medium / plain session |
| Worktree | `48h-gov-20260905`, step branch `…/docs-sync` |
| Window | H40 → H44 (after the last Wave-4 merge) |
| Depends on | everything merged by CP4; step 15 (so the log is reunified and you append under `## Decision log`) |
| Rewrite from | every closing report produced in the window (list them here at CP4) |
| Spend | $0 |
| Closing report | `docs/reviews/PROGRAM-48H-DOCS-SYNC-2026-09-07.md` |

**DECISIONS BINDING — added 2026-09-09 (read before anything else).** (1) Gate: every
Stage 3 PR merged — 18, 19, 20, 21, 23r, 23c, 24, 32, 33, 34 — `gh pr list` empty, and the
CP6/CP7 lines exist in INDEX §10. The launcher reset your lane branch `48h/gov-20260905` to
`origin/main`; cut `48h/gov-20260905-docs-sync` from it. Model: Sonnet (steps.tsv). $0, no
`.env.local`. (2) **Already done — do not redo:** (f1)–(f15) were signed 2026-09-07
(`1642971`; `grep -c UNSIGNED AGENTS.md` = 0); the R.16 C5-m entry, the #33 status and the
credentials rows were applied 2026-09-08; archive passes 9 and 10 cut the inline log to
2026-09-05 (D5's 7-day window is NOT amended — your pass moves only entries older than
today−7 and only if `wc -c AGENTS.md` needs it; run `bash scripts/check-decision-log-move.sh`
before committing any move). (3) **Your work order is three lists, applied exactly once per
line:** the WS-2 register's 22-item "Stale standing text" list (items 8 and 17 are
do-not-apply instructions — WS2-F08; resolve every anchor by content, never by the register's
line number — WS2-F34), the WS-3 register's 5-item list, and the "Proposed AGENTS.md changes"
blocks of `WS-2-4-EVAL-PARITY-2026-09-06.md`, `RELIABILITY-PROOFS-2026-09-06.md` (its
directory-map correction for `src/lib/cron/` and its proposed decision-log entry),
`WS-7-2-CITATION-MODE-2026-09-07.md`, `WS-7-3-DESCRIPTORS-2026-09-07.md`,
`WS-7-4-ESTIMATIVE-2026-09-07.md`, `AUDIT-REMEDIATIONS-2026-09-07.md` (both lanes),
`WS-3-3-EVIDENCE-POPULATION-2026-09-06.md` and `WS-3-5-SCOREBOARD-AND-SOAK-PREP-2026-09-07.md`.
Before appending any proposed decision-log entry, grep `AGENTS.md` for its title — several
were already applied by the operator at CP5/CP6; never duplicate one. (4) **Facts the
standing text must now say:** `main` is code-ahead of production by every PR since `883e5e3`
(list them by number from `git log --first-parent --merges 883e5e3..main`); migrations
0028/0029/0030 are on `main` and NOT applied to production (#111; production = 29 rows);
the Anthropic digest path is on `main` **dormant** (allowlist widened for `digest` only,
priced rows `claude-haiku-4-5-20251001` $1/$5 and `claude-sonnet-5` $2/$10, nothing enabled in
any Vercel environment); the eval plane is no longer OpenAI-only after #75/#85
(`--provider` flag; provider-qualified identity) — any sentence saying
`EVAL_DISPATCHABLE_PROVIDERS` is `["openai"]` is stale; #102/#103 are DOWNGRADED to
"fork-proven" (step 21), not closed; #93's receiver is built, unregistered, and
`scripts/audit-cron.ts` reports runtime-log coverage as a number; #33's remap driver was
EXECUTED 2026-09-08 (D7, $0.046263, measured $0.0660 per 1k pairs). (5) **OPEN-TASKS status
lines to close or advance in place** (numbers stable): #33, #93, #102, #103, #110 (fixed by
23r if its PR merged — cite the SHA), #112 (guard landed by 23r?), #113, #114 and #116 (add
the register ids WS3-F01 / WS3-F08; fixed by 23c?), #117, #118, #119 (the DEFER bundle filed by
step 23 — confirm it lists every deferred id from both registers). (6) **Disclosures to
record where the program log records models:** step 18's register ran on Fable 5.1 (its
own disclosure 1); step 17 accepted bounded verification (A3). (7) Re-measure unit counts on
the merged tree from your own worktree (`npm test`, never copy a lane's number) and write
them with the SHA. (8) The INDEX §10 program log is operator-written: draft its Stage 3 close
line in your report; do not append it yourself. (9) Draft the window's closing decision-log
entry UNSIGNED for step 27, at the END of `## Decision log` (before `## Conventions`).
(10) Report file name stays `docs/reviews/PROGRAM-48H-DOCS-SYNC-2026-09-07.md`.

Read `docs/prompts/2026-09-05-48h-COMMON.md` first, then every `docs/reviews/*-2026-09-0[5-7].md`
report from this program.

## Prompt shape (fill in at CP4)

In place, one commit per file: `docs/OPEN-TASKS.md` — status lines for every task a report
closed/advanced (#33 runbook, #39/#78 checklist, #44, #79 drained, #82, #83, #84 (still open
until a deploy records it), #85 (deferred), #89, #92 done, #93 receiver built/registration
pending, #97(a), #100, #102/#103 proofs per O3, #105 memo, #106 cases authored/run, new
entries for deferred audit findings) — numbers stable, close in place; `docs/CURRENT-STATE.md`
— routing, validation, evaluation, reliability sections to what `main` now holds (code-ahead
of production; nothing deployed by this program unless step 27 did); AGENTS.md snapshot
(≈105-360) corrected in place (main tip, open PRs, migrations 0028-0030 present in code and
NOT applied to production — say so explicitly), directory map for any new modules, Next steps
item 2; `docs/reviews/EVAL-SUCCESSOR-PLAN-2026-09-04.md` — a dated addendum (not an edit)
pointing at the injection-cases report, the exposure ledger, the val-typ-005 memo and the
v3-vs-v4 decision; the program INDEX §10 log. Draft the closing decision-log entry for the
window (what merged, what is code-ahead, what is unapplied, where the reports are) as
UNSIGNED for step 27. Verify with `git grep` that no doc still claims something a report
contradicts (list the greps you ran).
