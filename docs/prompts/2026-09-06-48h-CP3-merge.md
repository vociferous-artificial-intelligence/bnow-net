# Checkpoint 3 — merge queue (2026-09-06 evening, after Wave 3)

| | |
|---|---|
| Model / effort / mode | Opus / high / plain session — **attended, normal permissions** |
| Where | the main checkout `/Users/go/code/bnow-net`, on `main` |
| Spend | $0. No DB. No deploy. No `.env.local` copied. |
| Output | `main` pushed with every queued PR merged; one CP3 line in INDEX §10; terminal report. |

Read `docs/prompts/2026-09-05-48h-COMMON.md` §3, §4.10 and §4.11 first, then
`docs/prompts/2026-09-06-48h-CP2-merge.md` — its **Fence**, **Preflight**, per-PR procedure,
**If a PR reports conflicts** and **After the last merge** sections apply here unchanged.
You merge; you launch nothing; you delete no branch; you create no worktree.

## Queue — this order, one PR at a time, `git pull --ff-only` after each

Stacked PRs: merge the parent, then `gh pr edit <child> --base main`, re-check
`mergeStateStatus`, merge the child. Never merge a PR whose base is another PR's branch.

1. **#73** — step 13b, migration 0030 conflict observations — `48h/ws3-conflict-20260905-mig-observations`.
   After it lands: `node -e "const j=require('./drizzle/meta/_journal.json');console.log(j.entries.slice(-3).map(e=>e.idx+' '+e.tag).join(' | '))"`
   must print `28 … | 29 0029_runtime_logs | 30 0030_conflict_observations`, and
   `ls drizzle | tail -4` ends `0030_conflict_observations.sql, 9999_claim_source_trigger.sql, meta`.
   If not, stop and report.
2. **#70** — step 14 PR 1, series-aware edition discovery — `48h/ws3-gazetteer-20260905-edition-discovery`.
3. **#71** — step 14 PR 2, conflict-validate route, stacked on #70 — `48h/ws3-gazetteer-20260905-conflict-validate-route`.
   Both #70/#71 and #73 may touch `src/lib/usage/cron-run.ts` (13b added the optional
   `runId` callback argument; 14 said it used the single-argument form). If the rebase of #70
   or #71 conflicts there, stop and paste the block — that one is a code conflict.
4. **#67** — step 11b, Auto scorecard gate — `48h/ws2-routing-20260905-auto-scorecard-gate`.
5. **#68** — step 30 PR 1, tradecraft crosswalk doc — `48h/ws7-docs-20260905-step30-crosswalk`.
6. **#69** — step 30 PR 2, public `/methodology`, stacked on #68 — `48h/ws7-docs-20260905-step30-methodology-page`.
7. **#72** — step 31, retention policy + no-delete scan, stacked on #69 — `48h/ws7-docs-20260905-step31-retention`.
8. **Step 17's register** — `48h/audit-ws2-20260905-finding-register` — ONLY if its PR is open
   when you reach it (docs only; it is the audit of `98294c5`). If not open, leave it for CP4.

Not in this queue: #47, #48, #51 (operator housekeeping); anything HELD.

## Rebases

A conflicting PR is rebased in ITS worktree (`48h-<worktree>-20260905`) with
`git rebase --update-refs origin/main` from the TOP of its stack (for #68/#69/#72 that is
`…-step31-retention`; for #70/#71 it is `…-conflict-validate-route`), so the parents move with
it; then `--force-with-lease` every moved branch. Before touching a worktree:
`lsof -a -d cwd -c claude | grep <worktree>` must print nothing (COMMON §4.11) — if a session
is live there, stop and report instead. Resolve only PROGRESS/BLOCKERS/OPEN-TASKS/decision-log
tails (keep both, `main` first); `docs/OPEN-TASKS.md` may carry #108 from step 31 and status
lines from 14 — keep both. Any conflict in code, `drizzle/`, `src/db/schema.ts`: stop, paste
the block, report.

## After the last merge

CP2's closing procedure: `git pull --ff-only && npm run typecheck && npm run lint && npm test`,
then ONE CP3 line in INDEX §10 (merge order with SHAs, conflicts and resolutions, unit
count/files, `ls drizzle | tail -4`), commit `docs: 48h program log — CP3 merge queue landed`,
push, print `gh pr list --state open`, stop. Also print
`git worktree list | grep -c mut` — the audit's mutation worktrees (`mut1`…`mut12`) belong to
step 17; do not remove them; just report the count.
