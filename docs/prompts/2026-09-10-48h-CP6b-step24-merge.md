# CP6b — merge step 24 (#95, #96) and empty the Stage 3 queue (agent prompt, 2026-09-10)

**You are the CP6b merge agent.** Step 24 (WS-3.5 conflict observations view + WS-3.6-prep
enablement checklist) delivered **two** PRs and its session has exited. You work the plan's card
**4.2** (`docs/prompts/2026-09-09-48h-completion-plan.md` §4.2), bound by that file's **§7**,
`CLAUDE.md` and `AGENTS.md`, and you use the CP6 resolver's procedure
(`docs/prompts/2026-09-10-48h-CP6-resolve-and-finish.md`) for the one conflict this queue
predicts. Card 4.2 is the authority; everything this file adds is either a fact verified against
the refs on 2026-09-10 or a ruling the operator recorded that day.

**Authority you have, and its limit.** You may **rebase one head branch in its own worktree and
force-push it with lease** — the same authority CP6's resolver had, narrowed to one branch, once,
and only when GitHub reports that PR `CONFLICTING`. Everything else is fetch → merge → gate with
hard stops. **You resolve nothing by hand.** The only conflict this queue predicts is
`docs/PROGRESS.md`, and the `merge=union` driver settles it during the rebase without an edit.

**Model / effort / mode.** Opus / high, attended, a FRESH session (`/clear` or a new terminal) in
`/Users/go/code/bnow-net` on branch `main`:
`cd /Users/go/code/bnow-net && caffeinate -ims claude --model opus`, then paste this file. Default
permission prompts are fine — the operator is present and the one force-push is worth a glance.
Not `xhigh`: nothing here needs depth, the discipline is carried by the STOP rules.

**Where you run.** Native macOS terminal only (never Cowork/remote-mount — `lsof`, `gh` and
`git worktree list` only tell the truth from the Mac, and a remote-mount session writes
`/sessions/…` gitdir paths git cannot resolve later). Your cwd is the main checkout; every
worktree operation is `git -C <wt> …` except the gate in §3.c, which needs `npm` inside it. You
never create a worktree.

**Operator, while this runs:** do not commit on local `main`. CP6 lost two `--ff-only` pulls to an
unpushed docs commit; the INDEX §10 CP6b line is written after the agent reports, not during.

---

## 0. State you inherit (read from the refs 2026-09-10; re-verify in §1)

- `main` = `origin/main` = **`23b5d7a`** ("docs: 48h program log — CP6 landed (19, 23r, 23c)").
  Gate recorded at CP6 close: typecheck clean · lint 0 errors · unit **4,557 / 290** · build PASS ·
  **32** `.sql` under `drizzle/`.
- Step 24's two PRs, both opened from `origin/main` `23b5d7a`, both **0 commits behind `main`**:

| PR | head branch | commits ahead | contents |
|---|---|---|---|
| **#95** | `48h/ws3-gazetteer-20260905-conflict-observations-view` | 3 | **code** — the DB-backed `/conflicts/**` view (`db-product-view.ts`), `benchmark-key.ts`, the C10 `/scoreboard` relabel, `robots.ts`, `conflict-feature-off.itest.ts`; docs: `PROGRESS.md`, `RETENTION-AND-PRESERVATION.md` |
| **#96** | `48h/ws3-gazetteer-20260905-soak-prep` | 3 | **docs only** — `docs/reviews/CONFLICT-SHADOW-SOAK-ENABLEMENT-2026-09-07.md`, `docs/reviews/WS-3-5-SCOREBOARD-AND-SOAK-PREP-2026-09-07.md`, `PROGRESS.md` |

- **Neither is stacked**, and neither contains the other. Both carry the same shared plan-block
  commit `1e80d18` ("docs: step 24 plan block"), which is also the tip of the **local-only** lane
  branch `48h/ws3-gazetteer-20260905`; that branch does not exist on `origin`, so no PR can be
  based on it. Step 24's own report says the same ("Neither is stacked… No `gh pr edit --base`
  retarget is needed"). §3.a checks anyway — the CP5 stack rule costs one command.
- **The only file both branches touch is `docs/PROGRESS.md`** (verified by `comm` over the two
  name-only diffs). So: whichever PR you merge FIRST merges clean, and the one you merge SECOND
  reports **CONFLICTING** on GitHub, on `docs/PROGRESS.md` alone. Same union blind spot CP6
  recorded: `.gitattributes` gives `docs/PROGRESS.md` and `docs/BLOCKERS.md` the `merge=union`
  driver, GitHub does not honour it, the local rebase resolves it without hand-editing.
- **The local pre-check is blind to that conflict.** `git merge-tree $mb origin/main
  origin/<branch>` reads `0` for both branches — and `0` for the two branches merged against each
  other, which is the case that actually arises. Run card 3.2's pre-check for what it does catch
  (a real content conflict), but **`gh pr view <n> --json mergeable` is the reading that decides**
  whether §3.b runs.
- Neither branch touches `drizzle/`, `src/db/schema.ts`, `vercel.json`, `package.json` or
  `package-lock.json` — all verified byte-unchanged on both. Any of them in a diff is a STOP.
- Worktree for both branches: `/Users/go/code/bnow-net-worktrees/48h-ws3-gazetteer-20260905`; lane
  branch `48h/ws3-gazetteer-20260905` — **never** check it out, rebase it or push it. The step ran
  attended there with the trimmed four-key `.env.local`.
- Step 24's own gate, measured in that worktree against this same `main`: unit
  **4,557 / 290 → 4,599 / 293**, typecheck/lint clean, fork itest
  `src/integration/conflict-feature-off.itest.ts` **25/25** on disposable Neon branch
  `br-patient-bar-ath7n7n8` (created and deleted by `scripts/test-integration.sh`). Because `main`
  has not moved since, those are numbers you should reproduce exactly. If you do not, the reading
  wins.

---

## 1. Preflight — once

```
cd /Users/go/code/bnow-net
git status --porcelain
git fetch --prune origin
git log --oneline -1 main
git log --oneline -1 origin/main
lsof -a -d cwd -c claude | grep bnow-net-worktrees
scripts/launch/status.sh
gh pr list --state open
git -C /Users/go/code/bnow-net-worktrees/48h-ws3-gazetteer-20260905 status --short
git -C /Users/go/code/bnow-net-worktrees/48h-ws3-gazetteer-20260905 branch --show-current
cut -d= -f1 /Users/go/code/bnow-net-worktrees/48h-ws3-gazetteer-20260905/.env.local
git rev-list --count origin/main..origin/48h/ws3-gazetteer-20260905-conflict-observations-view
git rev-list --count origin/main..origin/48h/ws3-gazetteer-20260905-soak-prep
npm run typecheck && npm run lint && npm test
```

**Expect:** clean tree; both `git log` lines **`23b5d7a`** (or a later docs-only commit — read its
`--stat`; anything under `src/` or `drizzle/` is a STOP); `lsof` silent (the grep is on
`bnow-net-worktrees` because you are yourself a `claude` process with cwd
`/Users/go/code/bnow-net`); `status.sh` with no `run` session; `gh pr list` showing **#95 and
#96** — record any other open PR (a `docs/branding-strategy-20260909` PR, say) and carry it into
the report without touching it: plan card 4.3 wants that list EMPTY before step 25, and clearing a
non-48h PR is the operator's call, not yours; the worktree with an empty `status --short` and on
some branch (either lane or step branch — you check out explicitly); `.env.local` holding exactly
`DATABASE_URL`, `DATABASE_URL_UNPOOLED`, `NEON_PROJECT_ID`, `NEON_API_KEY` (if it is absent or
holds any other key, STOP — posture is `scripts/launch/env-posture.sh`'s alone, run by the
operator; you need it only if §3.c runs an itest); both counts **`3`**; the gate green at
**4,557 / 290** — that is the "before" of every later before→after.

Anything else: **STOP and report.** Do not start the queue.

---

## 2. Order — **#95 then #96** — and why

Three reasons, all of them about cost, not correctness (either order lands the same tree):

1. Only one PR then needs a rebase, and it is the **docs-only** one.
2. **#95 merges onto a `main` byte-identical to the tree its fork itest passed on** (25/25 at
   `23b5d7a`), so that evidence carries over unchanged and no itest re-run is required.
3. The lane's docs PR lands last, as lane C's `#94` did in CP6.

Run §3.a → §3.e for **#95** to completion before touching **#96**. `main` moves when #95 lands,
and that move is precisely what makes #96 conflict — do not batch, do not skip, do not
pre-rebase #96 before #95 is merged.

If §3.a instead reads **#95** `CONFLICTING` (something landed on `main` under you), the order
still holds: rebase #95 per §3.b and run its worktree gate at §3.c **including** the fork itest.

---

## 3. Per PR — cards a → e

`<n>` and `<branch>` come from §0's table. Cards b and d share the shell variable `wt`,
set at the top of 3.b: `wt=/Users/go/code/bnow-net-worktrees/48h-ws3-gazetteer-20260905`.

### 3.a Fence, stack check, mergeability

```
cd /Users/go/code/bnow-net
lsof -a -d cwd -c claude | grep bnow-net-worktrees
git fetch --prune origin
gh pr view <n> --json baseRefName,headRefName,mergeable,mergeStateStatus -q '[.baseRefName,.headRefName,.mergeable,.mergeStateStatus]'
mb=$(git merge-base origin/main origin/<branch>); git merge-tree $mb origin/main origin/<branch> | grep -c '^+<<<<<<<'
git diff origin/main...origin/<branch> --stat -- drizzle src/db/schema.ts vercel.json package.json package-lock.json
```

**Expect:** `lsof` silent; `baseRefName` = `main`; the pre-check `0`; the `--stat` line prints
nothing.

- `mergeable` **`MERGEABLE` / `CLEAN`** → skip b, c, d; go to **3.e**.
- `mergeable` **`CONFLICTING`** → **3.b**. (Expected for the second PR, on `docs/PROGRESS.md`.)
- `mergeable` **`UNKNOWN`** → wait 10 s and re-view, up to **6** polls. Still `UNKNOWN` → STOP.
- **`baseRefName` not `main`** → `gh pr edit <n> --base main` **once**, wait ~30 s, re-view, and
  record it as an incident (the CP5 stack rule, plan §7). A second retarget, or `CONFLICTING`
  that survives both the retarget and 3.b → STOP with the output.

### 3.b Rebase in the PR's own worktree — **only when 3.a read `CONFLICTING`**

```
cd /Users/go/code/bnow-net
wt=/Users/go/code/bnow-net-worktrees/48h-ws3-gazetteer-20260905
lsof -a -d cwd -c claude | grep bnow-net-worktrees
git -C $wt fetch --prune origin
git -C $wt status --short
git -C $wt checkout <branch>
git -C $wt log --oneline origin/main..HEAD | wc -l
git -C $wt rebase origin/main
git -C $wt status --short
git -C $wt log --oneline origin/main..HEAD
git -C $wt grep -n '^<<<<<<<\|^>>>>>>>\|^=======$' -- docs src
git -C $wt diff origin/main --stat
git -C $wt diff origin/main -- docs/PROGRESS.md | head -80
```

**Expect:** `lsof` silent; empty `status --short` before the checkout; pre-rebase count **`3`**;
**the rebase completes with no stop** — `docs/PROGRESS.md` is settled by the union driver and it is
the only overlapping file. After it: empty `status --short`; commit count **`2`** for #96 — the
shared plan block `1e80d18` is already on `main` via #95, and a rebase dropping an already-applied
commit is correct, not a loss (`3` if you are rebasing #95 instead, whose commits `main` does not
have); **no conflict markers**; the `--stat` showing only that PR's own files from §0's table and
nothing else; the `PROGRESS.md` diff adding only this PR's own block, in timestamp order, nothing
duplicated.

**If the rebase stops** — anything in `UU` — **STOP**. Paste the path, the `<<<<<<<` hunk count and
the full unedited conflict text. Do not hand-resolve: the only resolution authorized in this queue
is the one the union driver performs silently. Never `--skip` a commit; never `--abort` and retry a
different way.

### 3.c Gate in the worktree — **only when 3.b ran**

```
cd /Users/go/code/bnow-net-worktrees/48h-ws3-gazetteer-20260905
npm run typecheck && npm run lint && npm test
cd /Users/go/code/bnow-net
```

**Expect:** typecheck clean · lint **0 errors** · for a rebased **#96** (docs only) a unit count
**equal** to the current `main` reading; for a rebased **#95**, higher.

For a rebased **#95 only**, also run the fork itest from inside the worktree:
`npm run test:integration -- src/integration/conflict-feature-off.itest.ts` — expect **25/25**;
record the disposable Neon branch name the runner prints and that it printed `deleted`. **$0** (the
worktree holds only the four Neon keys; no provider key exists there).

A failure here is a **STOP** with the output. Do not patch code, do not edit a test. The one
exception: if the failure is a stale-dependency error (a resolver/loader failure, not an
assertion), `npm ci --silent` in the worktree once and re-run, and record it as an incident.

### 3.d Push with lease, confirm GitHub agrees — **only when 3.b ran**

```
git -C $wt push --force-with-lease origin <branch>
cd /Users/go/code/bnow-net
gh pr view <n> --json baseRefName,mergeable,mergeStateStatus -q '[.baseRefName,.mergeable,.mergeStateStatus]'
```

**Expect:** the push accepted — a refused lease means the branch moved under you: **STOP**, do not
force; `mergeable` reaching `MERGEABLE` / `CLEAN` within 6 polls of 10 s (the first is often
`UNKNOWN`). Still `CONFLICTING` after the rebase → **STOP** with the output; do not rebase again.

### 3.e Merge and gate `main`

```
cd /Users/go/code/bnow-net
gh pr merge <n> --merge
git pull --ff-only
git log --oneline -1
npm run typecheck && npm run lint && npm test
LLM_DISABLE=1 OPENAI_API_KEY= ANTHROPIC_API_KEY= DATABASE_URL=postgres://x:y@localhost/z npm run build
ls drizzle | grep -c '\.sql$'
git diff --stat HEAD~1 HEAD -- drizzle src/db/schema.ts vercel.json
```

`--merge` only — never `--squash`, never `--rebase`, never the web UI. Record the merge SHA.

**Expect:** a merge commit; typecheck clean; lint 0; unit **4,557 / 290 → 4,599 / 293 after #95**
and **unchanged at 4,599 / 293 after #96** (docs only); build **PASS**; `32`; an empty diff. A
count below the previous reading, or a red gate on `main`, is a **STOP** — no revert, no fix, no
next PR.

---

## 4. After #96 — close the queue, launch nothing

```
cd /Users/go/code/bnow-net
gh pr list --state open
git log --oneline --first-parent 23b5d7a..main
git status --porcelain
scripts/launch/status.sh
git worktree list
```

**Expect:** no open step-24 PR (report anything else still open); **two** merge commits; clean
tree; no `run` session; no `prunable` and no `/sessions/…` entry in `git worktree list`.

Step 24 is now merged, so plan card **4.3**'s gate — "steps 19, 23r, 23c and 24 all merged;
`gh pr list --state open` empty; the INDEX §10 CP6 line committed" — is satisfied except for
whatever that open list still shows and the CP6b record. **Say so in the report and launch
nothing:** step 25 (docs sync, Sonnet, `48h-gov-20260905`) is the last Stage 3 launch and the
operator runs it from card 4.3. Do not write INDEX §10, do not touch the tracker, do not write a
review file, do not commit anything — paste the readings and the operator drafts the CP6b record.

---

## 5. Hard rules

- The rebase and force-push authorized here are on **one** head branch (`…-soak-prep`, or
  `…-conflict-observations-view` if the readings invert), **once**, and only after 3.a read
  `CONFLICTING`. No other history rewrite. Never the lane branch
  `48h/ws3-gazetteer-20260905`, never `main`, never a merged PR's branch.
- `lsof -a -d cwd -c claude | grep bnow-net-worktrees` before every merge and every rebase, not
  once at the start. A live session in the worktree is a STOP.
- **No hand-edits, at all.** The union driver is this queue's only conflict resolution. No commits
  of your own. If one somehow becomes unavoidable: `area: imperative summary`, and **no vendor
  branding** — no `Co-Authored-By`, no "Generated with", no model or vendor names, in the message,
  the code, or the file contents.
- Never touch any `.env.local` (posture is `scripts/launch/env-posture.sh`'s alone), never launch a
  session, never consume a claim. No provider call, no deploy, no Vercel read or write, no
  migration applied anywhere. **$0** — the fork itest is $0 by posture and is the only thing that
  touches Neon.
- Do not commit on local `main` while you work there (CP6's `--ff-only` incident).
- `git cherry` under-predicts dropped commits under the union driver — count with
  `git rev-list --count` / `git log --oneline origin/main..HEAD`, as the cards do.
- If a reading disagrees with §0 — a third step-24 PR, a different commit count, a conflict in any
  file other than `docs/PROGRESS.md`, a base that is not `main`, a migration or `vercel.json` in a
  diff — **the reading wins: STOP and report it.**

---

## 6. Report shape (paste this, nothing else)

```
CP6b — step 24 merge — 2026-09-10
Start:  main = <sha>   unit = <n> / <m>   open PRs = <list>
  #95  base=<b>  mergeable=<v> after <p> polls  rebase: <none | 3→<k> commits, PROGRESS via union>
       worktree gate: <n/a | typecheck ok, lint 0, unit <n>/<m>, itest 25/25 on fork <name>, deleted>
       push: <n/a | ok>   merge=<sha>
       main gate: typecheck ok, lint 0, unit <before>→<after>, build PASS, drizzle 32, diff empty
  #96  base=<b>  mergeable=<v> after <p> polls  rebase: <none | 3→<k> commits, PROGRESS via union>
       worktree gate: <…>   push: <…>   merge=<sha>
       main gate: <…>
End:    main = <sha>, unit <n> / <m>, open PRs <none | list>, two merge commits: <sha> <sha>
Card 4.3 gate: 19/23r/23c/24 merged = <yes/no>; open list empty = <yes/no>. Nothing launched.
Stopped at (if any): <PR, card, exact command output, full conflict text, state left>
Incidents: <every extra poll, every retarget, every npm ci, every surprise — or "none">
```
