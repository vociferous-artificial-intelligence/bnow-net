# Step 20 — WS-2.4 eval-plane provider parity (+ 20b Anthropic PRs, option B) (Wave 4, after CP3)

| | |
|---|---|
| Model / effort / mode | Opus / high / plain session |
| Worktree | `/Users/go/code/bnow-net-worktrees/48h-ws2-routing-20260905` (idle after 11b — check per COMMON §4.11), step branches `48h/ws2-routing-20260905-eval-provider-flag`, `48h/ws2-routing-20260905-entity-audit-prompt-extract`, cut from `origin/main` after CP3. |
| Window | after CP3; 17's register may add remediations that step 23 applies — 20 does not wait for it |
| Depends on | #60, #61 on `main` (provider dimension; registry still `analysis-reg-v1`, R2: no bump); #67 (the `unscorecarded` provider literal exists — do not collide with it); R12 keep `json_object`; R13 `schemaMode` in the identity |
| Decisions | **D2 = B, signed 2026-09-06** (INDEX §2.2, operator column) — Anthropic is IN SCOPE. An earlier header here read "20b is NOT run this window: D2 = A now, seam-ready for B/C"; that was the pre-answer recommendation column, and it is void. **Current word on what is unblocked: `docs/prompts/2026-09-07-48h-CP4-and-completion-plan.md` §5.3 row A** — 20b is fully unblocked: R6 (reuse `LLM_SPRINT_USD_CAP` + `LLM_DIGEST_USD_CAP`, no new env), R7/R7-b price rows `claude-haiku-4-5-20251001` $1/$5 and `claude-sonnet-5` $2/$10 (alias, not a dated snapshot — re-verify at the first real invoice). Two conditions ride on it: **(f1)/(f12) signed into the log first** — the signed R7 entry still reads "Sonnet held" and the log wins where log and sheet disagree, so if the batch is still unsigned when you start, read (f1)/(f12) over that entry (CP4 §5.2 item 1) rather than refusing the Sonnet row; and **`mapreduceProviderTag()` must be made provider-aware BEFORE the Anthropic digest path is enabled** ((f13) — otherwise a Claude-synthesized digest is durably stamped `openai:…`). **Option C stays out of this round** (provisional yes, not needed for ≥2 weeks — leave the seam cheap, open no C PR). Whether 20b is built this window is settled by this session's kickoff message, not by this file. |
| Rewrite from | PLAN-WS-2 §WS-2.4 (PR-2.4-1 eval `--provider`, PR-2.4-2 entity-audit prompt extraction); `docs/reviews/WS-2-2-PROVIDER-CORE-2026-09-06.md` Handoff ("20 / PR-2.4-1": `evalDispatchConfig` already calls the provider-aware path — start there); embeddings coverage lives in the Ask eval runner, not `analysis-eval` — say so and do not build it |
| Spend | $0. Option B needs no key to build (dormant, metered path pinned by mocks); option C needs no endpoint (unroutable-sink itest pattern). |
| Closing report | `docs/reviews/WS-2-4-EVAL-PARITY-2026-09-06.md` |

**READ FIRST — handoff from the 2026-09-08 read-only pass (relaunch note).** This step was
launched at ≈20:27Z without a permission mode, so the session could read but not write or run; it
exited after doing the read-only half. Its output is preserved at
`/Users/go/code/bnow-net-worktrees/logs/step20.readonly-20260908.log` — **read it before
anything else.** It re-verified every `file:line` this prompt and the plan rely on against
`a7ba98b` and lists the corrections (several citations have drifted), plus cross-file breaks the
plan does not mention. Treat that log as the corrected working spec; do not redo its reading.
Two of its findings are binding on B1: (1) build the `mapreduceProviderTag()` fix but say in the PR body that it is latent-only this window (map/reduce stay `{openai}`); (2) the live misattribution risk is `AnthropicProvider.name` via `anthropicModel()` — route it through `resolveWorkloadModel("digest")` per PLAN-WS-2 §5.4 and remove the second model authority; a refused-provider tag must mirror ruling 13 (`model-config.ts:198-201`).

**DECISIONS BINDING — added 2026-09-08.** The (f1)/(f12)/(f13) preconditions named below are
no longer drafts: the whole (f1)–(f15) batch was SIGNED into `AGENTS.md`'s decision log on
2026-09-07 (commit `1642971`; `grep -c UNSIGNED AGENTS.md` = 0). Read the signed entries there —
"2026-09-07 (R7 addendum …)", "2026-09-07 (R7-b …)" for the `claude-sonnet-5` $2/$10 row and
its alias-repoint rider, and "2026-09-07 (T4-b …)" for the `mapreduceProviderTag()` rule — not
`DECISION-ENTRIES-DRAFT-2026-09-05.md`. Ordering gate restated: **`mapreduceProviderTag()` must
be provider-aware BEFORE the Anthropic digest path can be enabled** (else a Claude-synthesized
digest is durably stamped `openai:…`); build that fix first, test-pinned, in 20b's first PR.

Read `docs/prompts/2026-09-05-48h-COMMON.md` first, then PLAN-WS-2 and the step-12 report.

## Prompt shape (fill in at CP2)

PR 1 — `evals: --provider flag, provider-qualified identity, fail-closed placeholder`:
`scripts/analysis-eval.ts` parses `--provider` (default `openai`); `evalDispatchConfig`
resolves approval through the provider-keyed registry; `liveIdentity.provider` is no longer
a literal; `CandidateDispatchIdentity.provider` union widened; configKey carries the provider
only when ≠ `openai` (committed offline files resume unchanged — pin with
`hardening-cli.test.ts`); preflight refuses a provider outside the allowlist before any client
or DB; `--capture-reconcile` keyed accordingly. `docs/evals/analysis/` byte-untouched.

PR 2 — `analysis: extract entity-audit prompt/parse into a pure module (byte-identical request)`:
`src/lib/analysis/entity-audit-prompts.ts` + test proving the route builds the identical
request; note `json_object` vs strict `json_schema` as a DECISION for a later coverage PR.
Embeddings coverage: a short design note in the report that it belongs in the Ask eval runner
(retrieval recall), not `analysis-eval` — no code.

20b (D2 = B — build it unless the kickoff message says otherwise; the (f1)/(f12) and (f13)
preconditions are in the Decisions row): **B** — `pricing.ts` Anthropic entry (R7/R7-b price,
restated in the PR body), provider allowlist `{openai, anthropic}` for `digest` only, digest
routed through `model-config.ts` with `allowedEfforts` semantics per provider, metered
identity-stamped `analyze()` on a row that reuses `LLM_SPRINT_USD_CAP` + `LLM_DIGEST_USD_CAP`
(no new Vercel env) — order pin reserve < fetch < record < parse via fetch mock;
truncated/unparseable still recorded (ruling 8); refusal (`cap_unset`) → no fetch; step 09's
refusal replaced, not bypassed; `import-graph.test.ts` ALLOWED list updated if an SDK is
introduced (prefer raw fetch as today). **C (NOT this round — reference only, no PR: keep
the seam cheap for a later window)** — `<WORKLOAD>_BASE_URL` + provider `openai_compatible`
through `analysisOpenAiClient(cfg)` (ctor spy receives `{maxRetries:0, baseURL, apiKey}` only for
that provider; missing/invalid URL refused before `tryReserve`; loopback-only allowlist
unless the plan says otherwise); the `$0` price class as an explicit `PRICES_PER_MTOK` entry
+ request-cap-only guard shape (INDEX §1.11) — this reverses the 2026-08-25 stance and needs
the decision-log entry in hand before the PR opens.

Acceptance: counts; no env; no paid call; `docs/evals/analysis/` untouched. Handoff: what
step 22 needs from the flag; what the final audit must check for 20b.
