# CP7 — merge step 25 (docs sync) and close Stage 3 (agent prompt, 2026-09-10)

**You are the CP7 merge agent.** Step 25 (docs sync, Sonnet, `48h-gov-20260905`) has delivered
and its session has exited. You work the plan's card **4.4**
(`docs/prompts/2026-09-09-48h-completion-plan.md` §4.4), bound by that file's **§7**, `CLAUDE.md`
and `AGENTS.md`, with the CP6 resolver's rebase procedure
(`docs/prompts/2026-09-10-48h-CP6-resolve-and-finish.md` §3.a–3.c) for the one class of conflict
this queue expects. Card 4.4 is the authority; this file adds facts read from the refs on
2026-09-10 and the operator's rulings for a **docs-only** merge.

**Authority you have, and its limit.** You may rebase each of step 25's head branches in
`/Users/go/code/bnow-net-worktrees/48h-gov-20260905` and force-push with lease, once each, only
when GitHub reports the PR `CONFLICTING`. The union driver settles `docs/PROGRESS.md` /
`docs/BLOCKERS.md` during the rebase. **Any other conflict is a STOP** — including
`AGENTS.md`, `docs/OPEN-TASKS.md`, `docs/CURRENT-STATE.md`: step 25 is the only writer of those
this round, so a conflict there means something merged that the queue did not expect.

**Model / effort / mode.** Opus / high, attended, a FRESH session in `/Users/go/code/bnow-net`
on branch `main`: `cd /Users/go/code/bnow-net && caffeinate -ims claude --model opus`, then
paste this file. Default permission prompts. Not xhigh — the discipline is in the STOP rules.

**Where you run.** Native macOS terminal only; cwd is the main checkout; worktree operations are
`git -C <wt> …`; the worktree gate needs `npm` inside it. Never create a worktree.

**Operator, while this runs:** do not commit on local `main`. The INDEX §10 CP7 line and the
tracker's "Stage 3 DONE" are written after the agent reports.

---

## 0. State you inherit (re-verify in §1)

- `main` = `origin/main` = **`a0d7091`** (merge of #96) or a later docs-only commit whose
  `--stat` touches nothing under `src/`, `scripts/`, `drizzle/`. Gate at CP6b close: typecheck
  clean · lint 0 errors · unit **4,599 / 293** · build PASS · **32** `.sql` under `drizzle/`.
- Step 25's PR(s): read them from `gh pr list --state open --json number,headRefName,baseRefName`.
  Expected: one or more branches named `48h/gov-20260905-docs-sync*`, base `main`, **docs only**
  — `AGENTS.md`, `docs/OPEN-TASKS.md`, `docs/CURRENT-STATE.md`, `docs/reviews/*`, `docs/PROGRESS.md`,
  `docs/prompts/*`, `.env.example` at most. Its closing report:
  `docs/reviews/PROGRAM-48H-DOCS-SYNC-2026-09-07.md` on the branch.
- What step 25 was told (its prompt's 2026-09-09 block): apply both registers' stale-text lists
  and every report's "Proposed AGENTS.md changes" block exactly once; grep `AGENTS.md` for each
  proposed decision-log entry's title before appending (several were applied at CP5/CP6);
  re-measure unit counts itself; draft the window's closing decision-log entry **UNSIGNED**; not
  touch INDEX §10. Two hard limits you check on the merged tree: **`AGENTS.md` under 150,000
  bytes** and **`grep -c UNSIGNED AGENTS.md` = 1** (exactly the one closing entry step 27
  signs — 0 means it forgot, 2+ means it signed something it should not have).

---

## 1. Preflight — once

```
cd /Users/go/code/bnow-net
git status --porcelain
git fetch --prune origin
git log --oneline -1 main
git log --oneline -1 origin/main
lsof -a -d cwd -c claude | grep bnow-net-worktrees
scripts/launch/status.sh
gh pr list --state open --json number,headRefName,baseRefName,mergeable
git -C /Users/go/code/bnow-net-worktrees/48h-gov-20260905 status --short
git -C /Users/go/code/bnow-net-worktrees/48h-gov-20260905 branch --show-current
```

**Expect:** clean tree; both `git log` lines the same SHA (`a0d7091` or a later docs-only
commit); `lsof` silent (the grep is on `bnow-net-worktrees` — you are yourself a `claude`
process in `/Users/go/code/bnow-net`); `status.sh` row 25 `DEAD` and no `run` row; only step
25's PR(s) open, base `main`; the gov worktree clean. Anything else: STOP and report.

Then, per PR, before anything moves — the docs-only check:

```
cd /Users/go/code/bnow-net
git diff --stat origin/main...origin/<branch> -- src scripts drizzle package.json package-lock.json vercel.json .githooks
git diff --stat origin/main...origin/<branch> | tail -1
```

**Expect:** the first command prints **nothing**; the second is the docs summary line. Any
path under `src/`, `scripts/`, `drizzle/` or a lockfile change: STOP — step 25 is docs-only by
its prompt.

## 2. Per PR — in PR-number order

### 2.a Mergeability

```
cd /Users/go/code/bnow-net
gh pr view <n> --json baseRefName,headRefName,mergeable,mergeStateStatus -q '[.baseRefName,.headRefName,.mergeable,.mergeStateStatus]'
```

**Expect:** base `main` (else `gh pr edit <n> --base main` once, record as incident);
`MERGEABLE` / `CLEAN` → go to 2.c. `UNKNOWN` → wait 10 s, re-view, up to 6 times. `CONFLICTING`
→ 2.b.

### 2.b Rebase in the gov worktree (only on CONFLICTING)

```
cd /Users/go/code/bnow-net
git -C /Users/go/code/bnow-net-worktrees/48h-gov-20260905 fetch --prune origin
git -C /Users/go/code/bnow-net-worktrees/48h-gov-20260905 checkout <branch>
git -C /Users/go/code/bnow-net-worktrees/48h-gov-20260905 rebase origin/main
git -C /Users/go/code/bnow-net-worktrees/48h-gov-20260905 status --short
git -C /Users/go/code/bnow-net-worktrees/48h-gov-20260905 grep -n '^<<<<<<<\|^>>>>>>>' -- docs AGENTS.md
```

**Expect:** the rebase completes with no stop (union driver on `PROGRESS.md`); empty status; no
markers. **If the rebase stops in any file** — `AGENTS.md`, `OPEN-TASKS.md`, a review — run
`git -C … rebase --abort` and STOP with `git -C … diff --name-only --diff-filter=U` and the
conflict text: no hand-resolution is authorized in this queue. Then:

```
cd /Users/go/code/bnow-net-worktrees/48h-gov-20260905
npm run typecheck && npm run lint && npm test
git push --force-with-lease origin <branch>
cd /Users/go/code/bnow-net
```

**Expect:** gate green (docs-only, but the gate is the gate); push accepted; then 2.a again
until `MERGEABLE` / `CLEAN`.

### 2.c Merge and gate `main`

```
cd /Users/go/code/bnow-net
gh pr merge <n> --merge
git pull --ff-only
git log --oneline -1
npm run typecheck && npm run lint && npm test
LLM_DISABLE=1 OPENAI_API_KEY= ANTHROPIC_API_KEY= DATABASE_URL=postgres://x:y@localhost/z npm run build
ls drizzle | grep -c '\.sql$'
git diff --stat HEAD~1 HEAD -- drizzle src/db/schema.ts src scripts
wc -c AGENTS.md
grep -c UNSIGNED AGENTS.md
bash scripts/check-decision-log-move.sh HEAD
```

**Expect:** a merge commit (record its SHA); typecheck clean, lint 0 errors, unit count
**unchanged at 4,599 / 293** (docs cannot move it — a change is a STOP); build PASS; `32`;
empty diff; `AGENTS.md` **< 150,000**; `UNSIGNED` count **1**; the archive check PASS **only if
step 25 performed an archive pass** (its report says; if it did not, the script is expected to
fail on the append and that is not a STOP — record which). A red gate or a `wc -c` ≥ 150,000 is
a STOP: no revert, no fix.

## 3. After the last PR — Stage 3 close-out, launch nothing

```
cd /Users/go/code/bnow-net
gh pr list --state open
git log --oneline --first-parent 883e5e3..main | wc -l
git log --oneline --first-parent 883e5e3..main | grep -c 'Merge pull request'
scripts/launch/status.sh
git worktree list
grep -n 'UNSIGNED' AGENTS.md
```

**Expect:** no open PR; the merge count (record it — step 26's list); no `run` session; no
`/sessions/` path (a `prunable` entry named `branding-strategy-20260909` is not a program
worktree — note it, do not touch it); one `UNSIGNED` line, the closing entry. **Stage 3 is
closed** when 18, 19, 20, 21, 23r, 23c, 24, 25, 32, 33, 34 are all merged — they are, once step
25 lands. Do not write INDEX §10, the tracker, or any review file; do not launch step 26 (it is
attended, and the freeze card comes first).

## 4. Hard rules

- Rebase/force-push only step 25's own head branches, only on a GitHub `CONFLICTING`, only in
  `48h-gov-20260905`, once each. Never `main`, never a lane branch, never a merged PR's branch.
- No hand-resolution of any file. No commits of your own.
- `lsof -a -d cwd -c claude | grep bnow-net-worktrees` before every rebase and merge.
- Never `.env.local`, never a launch, never a claim, no provider call, no Vercel, no migration.
  $0.
- A reading that disagrees with §0 — a code path in the diff, a second lane's PR, a unit count
  that moved — wins: STOP and report it.

## 5. Report shape

```
CP7 — step 25 merge — 2026-09-10
Start:  main = <sha>   unit = 4599 / 293   open PRs = <list>
  #<n>  docs-only check: clean | mergeable <reading> | rebase: none|union-only | merge=<sha> | main gate: typecheck ok, lint 0, unit 4599/293, build PASS, drizzle 32, diff empty, AGENTS.md <bytes>, UNSIGNED 1, archive-check <PASS|n/a>
End:    main = <sha>, open PRs none, first-parent merges since 883e5e3 = <count>
Stage 3: CLOSED (all eleven steps merged) | NOT CLOSED because <reason>
Stopped at (if any): <PR, card, exact output>
Incidents: <every retarget, every extra poll, or "none">
```
