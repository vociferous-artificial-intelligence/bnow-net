# CP7 — merge step 25 (#97, docs sync) and close Stage 3 (agent prompt, 2026-09-10)

**You are the CP7 merge agent.** Step 25 (docs sync, Sonnet, `48h-gov-20260905`) has delivered
and its session has exited. You work the plan's card **4.4**
(`docs/prompts/2026-09-09-48h-completion-plan.md` §4.4), bound by that file's **§7**, `CLAUDE.md`
and `AGENTS.md`, with the CP6 resolver's rebase procedure
(`docs/prompts/2026-09-10-48h-CP6-resolve-and-finish.md` §3.a–3.c) for the one class of conflict
this queue expects. Card 4.4 is the authority; this file adds facts read from the refs on
2026-09-10 (after #97 was opened) and the operator's rulings for a **docs-only** merge.

**Authority you have, and its limit.** You may rebase step 25's head branch in
`/Users/go/code/bnow-net-worktrees/48h-gov-20260905` and force-push with lease, **once**, and only
if GitHub reports #97 `CONFLICTING`. The union driver settles `docs/PROGRESS.md` /
`docs/BLOCKERS.md` during the rebase. **Any other conflict is a STOP** — including `AGENTS.md`,
`docs/DECISIONS.md`, `docs/OPEN-TASKS.md`, `docs/CURRENT-STATE.md`: step 25 is the only writer of
those this round, so a conflict there means something landed that this queue did not expect.

**Model / effort / mode.** Opus / high, attended, a FRESH session in `/Users/go/code/bnow-net`
on branch `main`: `cd /Users/go/code/bnow-net && caffeinate -ims claude --model opus`, then
paste this file. Default permission prompts. Not xhigh — the discipline is in the STOP rules.

**Where you run.** Native macOS terminal only; cwd is the main checkout; worktree operations are
`git -C <wt> …`; the worktree gate needs `npm` inside it. Never create a worktree.

**Operator, before this runs: push `main`, then leave it alone.** One commit landed on local
`main` after #97 was opened — **`0060385`**, this prompt's own refresh. It is harmless to the merge
(it touches only `docs/prompts/2026-09-10-48h-CP7-step25-merge.md`, which #97 does not touch), but
**it must be on `origin` before you merge**: local `main` ahead of `origin/main` is what broke the
`git pull --ff-only` twice in CP6, and §2.c pulls. After the push, no further commits on `main`
until the agent reports — one that touches `docs/PROGRESS.md` would turn a clean merge into a
conflict and cost a rebase. The INDEX §10 CP7 line and the tracker's "Stage 3 DONE" are written
after the agent reports.

---

## 0. State you inherit (read from the refs 2026-09-10 after #97 opened; re-verify in §1)

- `main` = `origin/main` = **`0060385`** ("docs: refresh CP7 step 25 merge prompt for opened PR
  #97" — a `docs/prompts/`-only commit on top of `619986c`, the CP6b program-log commit; push it
  first, see above). Gate at CP6b close: typecheck clean · lint 0 errors ·
  unit **4,599 / 293** · build PASS · **32** `.sql` under `drizzle/`. On `main` today:
  `wc -c AGENTS.md` = **132,710**; `grep -c UNSIGNED AGENTS.md` = **0**.
- **One PR, #97** — `docs: 48-hour program Stage 3 docs sync (step 25)`, head
  **`48h/gov-20260905-docs-sync`**, base `main`, **6 commits**, cut from `619986c`. `main` is
  **1 commit ahead** of that merge-base (`0060385`), and that commit shares **no file** with #97 —
  verified by `comm` over the two name-only diffs — so #97 still reads `MERGEABLE` and still needs
  **no rebase**; §2.b should stay unused. Report on the branch:
  `docs/reviews/PROGRAM-48H-DOCS-SYNC-2026-09-07.md`.
- Its diff is **seven docs files, nothing else** — verified byte-level: nothing under `src/`,
  `scripts/`, `drizzle/`, no lockfile, no `vercel.json`:

| file | +/− | what |
|---|---|---|
| `AGENTS.md` | +306 / −125 | standing-text corrections, eleven new decision-log entries, **and the eleven 2026-09-05 entries removed by the archive pass** |
| `docs/DECISIONS.md` | +84 / −3 | the eleven archived entries, moved **byte-identical**, plus the split-point header |
| `docs/CURRENT-STATE.md` | +144 / −42 | snapshot resync |
| `docs/OPEN-TASKS.md` | +13 / −8 | #117 sub-issue closed; #110/#112 gained merge-SHA citations |
| `docs/PROGRESS.md` | +68 / −0 | this step's blocks (the `merge=union` file) |
| `docs/reviews/EVAL-SUCCESSOR-PLAN-2026-09-04.md` | +43 / −0 | dated addendum |
| `docs/reviews/PROGRAM-48H-DOCS-SYNC-2026-09-07.md` | +205 / −0 | new — the closing report |

  **`AGENTS.md`'s 125 deleted lines are expected**, not a red flag: they are the archive move to
  `docs/DECISIONS.md`. `scripts/check-decision-log-move.sh` in §2.c is what proves the move was
  lossless.
- **What the merged tree must read:** `wc -c AGENTS.md` = **148,686** (< 150,000 — only 1,314
  bytes of headroom, so record the number, not just the verdict); `grep -c UNSIGNED AGENTS.md` =
  **1** (the one closing entry step 27 signs — 0 means step 25 forgot, 2+ means it signed
  something it should not have); unit **unchanged at 4,599 / 293** (a docs diff cannot move a test
  count; a change is a STOP).
- **`scripts/check-decision-log-move.sh` prints `FAILED` on this tree, and that is expected.**
  The script is built for a pure archive-only pass; step 25 added eleven brand-new decision-log
  entries in the same tree as the move, and the script counts those as "new or edited", which
  flips its summary word. **Read the lines, not the verdict.** The safety property is
  **"0 lost or edited"** plus no duplicates, ascending date order in both files, and the ceiling.
  A non-zero **lost-or-edited** count, a duplicate, or an out-of-order date IS a STOP. Step 25's
  report records the same reading under `## Tests` and `## Debt and risks`.
- Worktree `/Users/go/code/bnow-net-worktrees/48h-gov-20260905`, lane branch `48h/gov-20260905`
  (never check it out, rebase it or push it). No itest here: this step touches no schema, no
  route, no script.

---

## 1. Preflight — once

```
cd /Users/go/code/bnow-net
git status --porcelain
git fetch --prune origin
git log --oneline -1 main
git log --oneline -1 origin/main
git log --oneline origin/main..main
lsof -a -d cwd -c claude | grep bnow-net-worktrees
scripts/launch/status.sh
gh pr list --state open --json number,headRefName,baseRefName,mergeable
git -C /Users/go/code/bnow-net-worktrees/48h-gov-20260905 status --short
git -C /Users/go/code/bnow-net-worktrees/48h-gov-20260905 branch --show-current
git rev-list --count origin/main..origin/48h/gov-20260905-docs-sync
wc -c AGENTS.md
grep -c UNSIGNED AGENTS.md
```

**Expect:** clean tree; both `git log -1` lines **`0060385`** and **`origin/main..main` empty** —
if that range prints anything, local `main` is unpushed: `git push origin main` before you go on, or
§2.c's `--ff-only` pull fails (the CP6 incident). A commit later than `0060385` is acceptable only
if its `--stat` is docs-only — anything under `src/`, `scripts/`, `drizzle/` is a STOP, and one that
touches `docs/PROGRESS.md` means #97 now conflicts there: that is §2.b, not a surprise. `lsof`
silent (the grep is on `bnow-net-worktrees` — you are yourself a `claude` process in
`/Users/go/code/bnow-net`); `status.sh` row 25 `DEAD` and no `run` row; **exactly one open PR,
#97**, base `main` (a second open PR is a STOP — report its number and branch); the gov worktree
clean; count **6**; `132710`; `0`.

Then the docs-only check, before anything moves:

```
git diff --stat origin/main...origin/48h/gov-20260905-docs-sync -- src scripts drizzle package.json package-lock.json vercel.json .githooks
git diff --numstat origin/main...origin/48h/gov-20260905-docs-sync
```

**Expect:** the first command prints **nothing**; the second prints §0's seven-row table, with the
same numbers. Any path under `src/`, `scripts/`, `drizzle/`, any lockfile, or an eighth file:
**STOP** — step 25 is docs-only by its own prompt (item 15).

---

## 2. The merge — cards a → c

### 2.a Mergeability

```
cd /Users/go/code/bnow-net
lsof -a -d cwd -c claude | grep bnow-net-worktrees
gh pr view 97 --json baseRefName,headRefName,mergeable,mergeStateStatus -q '[.baseRefName,.headRefName,.mergeable,.mergeStateStatus]'
```

**Expect:** base `main`; **`MERGEABLE` / `CLEAN`** → go straight to **2.c**. `UNKNOWN` → wait 10 s
and re-view, up to **6** polls; still `UNKNOWN` → STOP. `CONFLICTING` → **2.b** (only reachable if
something landed on `main` after `619986c`). Base not `main` → `gh pr edit 97 --base main` once,
wait ~30 s, re-view, record it as an incident; a second retarget would be needed → STOP.

### 2.b Rebase in the gov worktree — only on `CONFLICTING`

```
cd /Users/go/code/bnow-net
wt=/Users/go/code/bnow-net-worktrees/48h-gov-20260905
lsof -a -d cwd -c claude | grep bnow-net-worktrees
git -C $wt fetch --prune origin
git -C $wt status --short
git -C $wt checkout 48h/gov-20260905-docs-sync
git -C $wt log --oneline origin/main..HEAD | wc -l
git -C $wt rebase origin/main
git -C $wt status --short
git -C $wt log --oneline origin/main..HEAD
git -C $wt grep -n '^<<<<<<<\|^>>>>>>>\|^=======$' -- docs AGENTS.md
git -C $wt diff origin/main --numstat
```

**Expect:** `lsof` silent; empty `status --short` before the checkout; pre-rebase count **6**; the
rebase completes with **no stop** (the union driver settles `docs/PROGRESS.md`, the only file
`main` and this branch can both be writing); empty status after; no markers; the `--numstat`
still §0's seven rows.

**If the rebase stops in any file** — `AGENTS.md`, `DECISIONS.md`, `OPEN-TASKS.md`,
`CURRENT-STATE.md`, a review — `git -C $wt rebase --abort` and **STOP** with
`git -C $wt diff --name-only --diff-filter=U` and the full unedited conflict text. **No
hand-resolution is authorized in this queue.** Never `--skip`, never retry a different way.

Then gate in the worktree and push:

```
cd /Users/go/code/bnow-net-worktrees/48h-gov-20260905
npm run typecheck && npm run lint && npm test
git push --force-with-lease origin 48h/gov-20260905-docs-sync
cd /Users/go/code/bnow-net
```

**Expect:** typecheck clean; lint 0 errors (3 pre-existing unused-var warnings are expected and
are not errors); unit **4,599 / 293**, unchanged — docs-only, but the gate is the gate. Push
accepted; a refused lease means the branch moved under you: **STOP**, do not force. Then 2.a again
until `MERGEABLE` / `CLEAN`; still `CONFLICTING` after one rebase → STOP, do not rebase again.

### 2.c Merge and gate `main`

```
cd /Users/go/code/bnow-net
gh pr merge 97 --merge
git pull --ff-only
git log --oneline -1
npm run typecheck && npm run lint && npm test
LLM_DISABLE=1 OPENAI_API_KEY= ANTHROPIC_API_KEY= DATABASE_URL=postgres://x:y@localhost/z npm run build
ls drizzle | grep -c '\.sql$'
git diff --stat HEAD~1 HEAD -- src scripts drizzle src/db/schema.ts package-lock.json
wc -c AGENTS.md
grep -c UNSIGNED AGENTS.md
bash scripts/check-decision-log-move.sh HEAD~1
```

`--merge` only — never `--squash`, never `--rebase`, never the web UI. Record the merge SHA.

**Expect:** a merge commit; typecheck clean; lint 0 errors; unit **unchanged at 4,599 / 293** (any
movement is a STOP); build **PASS**; `32`; an empty diff; `AGENTS.md` **148,686** (record the
number; ≥ 150,000 is a STOP); `UNSIGNED` **1**.

**On the archive check:** `HEAD~1` is deliberate — the script compares BASE_REF against the
**working tree**, so after the merge the pre-merge `main` is the only base that makes it mean
anything (`HEAD` would compare the merged tree to itself and pass vacuously). Its summary word
will be **`FAILED`**, expected per §0. What you read and paste is the body: **lost or edited = 0**,
no duplicates, ascending date order in both files, ceiling ok. Non-zero lost-or-edited, a
duplicate, or an out-of-order date → **STOP** with the full output. A red gate on `main` is a STOP:
no revert, no fix.

---

## 3. After #97 — Stage 3 close-out, launch nothing

```
cd /Users/go/code/bnow-net
gh pr list --state open
git status --porcelain
git log --oneline --first-parent 883e5e3..main | wc -l
git log --oneline --first-parent 883e5e3..main | grep -c 'Merge pull request'
scripts/launch/status.sh
git worktree list
grep -n 'UNSIGNED' AGENTS.md
```

**Expect:** no open PR; clean tree; the two counts recorded (step 26's list); no `run` session; no
`/sessions/…` path in `git worktree list` — a `prunable` entry named `branding-strategy-20260909`
is **not** a program worktree (its branch `docs/branding-strategy-20260909` has never been merged
and has no PR): note it, do not touch it, do not prune it. One `UNSIGNED` line, the closing entry
dated 2026-09-10.

**Stage 3 is closed** when 18, 19, 20, 21, 23r, 23c, 24, 25, 32, 33, 34 are all merged — they are,
once #97 lands. Say so in the report. **Launch nothing:** step 26 is attended and the freeze card
(plan §6.1) comes first. Do not write INDEX §10, the tracker, or any review file; do not commit
anything. Step 25's report already carries a **draft INDEX §10 line** and a **draft tracker line**
under `## Handoff` — point the operator at them, do not apply them.

---

## 4. Hard rules

- Rebase and force-push only `48h/gov-20260905-docs-sync`, only on a GitHub `CONFLICTING`, only in
  `48h-gov-20260905`, **once**. Never `main`, never the lane branch `48h/gov-20260905`, never a
  merged PR's branch.
- **No hand-resolution of any file. No commits of your own.** If one somehow becomes unavoidable:
  `area: imperative summary`, and no vendor branding — no `Co-Authored-By`, no "Generated with",
  no model or vendor names, in the message or the file contents.
- `lsof -a -d cwd -c claude | grep bnow-net-worktrees` before every rebase and every merge.
- Never touch any `.env.local`, never launch a session, never consume a claim, no provider call,
  no deploy, no Vercel read or write, no migration applied anywhere. **$0** — nothing in this
  queue touches Neon or a provider.
- Do not commit on local `main` while you work there (CP6's `--ff-only` incident).
- A reading that disagrees with §0 — a code path in the diff, an eighth file, a second open PR, a
  unit count that moved, `AGENTS.md` ≥ 150,000, `UNSIGNED` ≠ 1 — **wins: STOP and report it.**

---

## 5. Report shape (paste this, nothing else)

```
CP7 — step 25 merge — 2026-09-10
Start:  main = <sha> (= origin/main: <yes/no>)   unit = 4599 / 293   AGENTS.md = 132710 b, UNSIGNED 0   open PRs = <list>
  #97  docs-only check: <clean | paths>   files 7/7 as expected: <yes/no>
       mergeable=<v> after <p> polls   rebase: <none | 6→<k> commits, PROGRESS via union>
       worktree gate: <n/a | typecheck ok, lint 0, unit 4599/293>   push: <n/a | ok>
       merge=<sha>
       main gate: typecheck ok, lint 0, unit <before>→<after>, build PASS, drizzle 32, diff empty
       AGENTS.md <bytes> (< 150000), UNSIGNED <n>
       archive check (HEAD~1): verdict <word>, lost-or-edited <n>, duplicates <n>, order <ok/not>
End:    main = <sha>, open PRs none, first-parent merges since 883e5e3 = <count>
Stage 3: CLOSED (18 19 20 21 23r 23c 24 25 32 33 34 merged) | NOT CLOSED because <reason>
Waiting on operator: INDEX §10 CP7 line + tracker 5.3 → DONE (drafts are in the step 25 report's Handoff)
Stopped at (if any): <PR, card, exact output, full conflict text, state left>
Incidents: <every retarget, every extra poll, or "none">
```
