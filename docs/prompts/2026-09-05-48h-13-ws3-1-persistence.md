# Step 13 — WS-3.1 persistence: migrations 0028 (benchmark editions) and 0029 (conflict observations) (Wave 2, lane C)

| | |
|---|---|
| Model / effort / mode | Opus / high / plain session with a 15-minute plan-mode preamble |
| Lane / worktree | C — `/Users/go/code/bnow-net-worktrees/48h-conflict-20260905`, step branches `…/mig-0028-benchmark-editions`, `…/mig-0029-conflict-observations` |
| Window | H8 → H16 |
| Depends on | 05 (memo + PLAN-WS-3); decisions C1 (must be Option 3 or this prompt is rewritten), C4, C6 |
| Decisions | C1, C4, C6 — if C6 is unanswered, build 0028 only and hold 0029 |
| Spend | $0. Disposable Neon fork for itests (`NEON_API_KEY` works). |
| Closing report | `docs/reviews/WS-3-1-PERSISTENCE-2026-09-06.md` |

Read `docs/prompts/2026-09-05-48h-COMMON.md` first, then
`docs/reviews/CONFLICT-VALIDATION-DECISION-MEMO-2026-09-05.md` and
`docs/reviews/PLAN-WS-3-validation-by-conflict-2026-09-05.md` §3.1a/3.1b (the specification).
**Rewrite note:** at Checkpoint 1 the operator pastes the answered C-decisions here.

**Migration mechanics (every migration PR):** claim the next free number at rebase time
(INDEX §4 gives the intended order); never renumber a merged migration; `9999` stays last;
`npm run db:generate` must reproduce your SQL from `schema.ts` (hand-authored statements are
listed in the PR body); the fork itest applies migrations itself via
`scripts/migrations-lib.ts` `runMigrations(URL)` (the harness `scripts/test-integration.sh`
does NOT apply them — `ask-billing.itest.ts:55` is the pattern).

## Invariants (ruling 5 and 1)

- Forward-only. New files `drizzle/0028_<name>.sql` and `drizzle/0029_<name>.sql` (INDEX §4
  numbers; rebase and regenerate before merge if another migration landed first);
  `drizzle/meta/_journal.json` + snapshots updated by `npm run db:generate`; hand-author only
  what drizzle-kit cannot emit (CHECK constraints, partial unique indexes) and say so;
  `9999_claim_source_trigger.sql` stays last and unedited; `migrations.test.ts` gains an
  additive-shape pin per file copying the 0027 pattern (:66-80) and keeps the "9999 last" pin.
- `isw_reports`, `source_citations`, `sources`, `source_theater_stats`, `validation_runs`: **no
  change of any kind** (design §4).
- Ruling 1: no ISW prose column anywhere. `benchmark_report_editions.derived` and the
  observation table hold signatures/hashes/ids/counts/verdicts only; add a schema comment and a
  test that the persisted scorer shape (`assertPersistableConflictResultV1`,
  `src/lib/conflicts/eval-profile.ts:401`; the ruling-1 contract comment is `scorer.ts:34-39`)
  is the only thing written.

## PR 1 — `db: migration 0028 — benchmark_report_editions + benchmark_series_days (promote disposable DDL)`

Promote `src/integration/sql/conflict-benchmark-reports.sql` into `src/db/schema.ts` +
migration 0028, carrying the design §5 hardening the disposable backend deferred: edition
insert + day-row clear in ONE transaction; the CASE-guarded `DAY_STATUS_UPSERT_SQL` monotone
rule stays at SQL level (`reference-repo-sql.ts:131-136`); partial-unique `canonical_url`
violations map to typed errors; anchor-change journal; URLs canonicalized before storage.
`SqlReferenceReportRepository` (`reference-repo-sql.ts`) targets the durable tables; the
disposable DDL file is deleted or reduced to a pointer (CLAUDE.md scoped exception — say
which). The existing conflict-reference-repo itest runs against the migrated fork, applying
the migrations itself with `runMigrations(URL)` (mechanics above).

## PR 2 — `db: migration 0029 — conflict_validation_observations`

Columns per the plan: conflict id (registry key), reference edition id (FK to 0028), report
date, series, contributing digest ids (array or join table — per plan), scorer output
(persistable shape only), matcher rung and vote audit ids, version identities (contract,
evidence policy, lanes, gazetteer, registry, extractor), `created_at`; unique key per C6
(overwrite-on-revalidate → `(conflict_id, reference_edition_id)`; append-only → include a run
id and no overwrite path). Coexists with `validation_runs`; no FK into it. Schema test that
every column is id/number/enum/jsonb-of-ids.

## Acceptance

`npm run db:generate` produces exactly the two files (plus meta); `npm test` green (counts);
`npm run test:integration -- src/integration/migrations.itest.ts` and the conflict reference
repo itest green on a named fork; `git diff src/db/schema.ts` touches only additions;
`git diff --stat drizzle/` shows 0028/0029 + meta only; 9999 byte-identical.

## Report

Per COMMON §5. In **Handoff**: the final table/column names (steps 14, 19, 24 import them),
which C-decisions the unique key encodes, and the fork name/run output.
