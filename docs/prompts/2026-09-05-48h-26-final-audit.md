# Step 26 — Final adversarial audit of the window + per-PR deploy go/no-go + next-48h handoff — SKETCH (Wave 5)

| | |
|---|---|
| Model / effort / mode | **Fable / max / ultracode** — read-only; parallel finders over every merged PR, refuters per finding, one synthesis |
| Worktree | `48h-audit-ws2-20260905` reset to frozen `main` (record the SHA) |
| Window | H44 → H48 |
| Depends on | CP4 freeze (steps 23–25 merged) |
| Rewrite from | the INDEX §10 program log and every report; paste the merged PR list with SHAs |
| Spend | $0 |
| Closing report | `docs/reviews/PROGRAM-48H-FINAL-AUDIT-2026-09-07.md` + `docs/prompts/2026-09-07-next-48h-handoff.md` |

**DECISIONS BINDING — added 2026-09-09 (read first).** (1) **Model: Opus / xhigh, attended,
NOT Fable.** Step 18's register was authored on Fable 5.1 (its disclosure 1) and step 17's
finding register was a Fable orchestration; the tracker's rule from CP5 is that the final audit
must not share a model with the registers it re-checks. `scripts/launch/steps.tsv` row 26 says
`opus`; the operator launches it with `launch.sh 26 --go` (attended: it prints the command).
Bounded fan-out from the first line: three refuters per major, one batched verification per PR
for minors and notes, one completeness critic (plan §5.4) — say so in the register. (2) **Freeze
SHA:** `FROZEN-SHA-TBD` (the operator replaces this token at the freeze step of the 2026-09-09 plan §6.1);
base `883e5e3`. Merged PRs on `main` at `f55534e`, first-parent order newest first (extend with
`git log --first-parent --merges 883e5e3..<frozen> --format='%h %s'` for 19, 23r, 23c, 24, 25):
#85 `4eccb06` · #81 `be591a1` · #75 `9f11619` · #76 `c6f0a6c` · #84 `f8c9a96` · #77 `c2e384d` ·
#80 `48a9d4e` · #79 `95d6aa7` · #78 `c32213a` · #47 `81acadd` · #48 `d0c981e` · #71 `5f30cd3` ·
#70 `72da938` · #74 `0924ed9` · #72 `c101737` · #69 `a485c80` · #68 `9c32fb7` · #67 `6b52c2c` ·
#73 `3e7cfdd` · #64 `1e06112` · #61 `ef0bba8` · #60 `fed1d03` · #66 `2a51f91` · #65 `7f267bd` ·
#62 `c75bd99` · #63 `c728b2b` · #59 `8a00ea2` · #57 `8ac41d2` · #58 `8b6d9d7` · #56 `2203150` ·
#55 `b5ca557` · #54 `ba968ce` · #53 `544b475` · #52 `d74d588` · #51 `6eaef5d` · #50 `b62538d` ·
#49 `8cb0e9e`.
(3) **First task, before any finder runs — merge fidelity (register gap G12):** for every merged
PR, `git range-diff <merge-base>...<reviewed head> <merge-base>...<merge SHA>` where the reviewed
head is the tip each closing report names; any non-empty range-diff is a finding. (4) **Gaps
handed to you as prompt text, not hope** (plan §5.4): G7 (`SpendGuard.cfg.provider` bare string
sharing row-key names with `<W>_PROVIDER`), G10 (#67's `unscorecarded` answer renders with no
degraded callout), G11 (#65's rehearsal spend claims taken on trust), G13 (silent doc reverts —
sweep every docs commit in the window with `git log -p -- docs | grep '^-'` for removed
operator paths and BSD/GNU lines; WS2-F09 was found by accident), G14 (run
`scripts/model-routing-inspect.ts` and paste its output — it was rewritten and never run), G16
(re-measure per-PR test counts). (5) **Carry from CP5:** the gate's verdict on subprocess tests
depends on the runner's `.env.local` — `hardening-cli.test.ts` was green in every worktree only
because none held `ANTHROPIC_API_KEY` (`25bdd27` blanked it); run `npm test` once on the
full-secret main checkout and record it. (6) **Eval-plane isolation now has TWO authorized
writers:** step 07's two files and step 19's E5 refresh
(`docs/evals/analysis/results/conflict-{roca,iran}-v1-offline-fixtures.json` + the conflict
scorecard, deterministic, $0) — anything else under `docs/evals/analysis/` in the window's diff
is a finding. (7) **The Anthropic digest path is dormant, not enabled:** verify on the frozen
tree that no Vercel-facing default selects it (allowlist widened for `digest` only; no
`DIGEST_PROVIDER` documented as set); `MAP_PROVIDER=anthropic` still refuses before any
reservation; `--execute-live --provider anthropic` with `ANTHROPIC_API_KEY` blank exits at
preflight with the key-not-set refusal. (8) **Step 18's register is single-reader:** treat
WS3-F01/F02 as confirmed (reproduced), the minors as verified-by-one, and re-check mutants
M9/M10 after step 23 lane C. Step 17's minors/notes carry one verification each (A3). (9) The
`git worktree list` check for `/sessions/` gitdir paths is run from a native Mac terminal
(a remote-mount session shows every worktree as `prunable` — that is the mount, not the
estate). (10) Outputs keep their names: `docs/reviews/PROGRAM-48H-FINAL-AUDIT-2026-09-07.md`
and `docs/prompts/2026-09-07-next-48h-handoff.md`. The per-PR deploy verdict table must carry,
for every `go`, the migration it needs applied first (0028/0029/0030 are unapplied — #111) and
the env names it reads (values are the operator's, step 27). No code changes; $0.

Read `docs/prompts/2026-09-05-48h-COMMON.md` first. Precedent:
`docs/prompts/2026-08-18-quality-foundation-fable-final-audit.md` and the three
`CONFLICT-EVALUATION-FABLE-AUDIT-*-REVIEW-2026-08-18.md` documents.

## Prompt shape (fill in at CP4)

Audit frozen `main` `<SHA>` against base `883e5e3`: every PR merged in the window
(`<list>`). Finders (parallel, one per lens, each reading the full diff and the files
touched): rulings 1–5 as invariants; 8, 9, 13, 14, 19, 21 as operational; eval-plane
isolation (`docs/evals/analysis/` diff limited to step 07's two files; offline results resume;
`bash scripts/evals/corpus-v2/check-regen.sh`, no arguments); migrations (0028–0030 additive, journal consistent, 9999 last,
NOT applied to production — a deploy applies nothing by itself; `db:migrate` is a separate
operator step, say where the checklist puts it); "tests as evidence" (mutate-and-run on ten
randomly chosen pins across PRs); docs truth (AGENTS.md snapshot, OPEN-TASKS, CURRENT-STATE
vs code); secret/PII leakage across the whole diff (`git log -p` grep for keys, emails, phone
numbers — the #48 roster decision included); commit hygiene (no vendor branding). Three
refuters per finding. Outputs: (1) the finding register (format of step 17); (2) a **per-PR
deploy verdict table**: go / go-after-fix / no-go, with, for every "go", the cap/secret envs
it reads and whether they are set (names only — the operator checks values), the migration
it needs applied first, the rollback target, and the observation window; (3)
`docs/prompts/2026-09-07-next-48h-handoff.md` — the same shape as the 2026-09-05 CTO handoff:
verified state after this window, what is code-ahead of production, the deploy sequence
step 27 should follow from `docs/RELEASE-CHECKLIST.md`, the open decisions with the evidence
now available, the label-gated steps' readiness (WS-1.3 inputs: ledger, memo, cases), the
WS-3.6 soak enablement state, the WS-1.5 sizing note (≈570 requests; `EVAL_DAILY_REQUEST_CAP`
300/day AND `EVAL_RUN_REQUEST_CAP` 200/process — runs must be chunked or the per-process cap
raised), and a first draft of the next program's Wave 1. Include a **"what the operator can
see working"** list with the exact commands: `scripts/model-routing-inspect.ts` prints a
provider column; `MAP_PROVIDER=anthropic` refuses before any reservation; `--execute-live
--provider anthropic` exits at preflight; a fresh fork applies 0000–0030 idempotently; the
gazetteer lane fixtures score; `git worktree list` shows no `/sessions/` paths. No code
changes.
