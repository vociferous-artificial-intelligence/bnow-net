# Step 11 — WS-2.1 Ask metering granularity + gate parity (Wave 2, lane R)

| | |
|---|---|
| Model / effort / mode | Opus / high / plain session with a 15-minute plan-mode preamble that maps PLAN-WS-2's WS-2.1 PRs to branches |
| Lane / worktree | R — `/Users/go/code/bnow-net-worktrees/48h-routing-20260905`, one step branch per PR |
| Window | H8 → H14 |
| Depends on | 04 (PLAN-WS-2 merged or at least reviewed by the operator); 09 merged |
| Decisions | R1 (attribution shape), R3 (Auto gate) — if unanswered, ship the read-only report and the embed pricing PR, hold the rest |
| Spend | $0. Read-only production SELECTs only if the plan says so and the operator agreed. |
| Closing report | `docs/reviews/WS-2-1-ASK-PARITY-2026-09-06.md` |

Read `docs/prompts/2026-09-05-48h-COMMON.md` first, then
`docs/reviews/PLAN-WS-2-routing-matrix-2026-09-05.md` §WS-2.1 — it is the specification; this
prompt only fixes scope boundaries and acceptance. **If PLAN-WS-2 is not on `main` in your
worktree, stop and ask the operator** (it is merged at Checkpoint 1). **Rewrite note:** at
Checkpoint 1 the operator pastes the plan's PR list for WS-2.1 here, in order, with R1/R3
answers.

## Read (in addition to the plan)

`src/lib/ask/router.ts:65-126` (scorecard gate only in the recording-only router; `ASK_ROUTER`
default off — `limits.ts:445-451`, `config.ts:101-103`); the money path `answer.ts:573`,
`rerank.ts:204`; `ask_usage` per-stage model + cost columns `schema.ts:609-663`;
`run-guards.ts:86-99` (shared `openai_ask` row) and `:48-50` (embed ceiling);
`src/lib/ask/retention.ts:13,81` (nulls only `question`; model/cost columns survive — so the
attribution window is unbounded); `src/lib/embeddings/client.ts:15-45,131-144`,
`src/lib/llm/openai.ts:180-185`, `scripts/backfill-embeddings.ts:74,83`; `src/lib/llm/pricing.ts`;
`src/lib/adapters/x-api.ts:201,223` (#44); `scripts/ask-eval-harvest.ts:190` and
`src/lib/evals/isolation.test.ts:137-141` (#82); `docs/OPEN-TASKS.md` #44, #82, #84, #100.

## Scope (from the plan; expected shape)

- PR 1 — `ask: per-model attribution report over ask_usage (read-only)`: a script
  (`scripts/ask-model-attribution.ts`) aggregating `ask_usage.rerank_model/answer_model` and
  cost columns per UTC day; pure aggregation unit-tested over fixture rows; SELECT-only pin
  (source scan for INSERT/UPDATE/DELETE). `retention.ts:81` nulls only `ask_usage.question`
  (`:13` keeps every cost/token/timing column), so the window is unbounded — state it.
- PR 2 — `embeddings: model-aware pricing; unpriced embed model refused before reservation`:
  `pricing.ts` gains an embeddings table; `embeddings/client.ts`, `run-guards.ts`,
  `scripts/backfill-embeddings.ts` read it; 3-small cost byte-parity test with the old
  constant; `ASK_EMBED_MODEL=<unpriced>` refused before any `guard.tryReserve` and before SDK
  construction (spy pins); ceiling pin (unknown-embed fallback ≥ every embed price).
- PR 3 (R3) — `ask: scorecard gate on the Auto money path`: `answer.ts:573` / `rerank.ts:204`
  consult `hasScorecard()` like the fast/deep router paths; a pin that with no env override
  (today's production) behaviour is byte-identical; with `ASK_ANSWER_MODEL=gpt-5-nano` (no
  scorecard) → deterministic cited-claims path, zero reservations (ruling 9: /ask degrades,
  never throws).
- PR 4 (R1, only if the column shape is chosen) — migration 0031 `provider_usage.model`
  (nullable) + model-tagged `record()`; spend-guard store contract tests; `migrations.test.ts`
  additive pin; `ask-runs.itest.ts` assertions extended; `9999` still last; **cap semantics
  unchanged** (the daily cap still aggregates the row). If R1 = per-model rows instead: STOP —
  that splits `ASK_USD_CAP_DAILY` and needs cap envs in all Vercel envs first (ruling 4);
  list it, do not build it.
- Hygiene, one commit each: #44 — align the `X_DAILY_USD_CAP` default/comment at
  `src/lib/adapters/x-api.ts:201,223` with the $4 production cap (comment-only unless the
  plan says raise the default; a raised default changes behaviour for any env with the var
  unset — say so); #84 — nothing to code (step 03 made it a checklist line; the record happens
  at the next deploy); #82 — route `scripts/ask-eval-harvest.ts:190` through
  `analysisOpenAiClient()` and drop the `isolation.test.ts:141` exemption, or record it as
  operator-only tooling if the plan says so.

## Rulings

4 (no new cap env unless PR 4's column keeps the row semantics; if any new row/cap appears,
the PR body lists the env and the ruling-4 ordering), 8 (metering stays inside `analyze()` /
the embed client), 9 (/ask degrades, never throws).

## Acceptance

Each PR: typecheck/lint/unit green with counts; PR 4 additionally reports the fork itest run
(`npm run test:integration -- src/integration/ask-runs.itest.ts` and `migrations.itest.ts`).
No file under `docs/evals/analysis/` changes. `git grep -n EMBED_USD_PER_TOKEN` returns only
the compatibility export if the plan keeps one.

## Report

Per COMMON §5, one section per PR. In **Handoff**: which PR numbers merged, which are held
on R1/R3, and what step 17 (audit) should look at hardest (the refusal-before-reserve pins).
