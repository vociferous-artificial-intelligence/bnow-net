# Checkpoint 4 — merge queue (2026-09-07, Stage 1 of the completion plan)

| | |
|---|---|
| Model / effort / mode | Opus / high / plain session — **attended, normal permissions** (no `--dangerously-skip-permissions`, no `-p`) |
| Where | the main checkout `/Users/go/code/bnow-net`, on `main`. Must be the Mac — `node_modules` is macOS-only, so the gate cannot run from a remote session. |
| Spend | $0. No DB. No deploy. No `.env.local` copied anywhere. |
| Output | `main` pushed with #74, #70 and #71 merged; the WS2-F06 OPEN-TASKS entry filed; one CP4 line in INDEX §10; a short terminal report. No closing-report file. |

Read first, in this order:

1. `docs/prompts/2026-09-05-48h-COMMON.md` §3, §4.10, §4.11
2. `docs/prompts/2026-09-06-48h-CP2-merge.md` — its **Fence**, **Preflight**, per-PR merge
   procedure and **If a PR reports conflicts** sections apply here unchanged
3. `docs/prompts/2026-09-07-48h-CP4-and-completion-plan.md` **§5.1** — this prompt is that
   stage, expanded; §0, §2 and §5.4 are the background it assumes

You merge. You launch nothing, you delete no branch, you create no worktree, you start no
step. When the queue is merged and the gate is green: report, stop.

---

## Fence

Same as CP2/CP3. This session does not start, launch, background, `nohup`, `claude -p` or
otherwise run any other session or step; does not create worktrees (the one rebase happens in
the worktree the PR was built in); does not delete branches; does not deploy; does not touch a
database. It edits exactly four things:

- one bullet on the **#74 branch** (queue item 1, authorized by (f4));
- whatever a rebase conflict resolution requires on the **#70/#71 branches**, within the
  shapes named below;
- `docs/OPEN-TASKS.md` — the new **WS2-F06** entry (authorized by (f2); A1's first half);
- one appended CP4 line in `docs/prompts/2026-09-05-48h-00-INDEX.md` §10.

Anything else that looks like it needs fixing gets **reported**, not fixed.

---

## Preflight

```
cd /Users/go/code/bnow-net && git rev-parse --show-toplevel && git status -sb
git fetch --prune origin && git pull --ff-only
git log --oneline -3
gh pr list --state open --json number,title,headRefName,baseRefName,mergeStateStatus
git worktree list
lsof -a -d cwd -c claude | grep bnow
```

Expected, and each is a stop condition if it does not hold:

1. **Tree clean** apart from the untracked `Claude outputs/`.
2. **`main` == `origin/main` == `a512fc8`** ("docs: CP4 plan rev 2 …"). The plan §5.0 gate
   names `f6eca71`; rev 2 of the plan itself is the commit on top, and Stage 0's push appears
   to have happened. If `main` is *ahead* of `origin/main`, push before merging anything — the
   rebases target `origin/main`. If it is behind or diverged, stop and report.
3. **Open PRs are exactly #74, #71, #70, #48, #47.** #47/#48 are Stage 2 operator work and are
   **not** in this queue. If **#51** is still open, it is Stage 0 residue — do not merge it,
   it landed at CP1 as `6eaef5d`; close it with
   `gh pr close 51 --comment "landed at CP1 as 6eaef5d; branch tip is an ancestor of main"`
   and say so in the report. Any *other* unexpected open PR: stop and report.
4. **`lsof` prints nothing** for `bnow` (COMMON §4.11 — the two detached sessions were
   confirmed gone 2026-09-07; if one is back, stop, do not touch a worktree).
5. **`/Users/go/code/bnow-net-worktrees/48h-ws3-gazetteer-20260905` exists and its
   `git status --short` is empty.** The stack is rebased there. The fence forbids creating a
   worktree, so if it is missing or dirty: stop and report — do not improvise a scratch clone.

Then, **before any merge**, the baseline build (this isolates blame for the gate below —
register gap G4 means `npm run build` has never run in this window, so a failure discovered
only after three merges would be unattributable):

```
OPENAI_API_KEY= ANTHROPIC_API_KEY= POSTMARK_SERVER_TOKEN= LLM_DISABLE=1 \
  DATABASE_URL='postgres://u:p@localhost:5432/dummy' npm run build
```

If this fails on `a512fc8`, **stop before merging anything**, paste the error, and report: the
break is pre-existing and belongs to step 23, not to this queue. Record the result either way —
pass or fail, this is the first tree-wide build of the window.

---

## Queue — this order, one PR at a time, `git pull --ff-only` after each

For each: confirm the head branch is the one named, confirm `baseRefName` is `main`, confirm
CI is green and `mergeStateStatus` is `CLEAN`/`MERGEABLE`, then
`gh pr merge N --merge --subject "Merge PR #N: <short title>"` and `git pull --ff-only`.

A GitHub **DIRTY** state on any of these three is the `.gitattributes merge=union` artifact
again (GitHub does not honour `merge=union`): `git merge-tree <merge-base> origin/main
origin/<branch>` emits **zero** conflict markers for all three, re-verified against `f6eca71`.
Check locally with `merge-tree` before believing GitHub.

### 1. #74 — step 17 WS-2 audit finding register — `48h/audit-ws2-20260905-finding-register` (tip `9521e44`)

Docs only: 5 files, all under `docs/`, +5,464/−0, no code. Its base is 6 docs commits behind
`main` but it is conflict-free.

**Before merging, one edit on the branch** — the register's own "For step 27 (deploy)" bullet
in `docs/reviews/WS-2-AUDIT-FINDING-REGISTER-2026-09-06.md` (around line 565) still says:

> The only PR whose verdict is not "merge-stands" is #64 …

That is overruled by the register's own per-PR verdict table (`#52 #57 #59 #60 #61 #65`
merge-stands; **`#62 #64 #67`** fix-before-deploy) and by the branch's own 2026-09-07 commit
recording #67's correction. (f4) — `docs/reviews/DECISION-ENTRIES-DRAFT-2026-09-05.md:514` —
records the overrule and says it is corrected when #74 merges.

Rewrite the bullet so it names **#62, #64 and #67** as the fix-before-deploy set, keeps the
existing #64 sentence (the hazard is the *enablement* sequence, not the code: the receiver is
inert until `LOG_DRAIN_SECRET` exists, and migration 0029 must be applied by hand before any
drain is registered), and adds one clause each for #62 and #67 taken from their rows in the
verdict table — **read the table on the branch and use what it says**; do not invent a reason.
Verify against the table before writing, then:

```
cd /Users/go/code/bnow-net-worktrees/48h-audit-ws2-20260905
git status --short          # must be empty
git fetch origin && git checkout 48h/audit-ws2-20260905-finding-register && git pull --ff-only
# edit, then:
git commit -am "docs: correct the register's step-27 bullet to the verdict table's three fix-before-deploy PRs (f4)"
git push
```

No gate re-run is needed for a docs-only edit; wait for CI, merge, `git pull --ff-only`.

### 2. #70 — step 14 PR 1, series-aware edition discovery — `48h/ws3-gazetteer-20260905-edition-discovery` (tip `515dbca`)

### 3. #71 — step 14 PR 2, conflict-validate route — `48h/ws3-gazetteer-20260905-conflict-validate-route` (tip `7566fd4`), stacked on #70

**#70 is fully contained in #71** — `515dbca` is an ancestor of `7566fd4`. One stack, one tip,
two reports. Both bases are `a821695`, 26 commits behind, so the stack is rebased once, **from
the top**, and both branches move together:

```
cd /Users/go/code/bnow-net-worktrees/48h-ws3-gazetteer-20260905
git status --short          # must be empty; lsof must have printed nothing
git fetch origin
git checkout 48h/ws3-gazetteer-20260905-conflict-validate-route
git rebase --update-refs origin/main
```

Resolution shapes, and only these:

- `docs/PROGRESS.md` — the union driver resolves it. Check the result reads in date order;
  do not hand-edit.
- `AGENTS.md` — the directory-map tail (the `src/lib/isw/` line). **Keep both**, `main`'s side
  first. This hunk survives the `3f09757` archive-pass restructure; if what you see is *not*
  the directory-map line, stop and paste it.
- `docs/OPEN-TASKS.md` / `docs/BLOCKERS.md` tails — keep both status lines.

**Do not hand-resolve `src/lib/usage/cron-run.ts`.** Step 13b added the optional `runId`
callback argument; step 14 used the single-argument form. The hunks are disjoint and must
auto-merge. **If git raises a conflict there, stop and paste the block** — that one is a code
conflict and it stops the queue. The same applies to anything in `drizzle/` or
`src/db/schema.ts` (§5.4: any conflict in code stops the queue).

After the rebase, on the rebased tip:

```
npm run typecheck && npm run lint && npm test
git push --force-with-lease   # …-conflict-validate-route
git checkout 48h/ws3-gazetteer-20260905-edition-discovery && git push --force-with-lease
```

Then, on GitHub: wait for CI, **merge #70 first**, `git pull --ff-only`, then
`gh pr edit 71 --base main`, re-check `mergeStateStatus`, merge #71, `git pull --ff-only`.

**Expect GitHub to close #70 as merged when #71 lands** — because #70 ⊆ #71. Merging #70 first
is still the right order: it keeps the two step-14 reports separable in the history. If GitHub
auto-closes #70 rather than recording a merge for it, that is expected — record it in the §10
line and move on; do not re-open or re-merge it.

---

## Gate — all of it, on `main`, on the Mac, after the last merge

```
git pull --ff-only
npm run typecheck
npm run lint
npm test
OPENAI_API_KEY= ANTHROPIC_API_KEY= POSTMARK_SERVER_TOKEN= LLM_DISABLE=1 \
  DATABASE_URL='postgres://u:p@localhost:5432/dummy' npm run build
ls drizzle | tail -4
git worktree list | grep -c mut
```

- `npm test` — expect **≥ 4,015 / 267 files**, growing by step 14's tests. Record the number
  before and after the queue.
- `npm run lint` — 3 pre-existing warnings, 0 errors is the known-good shape.
- `npm run build` — **register gap G4.** #64 added an App Router route with `runtime`,
  `dynamic` and `maxDuration` exports that the pre-push hook cannot see, and #71 adds another
  cron route. If the build fails **here** but passed in preflight, the queue introduced it:
  stop, paste the error, report, and do not push the closing commit — the fix belongs to the
  PR's author, not to this session.
- `ls drizzle | tail -4` — expect unchanged: `0029_runtime_logs.sql`,
  `0030_conflict_observations.sql`, `9999_claim_source_trigger.sql`, `meta`. Neither #70 nor
  #71 adds a migration; if a new number appears, stop and report.
- The `mut*` worktrees belong to step 17. Do not remove them; just report the count.

---

## Closing commit — two files, one commit

### 1. `docs/OPEN-TASKS.md` — file the WS2-F06 entry

(f2) at `docs/reviews/DECISION-ENTRIES-DRAFT-2026-09-05.md:488` is the authority; the finding
text is `WS2-F06` in the register that just merged (now on `main` at
`docs/reviews/WS-2-AUDIT-FINDING-REGISTER-2026-09-06.md`). Take the entry's substance from
those two sources, do not re-derive it:

- The documented Ask rollback configuration **`ASK_PIPELINE=legacy`** dispatches a paid OpenAI
  chat completion with **no SpendGuard at all** — no `askGuardFromEnv()`, `init()`,
  `tryReserve()`, `record()`, and no `provider_usage` row (`src/lib/ask/answer.ts:202-214`,
  `src/lib/llm/openai.ts:104-120`).
- It does not fail closed when `LLM_SPRINT_USD_CAP` / `ASK_USD_CAP_DAILY` are unset (ruling 4)
  and is bounded only by the fail-**open** `$10/day` `ASK_GLOBAL_DAILY_BUDGET_USD` first gate
  (`src/lib/ask/limits.ts:53-56`) plus 100 q/user/day.
- `limits.ts:34-36` and `:705-707` claim every paid stage is SpendGuard-backstopped — false
  for this path.
- **Pre-existing since `cea8cac` (2026-07-11)**, byte-identical at `98294c5` and `697aea4`; no
  PR in this window caused or widened it; production leaves `ASK_PIPELINE` unset (v2).
- **Owned by step 23**, not by #67 — scoping a pre-existing money-path hole into an unrelated
  PR's review would break that PR's envelope and hide the fix from its own review (A1/(f2)).
  Carry the register's remediation (a) and (b) forward as the two options; whichever is taken,
  #67's report's legacy bullet is amended to say the path is also unreserved and unmetered.

Number it as the next free task — **#110** (the file ends at #109; check the tail before
writing). Append it in the shape of #108/#109: a numbered entry at the end of the list with the
tier tagged inline in its title, `[Tier 1 — spend safety]` — a live money path reachable from a
documented configuration. Cite the register and (f2) in the entry so the provenance survives,
and say that step 23 owns the fix.

### 2. `docs/prompts/2026-09-05-48h-00-INDEX.md` §10 — one CP4 line

In the shape of the CP1/CP2/CP3 lines. It must carry:

- time (ET) and each PR in merge order with its **merge SHA**: #74, #70, #71 — including how
  GitHub recorded #70 if it auto-closed;
- the #74 branch edit (the step-27 bullet correction, under (f4));
- every conflict and how it was resolved — including that GitHub's DIRTY state was the
  `merge=union` artifact and that `cron-run.ts` auto-merged (or did not);
- unit count and file count **before and after** the queue;
- `ls drizzle | tail -4` output;
- **the `npm run build` result — the first tree-wide build of this window, closing register
  gap G4 at this SHA** (say which SHA), and the baseline preflight build result;
- **the four unlogged 2026-09-07 operator actions**, which §10 currently does not record at
  all: `a5caeb7` (CP1-batch decision entries signed into `AGENTS.md`'s log, 32 entries),
  `3f09757` (eighth archive pass — step 15's compaction, `AGENTS.md` 189,651 → 98,038 chars,
  29 entries moved to `docs/DECISIONS.md`, **the end-of-file append convention retired**),
  `901078a` (every open decision answered and drafted as (f1)–(f15)), and **Stage 0** (`lsof`
  clean, `git fetch --prune`, `gh pr list` read, `main` pushed, #51 closed);
- the WS2-F06 OPEN-TASKS filing, by number.

Then:

```
git add docs/OPEN-TASKS.md docs/prompts/2026-09-05-48h-00-INDEX.md
git commit -m "docs: 48h program log — CP4 merge queue landed; WS2-F06 filed"
git push
gh pr list --state open
```

and stop.

---

## Report — print, do not write a file

1. Merge order with SHAs; whether #70 was merged or auto-closed by #71.
2. Every conflict encountered and the exact resolution (or "none").
3. Gate numbers: typecheck, lint (errors/warnings), test count before → after with file count,
   **build pass/fail**, `ls drizzle | tail -4`.
4. `gh pr list --state open` — expect **#48 and #47 only** (Stage 2 operator work).
5. `git worktree list | grep -c mut`.
6. Anything you stopped on, pasted verbatim.
7. One line on what Stage 2 now needs from the operator (§5.2): sign (f1)–(f15), the #79 RU
   citation drain, the two C5-m probes, the WS-1.1 ×3 capture run, #48 squash-relaunch and #47.

Stop conditions, restated: any conflict in code, `drizzle/` or `src/db/schema.ts`; a
`cron-run.ts` conflict; a missing or dirty `ws3-gazetteer` worktree; a live `claude` session in
a worktree; a failing gate; an unexpected PR in the open list; `main` diverged from
`origin/main`. In every case: stop, paste, report. Do not improvise, do not fix, do not launch.
