# Step 30 — WS-7.1 tradecraft crosswalk: `docs/METHODOLOGY-TRADECRAFT.md` + public methodology page (Wave 2, added 2026-09-06)

| | |
|---|---|
| Model / effort / mode | Sonnet / medium / plain session (inventory-driven) |
| Worktree | `/Users/go/code/bnow-net-worktrees/48h-ws7-docs-20260905`, step branch `48h/ws7-docs-20260905-step30-crosswalk` |
| Window | H12 → H16 (start now; independent) |
| Depends on | — (step 29 runs concurrently in `ws7-tradecraft`; if PLAN-WS-7 lands first, its Handoff rewrite binds) |
| Decisions | T1 — if unanswered, write the crosswalk in ICS 206-01 / ICD 203 vocabulary (the addendum's recommendation) and mark the nomenclature choice as T1 in the report |
| Spend | $0 |
| Closing report | `docs/reviews/WS-7-1-CROSSWALK-2026-09-06.md` |

Read `docs/prompts/2026-09-05-48h-COMMON.md` first, then the addendum
`docs/prompts/2026-09-06-ws7-tradecraft-legibility-addendum.md` §1, §2, §4.1, §6, §7, §9. Re-verify
every file the addendum cites at your base commit (`drizzle/9999_claim_source_trigger.sql`,
`map-prompts.ts:255-266`, `synthesize.ts` provider tag, `openai-provider.ts:147`,
`model-config.ts` registry stamps, `schema.ts` ranges, `claim-evidence-model.ts:13-19`,
`docs/PRODUCT-BRIEF.md:107`, `src/lib/conflicts/evidence-selection.ts`).

## Do

PR 1 — `docs: tradecraft crosswalk (ICD 203 / ICD 206 / ICS 206-01 / ICD 208)`:
`docs/METHODOLOGY-TRADECRAFT.md` — per standard, per requirement: BNOW mechanism → enforcing file →
status BUILT / PARTIAL / GAP → which WS-7 step closes it. Carry the two corrections the addendum
names (ICD 208's title; the one-year rule is ICS 206-01). State plainly the two things BNOW does
beyond the standard (DB-enforced citation, ruling 2; the external ISW benchmark loop). "Coverage"
keeps its contract name — never "accuracy". No new claims about accuracy or calibration; reliability
stays behind `showReliability` and the #14 gates.

PR 2 — `site: public /methodology page rendered from the crosswalk`: a curated subset (no file
paths, no task numbers), public and unauthenticated (state that ruling 21 therefore imposes no gate;
if you find a reason it must be gated, STOP and list it — a gate means `requireAcceptedUser` first
and a ROUTES row). Generate the page from the same source as the doc or add a drift test that fails
when the doc's mechanism table and the page's rows diverge. Add the one-page reviewer/partner insert
(`docs/PARTNER-STRATEGY.md` "methodology validator" ask) as a section of the doc.

Rulings: 1 (no ISW prose, no source text — the page describes mechanisms), 3, 21 (posture stated).

## Acceptance

Every cited mechanism resolves to a real file at the base commit (list them with line numbers); unit
test for the page render; the drift test; `npm test` green with counts; no env named as set.

## Report

Per COMMON §5. In **Handoff**: what WS-7.2/7.3/7.4 must keep consistent with the crosswalk's wording.
