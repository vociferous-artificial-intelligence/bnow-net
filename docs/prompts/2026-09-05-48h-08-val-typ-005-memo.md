# Step 08 — val-typ-005 semantic adjudication memo (WS-1.2, #105) (Wave 1)

| | |
|---|---|
| Model / effort / mode | Sonnet / medium / plain session |
| Worktree | `/Users/go/code/bnow-net-worktrees/48h-ws1-adjudication-20260905`, step branch `…/val-typ-005-memo` (step 07 runs concurrently in `48h-ws1-injection`) |
| Window | H1 → H2 |
| Depends on | — |
| Decisions | E4 — the operator signs the ruling; you argue both readings |
| Spend | $0 |
| Closing report | `docs/reviews/EVAL-VAL-TYP-005-ADJUDICATION-2026-09-05.md` (the memo IS the report) |

Read `docs/prompts/2026-09-05-48h-COMMON.md` first.

## Goal

A one-page memo the operator can sign as the written reason #105 requires, without touching
any dataset. The label change itself (if any) lands later as a validation-v3 admission
(WS-1.3); this step records the semantic judgment only.

## Read

`docs/OPEN-TASKS.md` #105 (:1647-1656); `docs/reviews/EVAL-VALIDATION-PARITY-2026-09-04.md:84-88`;
`docs/reviews/EVAL-SUCCESSOR-PLAN-2026-09-04.md:21-23`; `src/lib/evals/contracts.ts:10-14`
(immutability: never edit a reference in place — new case id or datasetVersion);
`src/lib/validation/llm-match.ts` (`MATCH_SYSTEM_PROMPT` and the match rule wording the
production matcher uses — quote the rule, not any ISW text). For the case itself, read ONLY
the development case `val-typ-005` from `docs/evals/analysis/validation-v1.json` via a filtered
command (`node -e '…cases.find(c=>c.id==="val-typ-005")…'`); never print other cases; the
heldout twin with the same match-set signature must not be looked for or read.

## Do

Write the memo with: the takeaway-1 wording as the case carries it (the case text is
synthetic/derived and may be quoted); the two candidate labels (`claimId: null` as authored
from 2-2-1 vote arithmetic, vs `claimId: 1072` "were active" as the same event at weaker
strength); the production matcher's own rule for "same event, weaker strength" (cite the
prompt lines); the consequence of each reading for `expectMajority` (unchanged either way —
say why, with the test that pins it); the consequence for validation-v3 (new case id or
datasetVersion, never an in-place edit); a recommendation with the reasoning; and the exact
decision-log entry text for the operator to sign (`- **2026-09-0X (val-typ-005 semantic
adjudication, #105)** …`), marked UNSIGNED. Update OPEN-TASKS #105's status line to "memo
written; operator ruling pending" — nothing else.

## Acceptance

Docs only; no dataset bytes change (`git diff --stat docs/evals/analysis/` empty).

## Report

The memo, ending with COMMON §5 sections (Scope / Citations / Decisions needed / Handoff —
the handoff names what WS-1.3 must do with the signed ruling).
