# Step 18 — Independent audit of the WS-3 diffs (steps 06, 13, 13b, 14) (Wave 4, after CP3)

| | |
|---|---|
| Model / effort / mode | **Astra / xhigh / plain read-only session** (model diversity from step 17; if Astra is unavailable use Opus / xhigh and say so in the register — not Fable, which is reserved for 26); attended |
| Worktree | `/Users/go/code/bnow-net-worktrees/48h-audit-ws3-20260905`, reset to the audited SHA: `git fetch origin && git checkout -B 48h/audit-ws3-20260905 <main after CP3>`. Read-only except the register. |
| Window | after CP3 (13b and 14 merged) |
| Depends on | #56 (06), #63 (13), 13b's PR, 14's PRs on `main` — take the numbers and merge SHAs from `git log --merges` and INDEX §10 |
| Decisions | none; decides nothing, fixes nothing (step 23 remediates) |
| Spend | $0 beyond re-running the fork itests (`scripts/test-integration.sh` creates/deletes the fork; the worktree gets the TRIMMED `.env.local`, COMMON §4.10). |
| Closing report | `docs/reviews/WS-3-AUDIT-FINDING-REGISTER-2026-09-06.md` on branch `48h/audit-ws3-20260905-finding-register`, PR opened |

Read `docs/prompts/2026-09-05-48h-COMMON.md` first; same register format as step 17
(precedent `docs/reviews/CONFLICT-EVALUATION-FABLE-AUDIT-FINDING-REGISTER-2026-08-18.md`).
Reports under test: `GAZETTEER-IRAN-LEVANT-V1-2026-09-05.md`, `WS-3-1-PERSISTENCE-2026-09-06.md`,
`WS-3-1-OBSERVATIONS-2026-09-06.md`, `WS-3-2-EDITION-DISCOVERY-2026-09-06.md`, the WS-3.0
memo and `PLAN-WS-3-*`. You never open anything COMMON §3 forbids.

**Carried from step 06's report (PR #56) — attack these first:** (a) the `iran-levant-v1`
matcher uses `\b` word boundaries while RU/UA deliberately stays on substring (JS `\b` is
ASCII-only) — verify every transliterated toponym containing `-` or `'` (Bab el-Mandeb, Deir
ez-Zor, Sana'a/Sanaa, al-Qaim, Hodeidah/Hudaydah) has variants that actually match in prose;
(b) the new action classes (`nuclear`, `militia`, `maritime`, `domestic`) — confirm none of
the new terms reaches the RU/UA default signature path (the byte-identity snapshot must cover
ACTIONS, not only TOPONYMS); (c) the session fixed two fail-open holes in its own code (an
empty variant compiling to a match-all regex; `gazetteerFor("toString")` resolving via
`Object.prototype`) — look for the third: unicode case folding, trailing whitespace in
variants, duplicate canonical keys across groups, a prototype-less lookalike key;
(d) `keywordUnmatchable === insufficientData.length` is the invariant that keeps the
denominator unchanged — mutate one side and confirm a test fails.

**Carried from step 13's report (PR #63) — then these:** `edition_write_contention` after
four lost CAS rounds (reachable? bounded? test-pinned?); the journal/merge disagreement
guard (defensive, untested — can any input reach it?); `benchmark_report_editions_final_idx`
violations surfacing as raw driver errors; in-memory vs SQL URL canonicalization divergence;
`anchor_journal` capped at 50; two itests racing on first application of a new migration if
`fileParallelism` ever flips. Plus the attack list in 13b's and 14's own Handoff sections.

**Added 2026-09-08, from the Stage 2 C5-m probes (these are findings, not hypotheses — audit
them as such):** OPEN-TASKS **#114** — `isCleanNotFound` accepts only `status === 404`
(`edition-discovery.ts:226-228`), so understandingwar.org's deterministic 403 after ~20
not-found requests lands in `probeFailures` and forces `probe_failed`; the 403 state also
suppresses REAL pages (three identical `roca` passes: publishedDays 6 → 6 → 13), and
`confirmGapEligible` can therefore never confirm a `publication_gap` on a tripped day — (f14)/C15
is structurally unexercisable for windows longer than about a week. Evidence:
`docs/reviews/C5M-PROBES-2026-09-07.md` §6, R.10, R.15 (decision 1 proposes splitting 403 out of
`probe_failed`). OPEN-TASKS **#116** — the June-2025 suffix slug shape is neither generated
(`run.ts:31-41`) nor parsed (`editions.ts:73`); a backfill over 2025-06-12 → 06-24 manufactures
phantom gaps. Also carry the §4.1 dry-path finality defect (`DiscoveredEdition` has no
`identity`; `--dry` substitutes probe order, `edition-discovery.ts:502-503`) — latent in the
March 2026 corpus (pass 0 = pass 1 on all 23 days), not harmless.

## Lenses

1. **Ruling 5 — migrations.** 0028, 0029 (step 16's, out of scope but in the chain) and
   0030 additive only; `drizzle/meta/_journal.json` idx contiguous 28→29→30 and each
   snapshot's `prevId` = the previous snapshot's `id`; `npx drizzle-kit generate` on `main`
   produces NO new file (the schema and the snapshots agree); `9999_claim_source_trigger.sql`
   last and byte-identical to `2203150`; `migrations.test.ts` pins present for 0028 and 0030.
2. **Ruling 1 — no ISW prose persisted.** No column of the new tables can hold report text;
   `derived` holds hashes/signatures only; the discovery path writes only 0028's tables (never
   `isw_reports` / `source_citations`); observations write only 0030's.
3. **Design fidelity.** Option 3 exactly; C6 = (b) append-only with no overwrite path and
   read-time headline; the deferred hardening actually carried (single transaction, monotone
   day status at SQL level, typed unique violations, anchor journal, URL canonicalization);
   C4 = (a) every edition stored, daily-final scored; C5 link-only anchoring with `none`
   reported, not borrowed.
4. **Production isolation.** `src/lib/validation/run.ts` per-country path byte-identical in
   behaviour (re-run the RU/UA snapshot; diff `run.ts` for anything beyond re-exports);
   `vercel.json` unchanged; step 14's route unscheduled and auth-gated — the gate is the
   FIRST statement of the handler (ruling 21) and `conflict-feature-off.itest.ts` proves a
   flag-off request writes nothing; every observation stamps `unit_flags_version` and nothing
   produced under `unit-flags-v0` reaches a customer surface (INDEX §2.4).
5. **Gazetteer.** Versioned; no person names (ruling 20); every Iran lane scores ≥1 fixture
   unit; `insufficient_data` only where intended; goldens under `fixtures/conflicts/` changed
   only where the report justifies; `docs/evals/analysis/` untouched by these PRs.
6. **Tests as evidence.** Mutate-and-run on the key pins (byte-identity snapshot, append-only
   invariant, FK refusal, gate-first, unit_flags stamping); re-execute every fork itest and
   name the fork; confirm the fork was deleted.

## Output

The register (id, severity, PR, file:line, claim, reproduction, refutation, exact
remediation), per-PR verdict (merge-stands / fix-before-deploy / revert), stale standing
text for step 25, mutation log, fork ids. COMMON §5 sections. No code changes.
