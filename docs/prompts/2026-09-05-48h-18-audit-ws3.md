# Step 18 — Independent audit of the WS-3 diffs (steps 06, 13, 14) — SKETCH (Wave 3, lane C)

| | |
|---|---|
| Model / effort / mode | **Astra / xhigh / plain read-only session** (model diversity from step 17; substitute Fable if Astra is unavailable and say so in the register) |
| Lane / worktree | `48h-audit2-20260905` — read-only; reset to the audited SHA |
| Window | H22 → H26 |
| Depends on | 13, 14 complete; 06 merged |
| Rewrite from | `GAZETTEER-IRAN-LEVANT-V1-2026-09-05.md`, `WS-3-1-PERSISTENCE-2026-09-06.md`, `WS-3-2-EDITION-DISCOVERY-2026-09-06.md` |
| Spend | $0. May run the fork itests again (`NEON_API_KEY`). |
| Closing report | `docs/reviews/WS-3-AUDIT-FINDING-REGISTER-2026-09-06.md` |

Read `docs/prompts/2026-09-05-48h-COMMON.md` first; same register format as step 17.

## Prompt shape (fill in at CP2)

Audit `<PR list>` against `<SHA>`. Lenses: (1) **ruling 5** — migrations 0028/0029 additive
only; journal/snapshots consistent; `9999` last and byte-identical; `migrations.test.ts` pins
present; `db:generate` reproduces the files; (2) **ruling 1** — no column can hold ISW prose;
the persistable scorer shape is the only thing written; `derived` is hashes/signatures; (3)
**design fidelity** — Option 3 exactly (no change to `isw_reports`/`validation_runs`/etc.);
the §5 deferred hardening actually carried (single transaction, monotone day status at SQL
level, typed unique violations, anchor journal, URL canonicalization); (4) **production
isolation** — `run.ts` per-country path byte-identical in behaviour (re-run the RU/UA snapshot
test; diff `run.ts` for anything beyond `export`); `vercel.json` unchanged; the new route is
unscheduled and auth-gated; (5) **gazetteer** — versioned, no person names, Iran lanes score,
`insufficient_data` only where intended, goldens changed only where justified; (6) **tests as
evidence** — mutate-and-run on the key pins; fork itests re-executed (name the fork). Per-PR
verdict; exact remediations. No code changes. Step 23 remediates.
