# Checkpoint 2 — merge queue (2026-09-06, after Wave 2)

| | |
|---|---|
| Model / effort / mode | Opus / high / plain session — **attended, normal permissions** (no `--dangerously-skip-permissions`, no `-p`) |
| Where | the main checkout `/Users/go/code/bnow-net`, on `main` |
| Spend | $0. No DB. No deploy. No `.env.local` copied anywhere. |
| Output | `main` pushed with every queued PR merged; one CP2 line appended to INDEX §10; a short terminal report. No closing-report file. |

Read `docs/prompts/2026-09-05-48h-COMMON.md` §3 and §4.10 first.

## Fence

This session merges. It does not start, launch, background, `nohup`, `claude -p` or otherwise
run any other session or step; does not create worktrees (a conflicting rebase happens in the
worktree the PR was built in); does not delete branches; edits no file except the one
appended line in `docs/prompts/2026-09-05-48h-00-INDEX.md` §10 and whatever a conflict
resolution requires on a PR branch. When the queue is merged: report, stop.

## Preflight

```
cd /Users/go/code/bnow-net && git rev-parse --show-toplevel && git status -sb
git fetch origin && git pull --ff-only && git push origin main
gh pr list --state open --json number,title,headRefName,baseRefName,mergeStateStatus
```

`git status` must be clean apart from the untracked `Claude outputs/`. If `--ff-only`
refuses, or a queued PR is missing from the list, stop and report — do not improvise.

## Queue — this order, one PR at a time

For each PR: confirm its head branch is the one named; confirm `baseRefName` is `main`
(a PR stacked on another PR's branch is retargeted with `gh pr edit N --base main` only
after its parent has merged — never merge into a non-`main` base); confirm CI is green and
`mergeStateStatus` is `CLEAN` (or `MERGEABLE`); then

```
gh pr merge N --merge --subject "Merge PR #N: <short title>"
git pull --ff-only
```

1. **#58** — step 07 injection cases — `48h/ws1-injection-20260905-injection-cases`. CI is the
   check; do not run `check-regen.sh` yourself.
2. **#57** — step 11 PR 1, Ask attribution report — `48h/ws2-routing-20260905-ask-attribution-report`.
3. **#59** — step 11 PR 2, embed pricing — `48h/ws2-routing-20260905-embed-pricing`.
4. **#60** — step 12 PR 1, provider dimension — `48h/ws2-provider-20260905-provider-dimension`.
5. **#61** — step 12 PR 2, stacked on #60 — `48h/ws2-provider-20260905-eval-identity-decouple`:
   `gh pr edit 61 --base main`, re-check mergeability, merge.
6. **#63** — step 13, migration 0028 — `48h/ws3-conflict-20260905-mig-0028-benchmark-editions`.
7. **step 16 PR 1** — log-drain design — `48h/ws4-ops-20260905-log-drain-design` (take the
   number from the list).
8. **step 16 PR 2** — receiver + migration 0030 — `48h/ws4-ops-20260905-log-drain-receiver`,
   stacked on PR 1: retarget to `main`, merge. (D10 note: step 13b then claims the next free
   migration number; step 13's report says nothing depends on the number.)
9. **step 22** — remap runbook + `--base-ack` guard — `48h/ws2-remap-20260905-map-remap-runbook`.
10. **#66** — step 29 PLAN-WS-7 — `48h/ws7-tradecraft-20260905-step29-plan-ws7`.

Not in this queue: #47, #48, #51 (operator housekeeping, own instructions) and anything
marked HELD.

## If a PR reports conflicts

`cd /Users/go/code/bnow-net-worktrees/48h-<worktree>-20260905` (the worktree in the branch
name), `git status` must be clean, then `git fetch origin && git checkout <branch> &&
git rebase --update-refs origin/main`. Resolve only these shapes: `docs/PROGRESS.md` and
`docs/BLOCKERS.md` keep BOTH blocks in date order (the union driver usually does it — check
the result reads in order); an `AGENTS.md` decision-log tail keeps both sides, `main`'s
first; `docs/OPEN-TASKS.md` keeps both status lines. Any conflict in code, migrations,
`drizzle/`, or `src/db/schema.ts`: stop and report with the conflict block pasted — do not
guess. After resolving: `npm run typecheck && npm test` on the rebased branch,
`git push --force-with-lease`, wait for CI, then merge and continue the queue.

## After the last merge

```
git pull --ff-only && npm run typecheck && npm run lint && npm test
ls drizzle | tail -5
```

Append ONE line to INDEX §10 in the CP1 line's shape: time (ET), each PR in merge order with
its merge SHA, every conflict and how it was resolved, the unit test count and file count on
post-merge `main`, and the migration files now on `main` (expect 0028 and 0030, with
`9999_claim_source_trigger.sql` last). Commit `docs: 48h program log — CP2 merge queue landed`,
push, print `gh pr list --state open`, and stop.
