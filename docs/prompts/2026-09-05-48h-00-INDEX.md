# BNOW.NET 48-hour execution program — index (2026-09-05)

**What this is.** The operator has a large budget of frontier-model agent credits to spend
over the next 48 hours and one human reviewer (the operator). This index sequences the
CTO roadmap handoff (`docs/prompts/2026-09-05-cto-roadmap-handoff.md`) into agent sessions
that can run in parallel, names the model / effort / mode for each, and points at one
self-contained prompt file per step (`docs/prompts/2026-09-05-48h-NN-<slug>.md`). Every
step ends with a written report so the next step — or a rewritten prompt — can start cold.

**Status of this document.** Authored from a verified snapshot of `origin/main` @ `883e5e3`
plus the untracked handoff, read in full by eight parallel reader agents whose findings were
cross-checked against file:line evidence and then judged by an independent panel. Where the
readers found the handoff itself stale or wrong, §1 says so; the step prompts already carry
the corrections. The program is a living document: the operator rewrites later prompts at
each checkpoint from the reports of earlier steps (§6), and appends to the program log (§10).

**Binding scope for every session in this program** (also in `…-48h-COMMON.md`, which every
prompt reads first): planning sessions plan; execution sessions build code + tests on a
branch and open a PR; nothing deploys, changes a Vercel environment, writes production data,
spends money, edits the evaluation datasets under `docs/evals/analysis/` (except step 07's
two authorized new files: the dataset and its committed offline-results file), the scorer, the registry approvals, or the map lock without
a named operator authorization recorded in the decision log. Where the handoff says
DECISION, the session lists it (§2) — it never decides.

---

## 0. How to run the program

1. **H0 — operator kickoff (≈45 min, §5 Wave 0).** Answer the decision sheet (§2) — at
   least the items marked H0. Create the lane worktrees natively on the Mac (§4). Merge
   PR #47; resolve PR #48 per D1. Confirm the Neon key with one branch create/delete
   (§5 Wave 0). Paste the Wave 1 prompts into fresh sessions, one per lane.
2. **Each session is started INSIDE its worktree**, never in `~/code/bnow-net`:
   `cd /Users/go/code/bnow-net-worktrees/48h-<worktree>-20260905 && claude`, then paste the
   step prompt. (A session started in the main checkout has `main` as its working tree; if
   it forgets to `cd`, it edits and commits on `main` — six concurrent sessions make that a
   collision, not a hypothetical.) The session reads
   `docs/prompts/2026-09-05-48h-COMMON.md`, then its own prompt file, proves it is in the
   worktree (COMMON §1), opens a PROGRESS.md plan block (AGENTS.md operating protocol step 1),
   does the work on its step branch, and ends with the closing report the prompt names. Reports go to
   `docs/reviews/` under the name the prompt gives, committed on the same branch.
3. **Checkpoints (§6).** The operator reads reports, merges PRs in the order §6 gives (migration
   PRs have a fixed order, §4), records decisions as decision-log entries (the reconcile
   session drafts them), and rewrites or confirms the next wave's prompts. A prompt marked
   *SKETCH* in its header must be rewritten from the named upstream report before use.
4. **H48 — close.** Step 26's final audit issues a per-PR deploy go/no-go; step 27 (operator)
   deploys only through the new release checklist and only what needs no new cap env.
   Nothing in this program deploys before that point.

Model roster used throughout (§3 has the rules): **Fable** = architecture plans and
adversarial audits (plan mode / ultracode); **Astra** = the second frontier model, used where
independence from Fable's reasoning is the point (second-opinion audits) — substitute Fable
if Astra is unavailable; **Opus** = implementation PRs with tests; **Sonnet** = inventory,
docs, and mechanical edits with a verification script.

---

## 1. Corrections to the handoff found while planning (read before H0)

The handoff is accurate where it cites code (78 of 100 checked file:line citations verified
exactly; the rest are off by a few lines or describe files that moved). These are the
findings that change what the steps do. Each step prompt already carries them.

1. **WS-6.2 target file.** The append-only archive already exists as `docs/DECISIONS.md`
   (1,480 lines; named by AGENTS.md:10, :98, :109, :497 and OPEN-TASKS #92). Creating the
   handoff's `docs/DECISION-LOG.md` would fork the archive. Step 15 targets `docs/DECISIONS.md`.
   Also: "last 30 days inline" keeps ≈1,160 log lines inline (file ≈1,740 lines), so the
   handoff's "two-thirds reduction" needs either a 7-day inline window (file ≈780 lines) or
   pointer-only (≈580 lines). Decision D5 chooses the window.
2. **`NEON_API_KEY` is not expired.** BLOCKERS.md:76-79 (2026-07-15) was superseded the same
   day (PROGRESS.md:1502); AGENTS.md:978 says WORKS; disposable Neon forks ran on 2026-09-03
   and 2026-09-04, and the key is present in the Mac `.env.local`. WS-2.3 and WS-4.2 are not
   credential-blocked. Only the GitHub Actions secret is absent (CI's integration job
   clean-skips — a green CI "integration" check is NOT evidence; every PR reports local
   disposable-fork integration results instead).
3. **WS-3.1 as written contradicts the reviewed schema design.** Adding `series`/`edition` to
   `isw_reports` with a relaxed unique key is exactly Option 1, which
   `docs/designs/CONFLICT-REFERENCE-REPORTS-SCHEMA.md` §2 rejected in favour of Option 3
   (new `benchmark_report_editions` + `benchmark_series_days`, `isw_reports` untouched;
   disposable DDL and `SqlReferenceReportRepository` already exist). Step 05's memo carries
   this as decision C1 with Option 3 recommended.
4. **`legacy_only` semantics.** The shipped contract keeps legacy claims as labeled members of
   published retention (excluded only from corpus recall); the handoff's "excluded from the
   numerator" would be a methodology change. Decision C8.
5. **`/scoreboard` is public, ungated, and sitemap-listed.** The handoff's WS-3.5 gate order
   (`requireAcceptedUser` then `requireConflictsUi`) would gate or 404 a public page. The
   conflict view needs its own route or a decision to gate the scoreboard (C10).
6. **Six injection cases cannot be appended to `map-v2.json`.** It is byte-generated by
   `scripts/evals/corpus-v2/run-admit.ts`, byte-checked by `check-regen.sh`, and every
   committed v2 results file is identity-pinned to its content hash. The cases need their own
   dataset file (E1). The "report-only follow/report sub-label" is a `score-map.ts` change,
   which the successor plan's step-1 envelope excludes (E2). The step-1 authorization itself
   is not recorded in the repo (the plan only proposes it): step 01 drafts the entry.
7. **`#108` does not exist** (OPEN-TASKS ends at #107). The "withheld harness" is most
   plausibly the parked branch `claude/local-model-ask-eval-20260817` described in
   `PENDING-MERGE-ADJUDICATION-2026-08-25.md:90-96`. Decision R5 names the locator.
8. **`#44` is not an Ask item** — it is the `X_DAILY_USD_CAP` code-default reconciliation
   (`src/lib/adapters/x-api.ts:201,223`, now $4 in prod vs 1.5 default). Step 11 closes it as
   hygiene, not as Ask parity.
9. **Registry version bump hazard.** `ANALYSIS_ROUTING_REGISTRY_VERSION` is stamped into every
   offline eval results header and compared on resume; `hardening-cli.test.ts:192-206` pins
   the committed files. Bumping to `analysis-reg-v2` naively rewrites files under
   `docs/evals/analysis/`. Step 12 decouples offline identity first (R2).
10. **Anthropic seam is a ruling-4 hazard today.** `getProvider()` selects the unmetered,
    unregistered `AnthropicProvider` whenever `ANALYSIS_PROVIDER=anthropic` + key, or when
    only an Anthropic key exists — no `workloadDispatchConfig`, no `tryReserve`. Step 09 closes
    this regardless of the provider-ambition decision, and fixes `anthropic-provider.ts:70`.
11. **A $0 daily cap fails closed** (`spend-guard.ts:136`; `envCap` treats 0 as unset), so an
    "unpriced-local" class (option C) cannot reuse the USD-cap guard shape unchanged.
12. **The keyword rung already returns a scored outcome for Iran**; the recorded pre-soak
    blocker is the missing `insufficient_data` diagnostic plus the empty Iran gazetteer. Step
    06's acceptance is worded accordingly, and the RU/UA default signature path must stay
    byte-identical (it feeds production `validation_runs`).
13. **AGENTS.md standing text is stale vs `main`:** snapshot still says main tip `8a19ade` ==
    production; PR #46's merge hash (`883e5e3`) is unrecorded; no 2026-09-05 step-1A entry;
    the directory map omits `src/lib/conflicts/`, `src/lib/evals/`, `src/lib/embeddings/`,
    `scoreboard/`, `registry/`, `analyst/`, `analytics/`, `cron/`, `docs/prompts/`,
    `docs/evals/`. Step 01 corrects all of it in place before any other PR cites AGENTS.md.
14. **Linux-isms (operator's question).** The toolchain is already portable: darwin-arm64
    binaries in the lockfile, all `child_process` use is `execFileSync`/`spawn` of
    `node_modules/.bin`, zero case-collision paths, zero import-case mismatches across 1,784
    imports, CRLF only in 7 saved HTML/RSS fixtures, both live shell scripts bash-3.2/BSD
    clean. Debt is docs + one dead script (§8). One consequence matters for the roadmap: the
    Mac reaches `api.openai.com`, so the "route all LLM work through the deployed route"
    premise in `map-remap.ts:3-6` is gone — a local `next start` bound to a disposable Neon
    branch is a viable WS-2.3 dry-run target (step 22).
15. **Commit attribution.** Repo `CLAUDE.md:41-45` forbids `Co-Authored-By` trailers,
    "Generated with" lines, and model/vendor names in commits, PRs, code comments and file
    contents. This program's prompts instruct sessions to write plain commit messages; the
    review docs may name the reviewing model only where the existing decision log already
    does (D12 confirms).

---

## 2. Decision sheet (operator)

**How to use this.** §2.1 is the short list of what is still waiting on you; §2.2 is the
permanent record of what you already answered (your own words, verbatim); §2.3 holds the
decisions the Wave-1 plans surfaced after this sheet was written; §2.4 carries the conditions
and hazards attached to answers already given — read it before Wave 2 runs.

**Rules that do not change.** Recommendations are not defaults: **blank = defer**, and a
session that reaches a blank lists it in its report and continues with decision-free work.
Spend (D6, D7), production writes (O2, N2, N3) and writes under `docs/evals/analysis/`
(E1, E5) require an explicit written yes **with the value** (cap, ceiling, file name) — a
session that finds them blank builds on its branch, marks the PR HELD, and stops before any
run. Every answer becomes a decision-log entry: drafts live in
`docs/reviews/DECISION-ENTRIES-DRAFT-2026-09-05.md` — sections (a)–(d) for H0, **section (e)
for the Checkpoint-1 answers below** — and the operator signs by copying the entry into
AGENTS.md's `## Decision log` (step 10 item 1, then step 25). An unsigned draft means "not
authorized".

### 2.1 Open right now

**NOTHING IS WAITING ON AN OPERATOR ANSWER.** Reconciled 2026-09-07 (CP4): every decision this
program raised has been answered. Where this sheet and AGENTS.md's `## Decision log` disagree,
**the log is authoritative.** Answered at CP3/CP4 and moved to §2.2: **R6**, **R7** (rate and id
both settled), **O1**, **A1**, **A2**, **A3**, **O3**, **T3** (tables signed), **T4** + **T4-b**,
**T5**, **C5-m**, **D7**, **R4**, **C15**, **R14**. Entries for the CP4 batch are drafted as
sections **(f1)–(f15)** of `docs/reviews/DECISION-ENTRIES-DRAFT-2026-09-05.md` and are
**UNSIGNED until step 25** — an unsigned draft still means "not authorized". If a later step
surfaces a new decision, it is added back here.

**Carried actions attached to answered decisions** — work items, not decisions:

- **A1** — no OPEN-TASKS entry for **WS2-F06** exists yet. Filing it is half the decision; step 23 is the other half.
- **A2** — the drain runbook correction must reach the operator **before the Vercel registration step** (O1 puts the operator there personally), not merely before deploy.
- **T4 / T4-b** — WS-7.2 carries a test pinning the disclosure **OFF for every resolvable role**; the gate is a policy function on the `view-policy.ts` pattern, never a boolean constant; the disclosure block is **per stage**, and `extraction` renders "not recorded for this digest" rather than being back-filled with the digest-run map model.
- **T4-b / step 20b ordering** — `mapreduceProviderTag()` (`synthesize.ts:443-449`) hard-codes the literal `openai:` prefix and is provider-blind. **Fix it before the Anthropic digest path is enabled**, or a Claude-synthesized digest is stamped `openai:…` durably on `digests.provider`.
- **T5** — verify step 30's source-scan moat test pins option (a) specifically, not merely "some wording"; `/methodology` is already live (PR #69, `a485c80`).
- **C5-m** — the **operator** runs both probes (step 10 item 7) and pastes the outputs into step 24's prompt.
- **D7 / R4** — operative `--budget` is **$1.00** ($10 is the authorized outer bound); caps are `MAP_SPRINT_USD_CAP = T + C` / `MAP_USD_CAP_DAILY = D + C` off the fork's **copied** ledger, re-read on the day; verify `MAP_CONTENT_CHARS` is **absent from all three Vercel environments before AND after**.
- **C15** — reopen and schedule `first_observed_at timestamptz` on this observation only: **any `publication_gap` whose confirming `probe_failed` came from the same backfill run.**
- **R14** — when the paid answer-model matrix is scheduled, its hatch is a route-unreachable `opts.evalUngated` set only by `ask-eval.ts`, proven by source scan. **`ASK_SCORECARD_GATE=0` is forbidden.**
- **R7** — `claude-sonnet-5` is an alias, not a dated snapshot; **re-verify the $2.00 / $10.00 rate at the first real Anthropic invoice.**

### 2.2 Answered — the record

| ID | When | Decision | Recommendation | Operator answer (verbatim) |
|---|---|---|---|---|
| D1 | H0 | PR #48 roster: merge as-is / redact / remove `docs/OUTREACH-ROSTER-2026-08-23.md` from git | Remove from git (handoff §4.0 (c)); keep the GO-NO-GO register. | yes remove outreach from git |
| D2 | H0 | Provider ambition for WS-2: A (OpenAI-only) / B (+Anthropic) / C (+local OpenAI-compatible) | **A now, seam-ready for B/C**: step 12 adds the provider dimension with allowlist `{openai}`; B/C PRs (step 20b) only if answered B/C. B needs an Anthropic key + operator-verified price; C reverses the "local ids stay out of `PRICES_PER_MTOK`" stance and needs the request-cap-only guard shape (§1.11). | B. Yes Anthropic ANTHROPIC_API_KEY added to .env.local. Same Budget. C. provisional yes. Don't need for at least two weeks if easily added in the future, leave this out of this dev round. |
| D3 | CP1 | Validation-v3 versioning: conflict-keyed = v3 (with step 2) or v4 (after per-country v3) | v4. Keeps WS-3 off the label timeline and step 2's identities stable. | Yes approve |
| D4 | CP1 | Conflict unit details (memo from step 05: C1–C14) | Step 05 lists each with a recommendation; C1 Option 3 is required before step 13. | yes sign all 14 |
| D5 | H0 | AGENTS.md compaction: approve split; inline window 0 / 7 / 30 days; restore strict date order when reunifying | Approve; 7-day inline window; restore date order (sanctioned by DECISIONS.md:16-19 precedent). | Approve. AND keep below 150k characters as an additional constraint to avoid warning like: AGENTS.md is over the 150.0k-char limit (156.8k chars) |
| D6 | H0 | Spend: WS-1.1 ×3 capture run ≈18 map calls (≈$0.01) on the kept Neon branch; campaign-local `LLM_SPRINT_USD_CAP` value | Authorize $0.50 ceiling; record as the step-1 authorization entry (§1.6). | Yes Authorize $0.50 to $2.00 Summary report should explain impact of this LLM_SPRINT_USD_CAP |
| D8 | H0 | Credentials: confirm `NEON_API_KEY` works; `VERCEL_TOKEN` regen (optional); `ANTHROPIC_API_KEY` only if D2 = B | Confirm Neon; leave the others. | NEON, ANTHROPIC, OPENAI keys confirmed working. VERCEL_TOKEN is valid and correctly scoped to bnow-net |
| D9 | H0 | Who authors the six injection cases | A fresh Opus (or Astra) session under the forbid list in step 07 (never a session that has opened the artifacts folders or `build-draft.py:746-800`); the operator names the session in the exposure ledger. | We have never used Astra in this project. Use Astra via openai key to "author" |
| D10 | CP1 | Migration numbering/merge order across lanes (§4) | Accept the assignment in §4. | Accept, minus 0031 if R1 = (a): 0028/0029 (13) → 0030 (16). |
| D11 | H0 | May the reconcile session (step 01) correct AGENTS.md standing text before the compaction lands? | Yes. | Yes correct AGENTS and remove note VERCEL_TOKEN is working now retricted to bnow-net project |
| D12 | H0 | Model names in review/prompt docs (CLAUDE.md:43-45 vs existing practice) | Allow in `docs/prompts/*`, `docs/reviews/*` and the decision log only; never in commits, PRs, code, or code comments. | Yes model names or recommendation in these files is OK. |
| E1 | H0 | Vehicle for the six cases: new dataset file `map-inj-dev-v1.json` vs pre-creating `map-v3.json` | New file `map-inj-dev-v1.json`; WS-1.3 unions it into map-v3 later. | Yes approve new name. |
| E2 | — | Report-only injection follow/report sub-label (a `score-map.ts` change) | Deferred to step 2 (WS-1.3) outright — the step-1 envelope excludes scorer changes and adding a checks key would rewrite the frozen v2 offline results on regeneration. | (deferred by the program; step 07 records it in the ledger/handoff) |
| E3 | H0 | Exposure-ledger home | `docs/reviews/EVAL-EXPOSURE-LEDGER.md`, append-only, dated sections. | Yes fine for new file in new location |
| E4 | CP1 | val-typ-005 ruling: "activity increased" = same event, weaker strength, or no match | Step 08's memo recommends `claimId: null` on semantic grounds; `expectMajority` unchanged either way. | Agree |
| E5 | CP1 | Refresh of the committed conflict offline results (`docs/evals/analysis/results/conflict-{roca,iran}-v1-offline-fixtures.json` + `CONFLICT-EVAL-SCORECARD`) when the scorer-level `insufficient_data` per-unit diagnostic lands (step 19): the golden `cc-matcher-failclosed-013b#B-zero-valid-rounds` changes, its bytes feed `conflictDatasetContentHash`, and `hardening-cli.test.ts:193` fails until the two results files are regenerated (`--offline --profile conflict --fresh --fresh-ack`; deterministic, $0, no provider contact, but a write under `docs/evals/analysis/`) | Authorize for step 19 only, with an exposure note; step 06 lands the rung-level diagnostic alone so its PR stays inside the envelope. | Agree |
| R1 | CP1 | Ask per-model attribution shape: read-only report over `ask_usage` / `provider_usage.model` column (migration) / per-model rows | Read-only report first (no migration, no env); revisit after step 4 of the eval program. **Consequence: migration 0031 is not needed.** | Agree |
| R2 | CP1 | Registry-version bump strategy (§1.9) | Decouple offline identity from the live constant in this window; do NOT bump to `analysis-reg-v2` until the first non-OpenAI approval lands. | Agree |
| R3 | CP1 | `hasScorecard()` gating on the Auto money path | Yes, behind a test that pins baseline behaviour unchanged when no env override exists. | Agree |
| R5 | H0 | Locator for "#108" | Name the branch/commit or file the task. | Name branch → resolved: `claude/local-model-ask-eval-20260817` (program log 2026-09-06 14:10 ET) |
| O1 | H0 (plan tier) | Does the `vociferous` Vercel plan support log drains? | If not, step 16 ships the design memo only. | Yes Vercel supports Log Drains. Neon DOES NOT support drains on the "Launch" level plan. → see §2.4: the receiver does not use any Neon drain feature |
| O2 | CP1 | #79 drain: authorize production writes for `isw-refresh.ts --theater ru` + `registry-materialize`; take a Neon backup branch first? | Authorize; take the backup branch (Iran precedent). | Agree |
| T1 | CP2 | WS-7 nomenclature: ICS 206-01 descriptors + ICD 203 estimative language as the product vocabulary; NATO AJP-2.1 / Admiralty two-axis codes as derived EXPORT fields only | Yes — the addendum's recommendation. | Yes agree |
| T2 | CP2 | Access date in the citation mode: (a) `fetched_at` shown in citation mode only, labeled "Accessed (BNOW ingest)"; (b) `asOf`; (c) withheld | (a), recorded in the decision log. | Yes agree |
| T3 | CP3 | The two mapping tables — approach vs the tables themselves | Sign after reading step 29's tables. | Yes agree to approach need to see the tables after step 29 |
| R6 | CP3 | Caps for the `anthropic_digest` metering row: reuse `LLM_SPRINT_USD_CAP` + `LLM_DIGEST_USD_CAP` vs a dedicated `ANTHROPIC_DIGEST_USD_CAP` | Reuse — a new cap env is a deploy-ordering hazard for no gain. | Signed 2026-09-06 as recommended (reuse; no new env var) |
| R7 | CP3/CP4 | The Anthropic model ids and their operator-verified list prices for `pricing.ts` | Per-model rows, each verified on the day; an unpriced model is refused before reservation, by design. | Haiku 4.5 entered $1/$5 (2026-09-07). **Sonnet 5 resolved 2026-09-07:** "The $2/$10 per million input/output token pricing for Claude Sonnet 5, announced at launch as introductory pricing through August 31, 2026, is now the standard price. The previously scheduled increase to $3/$15 per million input/output tokens on September 1, 2026 will not occur." → **$2.00 / $10.00**; exact model id still needed (§2.1 R7-b) |
| O1 | CP1 | Log-drain sink, retention days, and who registers the drain | Own `/api/logs/drain` receiver into `runtime_logs`; 14 days; operator registers via Vercel after the receiver deploys with `LOG_DRAIN_SECRET` in Production. | Signed 2026-09-06 as recommended |
| A1 | CP4 | Own WS2-F06 (the `ASK_PIPELINE=legacy` rollback dispatches a paid completion with **no SpendGuard at all**, pre-existing since `cea8cac`) as an OPEN-TASKS entry rather than fixing it in #67 | File as OPEN-TASKS, fix in step 23. | A1, A2, A3, and O3 accepted and approved |
| A2 | CP4 | May #64 deploy before its enablement order is corrected? | (b) — fix the docs before *registration*, not before deploy. | A1, A2, A3, and O3 accepted and approved |
| A3 | CP4 | Accept the step-17 register's bounded verification as that step's evidence | Accept, with its stated limits (no DB, no env read, no tree-wide lint/build, G2–G16 open) on the record. | A1, A2, A3, and O3 accepted and approved |
| O3 | CP4 | Accept fork-based itest proofs as the #102/#103 "live proof", or require a preview-deployment drill | Accept fork proofs this window; log the drill as follow-up. | A1, A2, A3, and O3 accepted and approved |
| T4 | CP4 | WS-7.2's ICS 206-01 AI-tool disclosure vs the 2026-07-16 provider-hiding decision | (a) disclose in citation mode only. | "a disclose in so-called citation mode only and that would only be for paying customers and if this is too complex right now hold and do not display. We need strong market signals that this full disclosure is needed, which we don't have yet so we can add the capability now if easily done, but hold on the public display or display for login users at this point in time." → **fallback branch is operative** (no paying-customer gate exists — `access-context.ts` is a beta stub, billing entitlements module does not exist): capability BUILT, display DARK on every surface; the 2026-07-16 decision stands unreversed; citation mode is knowingly non-conformant with ICS 206-01 |
| T5 | CP4 | May public `/methodology` print the hedging weight constants? | (a) qualitative ordering only — they are a moat field. | approve a qualitative ordering |
| C5-m | CP4 | Authorize the read-only production probe measuring citation-anchor vs daily-final on multi-edition days | Authorize; the operator runs it and pastes both outputs into step 24. | C5M authorized |
| T3 | CP4 | The two WS-7 mapping tables themselves (PLAN-WS-7 §6.1–6.3) | Sign as drafted, with T3-a (no `claims.confidence`, no source reliability) and T3-b (`almost certain`, the three sub-even bands and AJP-2.1 levels 4/5 never machine-assigned). | T3 tables approved |
| D7 | CP4 | Spend ceiling for the measured WS-2.3 remap run | Authorize a ceiling `C` on the fork's own `openai_map` ledger, one day, `--limit 1000`; runbook recommended $1.00. | "$10 if that sounds insufficient, let me know" → **far more than sufficient** (the run is ≈$0.05 actual). Recorded as the authorized OUTER BOUND; operative `--budget` is $1.00. **`C` is the `--budget`, not the env cap** — caps are `MAP_SPRINT_USD_CAP = T + C` / `MAP_USD_CAP_DAILY = D + C` off the fork's COPIED ledger (T was $23.0763 on 2026-09-06), so a literal `MAP_SPRINT_USD_CAP=10` would refuse the first reservation |
| R4 | CP4 | Measurement path for the D7 run | (a) `MAP_CONTENT_CHARS=1499` on the fork-bound local server. | approve a — hazard binding: the variable must be verified ABSENT from all three Vercel environments before AND after, or the hourly worker silently re-maps the whole corpus at production spend |
| R7-b | CP4 | The exact Anthropic API identifier for the Sonnet 5 pricing row | Prefer a dated snapshot id over an alias. | use `claude-sonnet-5` as model identifier which is correct for direct anthropic access — recorded with the alias-repoint risk and a re-verify-at-first-invoice mitigation |
| T4-b | CP4 | Build shape for the withheld tool disclosure | Option 1 + refinements: citation mode ships, only the disclosure is gated, via a policy function not a constant. | option 1 with your suggested refinements — see (f13); disclosure is gated PER STAGE, and a provider-blind `mapreduceProviderTag()` defect must be fixed before step 20b enables Anthropic |
| C15 | CP4 | `publication_gap` needs two runs ≥24 h apart, but `benchmark_series_days` has no timestamp, so the rule shipped as a ≥48 h report-date proxy | (a) accept as shipped; (b) add `first_observed_at timestamptz` later. | **a+** — accept as shipped, with the reopening trigger written down: any `publication_gap` whose confirming `probe_failed` came from the same backfill run |
| R14 | CP4 | The scorecard gate blocks the very matrix that would produce a scorecard | (a) route-unreachable in-code opt-in; (b) `ASK_SCORECARD_GATE=0` — not recommended; (c) offline harness. | **b** — pre-commit the shape now, build nothing: the hatch is a route-unreachable `opts.evalUngated` set only by `ask-eval.ts`; `ASK_SCORECARD_GATE=0` is FORBIDDEN; built in the PR that schedules the matrix |
| — | CP4 | ID collision: the remap runbook minted its own R14 and R15, both already in use | Renumber the runbook's. | rename runbook R14 to R16 (and R15 → R17, same collision, found while renaming) |

### 2.3 Surfaced by the Wave-1 plans (recorded; not on the critical path)

PLAN-WS-2 and the WS-3.0 memo raised these after this sheet was written. R6/R7 are in §2.1
because step 20b waits on them; the rest block nothing this window and are listed so a later
session does not rediscover them as new.

| ID | Source | Item | Disposition |
|---|---|---|---|
| N1 | memo §3 | Supersede the soak design's `openai_eval` provider row with `llm_conflict_match` + `CONFLICT_MATCH_USD_CAP_DAILY`, every threshold unchanged | Sign with D4/C12; drafted in the 3.6-prep checklist |
| N2 | memo §3 | Manual `GET /api/cron/conflict-validate` in production writes the new tables — a production write | Forbidden until the WS-3.6 scheduling entry, except one operator-signed bounded smoke (one date, one conflict) |
| N3 | memo §3 | Optional $0 idempotent backfill registering existing `isw_reports` URLs as edition rows | Authorize after PR 3.2a deploys, with a Neon backup branch; it also gives register #12.2 its real compound-rate sample |
| R8 | PLAN-WS-2 | Embedding prices beyond `text-embedding-3-small` | Leave unpriced (refused before reservation) until a swap is wanted |
| R9 | PLAN-WS-2 | Spend guard for `ask-eval-harvest --generate` | Factory swap + exemption removal now; a dedicated guard row as a follow-up |
| R10 | PLAN-WS-2 | `X_DAILY_USD_CAP` read with fail-open `envNum` vs every LLM guard's fail-closed `envCap` | Comment only now; aligning is a behaviour change needing its own ruling-4 env note |
| R11 | PLAN-WS-2 | The `$0` price class for `openai_compatible` (reverses the 2026-08-25 "local ids stay out of `PRICES_PER_MTOK`" stance) | Record only; decide when option C is scheduled (D2: not this round) |
| R12 | PLAN-WS-2 | Entity-audit response format (`json_object` vs strict `json_schema`) | Keep `json_object`; non-strict eval path |
| R13 | PLAN-WS-2 | Anthropic digest cells have no strict-schema equivalent | Record `schemaMode` in the identity; separate comparability class |
| R15 | step 11b | An unscorecarded answer model still pays for embed + rerank before the gate refuses (low) | Accept — recorded; the gate's purpose is the answer-model spend and the scorecard integrity |
| **P1** | memo C10 | **Pre-existing, not caused by this window:** the public scoreboard detail page renders `claimText` to anonymous visitors (`/scoreboard/[country]/[date]/page.tsx:128`) | Left as-is for this window (our own claim text; the charter names a public scoreboard). Flagged as a deliberate product posture to confirm or open as a task — the memo declined to adjudicate it |

### 2.4 Conditions and hazards attached to answers already given

- **D4 / C13 (`unit-flags-v0`).** `compound` is hard-coded `false` until a human-calibrated
  `compound-v1` exists, which is the one recommendation that errs toward OVER-credit (a
  genuinely compound unit can be attested `full`). Binding condition on the signature: every
  observation stamps `unit_flags_version`, the view labels such rows "compound handling
  undetermined — not soak-eligible", and **no number produced under `unit-flags-v0` leaves the
  internal view** — not to a customer, not onto `/scoreboard`, not into a report figure.
- **D4 / C12 (new `llm_conflict_match` ledger row).** Deploying the guard with
  `CONFLICT_MATCH_USD_CAP_DAILY` unset stops nothing (the path is inert and the ladder falls
  back to the keyword rung). Ruling 4's ordering bites at WS-3.6: the cap must exist in ALL
  THREE Vercel environments **before** the cron line is added, never after.
- **D4 / C5 (link-only citation anchoring).** Some scored editions will legitimately carry
  `citation_anchor = none`, and the soak's non-independence diagnostic reports `unavailable`
  for those observations rather than borrowing a sibling edition's endnotes. Step 14 measures
  how often anchor ≠ daily-final; changing production's probe order is a WS-3.7 decision.
- **D10 (numbering) — amended at CP2/CP2b.** Step 13 built 0028 only (C6 was blank at
  launch). Step 16's receiver arrived as "0030" but its `drizzle/meta/0030_snapshot.json` chained
  from the 0027 snapshot (the branch predates 0028), so it is REGENERATED on top of 0028 at CP2b
  and becomes **0029** (SQL identical, chain 0028→0029). Step 13b's observation migration is
  **0030**, generated on top of 0029. Rule from this: a migration branch that is rebased over
  another migration regenerates its snapshot; it never keeps a snapshot whose `prevId` is not
  the last one on `main`. `9999_claim_source_trigger.sql` stays last.
- **E5.** The regeneration is deterministic, $0 and contacts no provider, but it is a write
  under `docs/evals/analysis/` — the folder this program otherwise freezes. Authorized for
  **step 19 only**; the PR body must show the command, the changed golden, and an
  `EVAL-EXPOSURE-LEDGER.md` entry.
- **O2 (#79 drain) is not a pure backfill.** `registry-materialize` does a full DELETE +
  rebuild of `source_theater_stats` inside one transaction and updates the global reliability
  columns on `sources`; the digest gather orders documents by `s.reliability_score`
  (`src/lib/analysis/digest.ts:89,100`). Draining 36 RU reports therefore changes **which
  documents enter ru digests** — the intended fix, but a live behaviour change. Run it away
  from the 02:00Z finalize, keep the backup branch until the decision-log entry is written,
  and check the next ru digest and scoreboard row.
- **R4 (a) hazard.** `MAP_CONTENT_CHARS` is part of the extractor-version basis
  (`src/lib/analysis/map-prompts.ts:260`) — which is exactly why `1499` on a fork-bound local
  server gives remap pending work with zero code change. The same property makes it dangerous:
  if that variable ever reaches a Vercel environment, the hourly worker's version hash changes
  and it silently re-maps the entire corpus at production spend. The runbook must verify the
  variable is ABSENT in all three Vercel environments before and after the measurement.
- **O1 — the "Neon does not support drains" note concerns a Neon feature this design never
  uses.** The recommended receiver is our own Vercel route (`/api/logs/drain`) inserting into
  our own `runtime_logs` table; it needs nothing from Neon beyond ordinary writes. Vercel
  supports the drain, which is the only platform capability required. (Step 16's prompt
  already carries this correction; the step-01 decision-entry draft states the opposite and is
  corrected in the CP1 entries.)
- **D6 — what the campaign-local `LLM_SPRINT_USD_CAP` actually does** (the explanation the
  answer asked for). It is the **all-time total cap for one provider row**, compared against
  that row's cumulative lifetime total before each call (`src/lib/usage/spend-guard.ts:122`) —
  not a per-run budget. On the eval CLI the row is `openai_eval`, and the kept disposable
  branch's ledger **already holds ≈$0.15** from the 2026-09-03 campaign, which counts against
  whatever value is set: $0.50 leaves ≈$0.35 of headroom, $2.00 leaves ≈$1.85, against a run
  that costs ≈$0.01. Because the check is a threshold test before dispatch, terminal spend can
  exceed the ceiling by at most one response's cost. It does not stand alone —
  `EVAL_USD_CAP_DAILY` must also be set or the guard refuses everywhere (`eval-guard.ts`,
  deliberately no out-of-production default). It is **campaign-local**: a shell variable for a
  local CLI run against a disposable branch, never a Vercel environment row, and it does not
  touch production's shared $10 backstop.
- **D5 arithmetic, verified.** AGENTS.md holds 35 log entries totalling ≈118.6k characters in
  a ≈159.4k-character file. A 7-day inline window keeps the 2026-08-31 → 2026-09-04 entries
  (≈27k characters), so the compacted file lands at **≈68k characters** — comfortably under
  the 150k ceiling with room for this window's own entries.
- **D9.** "Astra via openai key" reads literally as an OpenAI-hosted model, while the roster
  (§3) defines Astra as the independent second frontier model. Step 07 confirms the concrete
  model/provider with the operator before authoring, and records it in the exposure ledger.

---

## 3. Model, effort, and mode rules

| Work | Model | Effort | Mode | Why |
|---|---|---|---|---|
| Architecture plans with ruling interactions (WS-2, WS-3.0/3.1) | Fable | xhigh | Plan mode; write the plan docs, no code | The handoff's own guidance was Opus-medium; the reader findings show ruling interactions (4, 5, 13, 21) and eval-identity coupling that justify the top model. |
| Adversarial audits of merged/mergeable diffs | Fable | xhigh–max | Ultracode (parallel finders + refuters) | Precedent: the 2026-08-18 fable-final-audit sessions. |
| Independent second-opinion audit | Astra | xhigh | Plain session, read-only | Model diversity catches failure modes redundancy cannot. Substitute Fable if unavailable. |
| Implementation PRs with tests | Opus | high | Plain session in its lane worktree; 15-minute plan-mode preamble when the prompt says so | Solid coding; the plan doc bounds scope. |
| Inventory-driven docs, mechanical moves with a verification script | Sonnet | medium | Plain session | Cheap; every mechanical step carries a byte-identity or grep check. |
| Anything touching production data, envs, spend | human | — | — | Operator only. |

Rules that apply to every session (restated in COMMON): read AGENTS.md standing rulings and
operating protocol first; PROGRESS.md plan block at start; adversarial self-review; tests
green before every commit; plain commit messages; closing report; never read the forbid
list; never create a worktree from a remote/Cowork mount; never deploy.

**Fable fallback.** The 2026-08-22 decision-log entry records two Fable reviewers terminating
on model-side errors with Opus running under operator override. Every Fable step prints its
model/effort at the top of its report; if Fable is unavailable, Opus at xhigh runs the same
prompt and the report says "model gate: Opus fallback". Prioritize Fable for steps 05 and 26
if capacity is short.

---

## 4. Lanes, worktrees, migration order

Create worktrees **natively on the Mac** (never from a Cowork/remote mount — see §8), from a
fresh `origin/main`. One worktree per step that runs concurrently — two sessions can never
share a checkout:

```
cd /Users/go/code/bnow-net && git fetch origin && git pull --ff-only && git push origin main
# (push first: worktrees are cut from origin/main and must see the current program docs)
for wt in ws2-routing ws2-provider ws2-remap ws3-conflict ws3-gazetteer ws1-injection ws1-adjudication ws4-ops audit-ws2 audit-ws3 ws7-tradecraft ws7-docs; do
  [ -d ../bnow-net-worktrees/48h-$wt-20260905 ] || \
    git worktree add ../bnow-net-worktrees/48h-$wt-20260905 -b 48h/$wt-20260905 origin/main
done
git worktree list   # must show no /sessions/ paths
for wt in ws2-routing ws2-provider ws2-remap ws3-conflict ws3-gazetteer ws1-injection ws1-adjudication ws4-ops audit-ws2 audit-ws3 ws7-tradecraft ws7-docs; do
  [ -d ../bnow-net-worktrees/48h-$wt-20260905/node_modules ] || \
    cp -Rc node_modules ../bnow-net-worktrees/48h-$wt-20260905/node_modules
done   # APFS clone of the main checkout's node_modules: near-instant, no extra disk until
       # modified; identical lockfile at every worktree. A PR that changes dependencies
       # re-runs `npm install` in its own worktree only.
```
(`gov` and `gov2` already exist from steps 01–03. `core.hooksPath` is repository config, so
the pre-push gate applies in every worktree without re-running `git config`.)

| Worktree | Steps (in time order; never two at once) | Model |
|---|---|---|
| `48h-gov` | 01 → 15 → 25 | Sonnet |
| `48h-gov2` | 02 → 03 (one session) | Sonnet |
| `48h-ws2-routing` | 04 (plan) → 11 → 20 → 23 (routing half) | Fable / Opus |
| `48h-ws2-provider` | 09 → 12 | Opus |
| `48h-ws2-remap` | 22 | Opus |
| `48h-ws3-conflict` | 05 (plan) → 13 → 19 → 23 (conflict half) | Fable / Opus |
| `48h-ws3-gazetteer` | 06 → 14 → 24 | Opus |
| `48h-ws1-injection` | 07 | Opus (or Astra) |
| `48h-ws1-adjudication` | 08 | Sonnet |
| `48h-ws4-ops` | 16 → 21 | Opus |
| `48h-audit-ws2` | 17 → 26 (read-only, reset to the audited SHA each time) | Fable |
| `48h-audit-ws3` | 18 (read-only) | Astra |
| `48h-ws7-tradecraft` | 29 (plan) → 32 → 34 | Opus |
| `48h-ws7-docs` | 30 → 31 (one session) → 33 | Sonnet / Opus |
| — | 10, 27 (operator, `main` checkout / release clone) | human |

Each step that opens a PR does so from a step branch cut from the worktree branch, named with a
DASH, never a slash: `48h/<worktree>-20260905-<step-slug>` (e.g.
`48h/gov2-20260905-step02-port-hygiene`). Git refuses `48h/gov-20260905/<slug>` because the
worktree branch `48h/gov-20260905` already occupies that ref prefix — step 01 hit this on
2026-09-06. A lane can hold several small PRs in flight. Viewed from a Cowork/remote mount,
`git worktree list` shows the native worktrees as "prunable" — that is a mount artifact;
never run `git worktree prune` from a remote session.

**AGENTS.md write-lock.** Only steps 01, 02, 03 (the governance PRs, merged first and in
order), 15, 25 and the operator edit `AGENTS.md` during the window, with one carve-out: a step may correct in place the single standing line its PR
makes wrong (a directory-map entry, the `drizzle/` range) — nothing else. Every other
proposed standing-text correction and every proposed decision-log entry goes in the step's
closing report ("Proposed AGENTS.md changes" block); step 25 applies them in one pass. This
is what lets twelve worktrees run without a serial AGENTS.md conflict. `docs/OPEN-TASKS.md`
may be edited by any step, but only the status lines of the tasks that step touches.
**`docs/PROGRESS.md`:** every session appends its own plan block; on a rebase conflict keep
BOTH sides verbatim in timestamp order (the 2026-08-20 precedent).

**Migration numbering (ruling 5; `9999_claim_source_trigger.sql` stays last).** Parallel
lanes would otherwise collide on `drizzle/meta/_journal.json`. Target order and numbers —
each migration PR rebases onto `main` and regenerates before merge, in this order:

| # | Migration | Step |
|---|---|---|
| 0028 | `benchmark_report_editions` + `benchmark_series_days` (Option 3) | 13 |
| 0029 | `conflict_validation_observations` | 13 |
| 0030 | `runtime_logs` (log-drain receiver) | 16 |
| 0031 | `provider_usage.model` column — only if R1 chooses the column | 11 |
| later | #85 fence column | WS-4.3, deferred |

---

## 5. The 48-hour schedule

Hours are from H0 (operator kickoff). "Agent h" is agent time; operator review is extra.
Full prompts exist for 01–16 and 22 (and the side session 28); 17–21 and 23–26 are SKETCH
prompts rewritten at the checkpoint named.

### Wave 0 — H0 (operator, ≈45 min)

- Answer §2 H0 items. Create worktrees (§4). Merge PR #47. Answer D1 for PR #48 (step 10
  executes the removal/merge).
- Confirm Neon: `npx tsx scripts/neon-branch.ts create` (prints `{branchId, connectionString}`;
  the branch is auto-named `itest-<ts>`), then `npx tsx scripts/neon-branch.ts delete <branchId>`
  — or `npm run test:integration -- src/integration/migrations.itest.ts`. Record the result
  for step 01. Check the Vercel dashboard once for log-drain support (O1).
- `vercel whoami` in the release clone (the CLI session is the deploy credential).
- Paste Wave 1 prompts.

### Wave 1 — H0 → H8

| Step | Title | Worktree | Model / effort / mode | Agent h | Depends | Decision |
|---|---|---|---|---|---|---|
| 01 | Reconcile standing docs with `main` (AGENTS.md snapshot, PR #46 hash, step-1A entry, credentials, stale OPEN-TASKS headers, directory map, decision-entry drafts) | gov | Sonnet / medium / plain | 1.5 | — | D11 |
| 02 | Port hygiene (WSL2→macOS): dead script, stale WSL2 text, worktree convention, Node pin | gov2 | Sonnet / medium / plain | 1.5 | — | — |
| 03 | Release checklist (#39/#78/#84) + #79 drain runbook — same session as 02, second PR | gov2 | Sonnet / medium / plain | 1.5 | 02 | — |
| 04 | PLAN-WS-2 routing matrix (all options; PR-by-PR) | ws2-routing | Fable / xhigh / plan mode | 3 | — | D2, R5 |
| 05 | WS-3.0 decision memo + PLAN-WS-3 | ws3-conflict | Fable / xhigh / plan mode | 3 | — | D3, D4 |
| 06 | Iran/Levant gazetteer `iran-levant-v1` + `insufficient_data` diagnostic (WS-3.4a) | ws3-gazetteer | Opus / high / plain | 3 | — | — |
| 07 | Exposure ledger + six development-split injection cases + offline proof (WS-1.1) | ws1-injection | Opus (or Astra) / high / fresh non-exposed session | 4 | — | D9, E1 (explicit yes required before the PR can merge), E3 |
| 08 | val-typ-005 adjudication memo (WS-1.2) | ws1-adjudication | Sonnet / medium / plain | 0.5 | — | E4 |
| 09 | Anthropic seam hardening (#97a + key-alone activation bypass) | ws2-provider | Opus / high / plain | 1.5 | — | — |

**Checkpoint 1 (≈H8).** Read 04/05 plans and the memo; answer CP1 decisions; merge 01, 02, 03,
06, 08, 09 (in that order — 01, 02 and 03 all touch CLAUDE.md/AGENTS.md). 06 and 09 merge
before any Fable audit because both are inert (an unwired gazetteer; a dormant provider that
now refuses) — steps 17/18 audit them post-merge and 26 re-checks. For 07's dataset PR run
the two mechanical checks yourself before merging: `bash scripts/evals/corpus-v2/check-regen.sh`
and read the new `EVAL-EXPOSURE-LEDGER.md` entry (commands run, "heldout ids seen: none",
and the note that `check-regen.sh` EXECUTED `build-draft.py`). **Merge (or cherry-pick onto
`main`) the 04/05 plan documents and the step-05 memo BEFORE pasting Wave 2** — steps 12, 14,
22 and 24 run in other worktrees cut from `main` and cannot see a worktree branch. Paste Wave 2
prompts after rewriting 11–14 from the plans.

### Wave 2 — H8 → H22

| Step | Title | Worktree | Model / effort / mode | Agent h | Depends | Decision |
|---|---|---|---|---|---|---|
| 10 | Operator runs: #79 drain (O2), WS-1.1 ×3 capture run (D6), decisions recorded | `main` | — | 1 | 03, 07 | O2, D6 |
| 11 | WS-2.1 Ask parity per PLAN-WS-2 (attribution report, model-aware embed pricing, Auto gate, #44/#84 hygiene) | ws2-routing | Opus / high / plan-mode preamble | 4 | 04 | R1, R3 |
| 11b | WS-2.1 PR-2.1-3 `hasScorecard()` gate on the Auto money path (follow-up: R3 signed after step 11 ran) | ws2-routing | Opus / high / plan-mode preamble | 1.5 | CP2 (#57, #59) | — |
| 12 | WS-2.2 core: provider dimension + registry key + offline-identity decoupling (no version bump) | ws2-provider | Opus / high / plan-mode preamble | 5 | 04, 09 | D2, R2 |
| 13 | WS-3.1 persistence: migrations 0028/0029 (Option 3) + fork itests | ws3-conflict | Opus / high / plan-mode preamble | 5 | 05 | D4 (C1, C6) |
| 13b | WS-3.1 conflict-observation migration (PLAN-WS-3 §3.1b; drafted as "0029", lands as 0030 after step 16's 0029) — follow-up: D4 signed after step 13 ran | ws3-conflict | Opus / high / plan-mode preamble | 2.5 | CP2b (#63, #64) | — (D4 signed) |
| 14 | WS-3.2 series/edition-aware discovery + report-only cron entrypoint (unscheduled) | ws3-gazetteer | Opus / high / plain | 4 | 05, 13 schema agreed | D4 (C4, C5) |
| 15 | AGENTS.md compaction into `docs/DECISIONS.md` (#92) with byte-identity check | gov | Sonnet / medium / plain (Opus if the operator prefers; the check script is the proof either way) | 1.5 | 01 merged; 10 done (entries signed); no other AGENTS.md-touching PR open | D5 |
| 16 | Log-drain design + Neon receiver (`runtime_logs`, HMAC, retention sweep) | ws4-ops | Opus / high / plain | 4 | — | O1 |
| 29 | PLAN-WS-7 tradecraft legibility (ICD 203/206, ICS 206-01) — added 2026-09-06 from the WS-7 addendum; WS-7.5 does NOT ride step 13 | ws7-tradecraft | Opus / high / plan mode | 3 | — | T1–T3 listed; T3 tables drafted |
| 30 | WS-7.1 tradecraft crosswalk doc + public `/methodology` page | ws7-docs | Sonnet / medium / plain | 2 | — | T1 |
| 31 | WS-7.6 retention/preservation policy + no-delete source scan (same session as 30) | ws7-docs | Sonnet / medium / plain | 1 | 30 | — |
| 22 | #33 remap runbook + `--estimate` dry run against a local `next start` on a Neon fork (measured run only if D7) | ws2-remap | Opus / high / plain | 3 | 02 merged (reworded driver header); R4 for the measurement path | R4, D7 |

**Checkpoint 2 (2026-09-06 evening, after Wave 2).** The queue and the fence are in
`docs/prompts/2026-09-06-48h-CP2-merge.md`: #58, #57, #59, #60, #61 (retarget to `main`
after #60), #63, step 16's two PRs (0030), step 22's PR, #66 — attended session, merge only.
12 merges before 17's audit (the audit reads `main`). CP2 landed 7 of 10 (`b0c5cf9`); #60/#61
(a `pricing.ts` add/add) and #64 (journal + a snapshot chained from 0027) are landed by
`docs/prompts/2026-09-06-48h-CP2b-rebase-merge.md`. Then Wave 3: 14 (needs #63), 13b (needs #64),
11b (needs #57/#59), 30→31 (need #66), 10 (operator; needs #58), 15 after 10, 17 (Fable)
and 18 (Astra) once 11b is in, 32/33/34 per T2/T4/T3. CP2 decisions: O1 (rest), T3, T4, T5
now; D7/R4/O3 when the operator chooses.

### Wave 3 — H22 → H34

| Step | Title | Worktree | Model / effort / mode | Agent h | Depends |
|---|---|---|---|---|---|
| 17 | Adversarial audit of the WS-2 + WS-4 code on `main` at CP2b (#52, #57, #59, #60, #61, #62, #64, #65; 11b if open) — full prompt | audit-ws2 | Fable / xhigh / ultracode | 4 | CP2b |
| 18 | Independent audit of the WS-3 diffs (#56, #63, 13b, 14) — full prompt | audit-ws3 | Astra / xhigh / read-only | 3 | CP3 (13b, 14) |
| 19 | WS-3.3 evidence population (DB-backed claim sources, live observation pipeline, inert) — SKETCH | ws3-conflict | Opus / high | 6 | 13, 14 |
| 20 | WS-2.4 eval-plane parity (`--provider`, identity, entity-audit prompt extraction) + 20b option B/C PRs if D2 says so — SKETCH | ws2-routing | Opus / high | 5 | 12 |
| 21 | Reliability proofs on a Neon fork (#102 shed/refusal, #103 watchdog) — SKETCH | ws4-ops | Opus / high | 4 | — |
| 32 | WS-7.2 ICS 206-01 citation mode on "Copy for report" + AI-tool disclosure block — SKETCH | ws7-tradecraft | Opus / high | 5 | 29, 30 merged; T2 |
| 33 | WS-7.3 templated source descriptors + per-digest source summary (render-time) — SKETCH | ws7-docs | Opus / high | 5 | 29, 30 merged |

**Checkpoint 3 (≈H34).** Read finding registers from 17/18; decide remediations; merge 19–21
and 32–33 as they clear (step 26's final audit covers the WS-7 PRs: ruling 1 sentinel tests,
ruling 3 stub refusal, ruling 19 labels); sign T3; rewrite 23–25 and 34.

### Wave 4 — H34 → H44

| Step | Title | Worktree | Model / effort / mode | Agent h | Depends |
|---|---|---|---|---|---|
| 23 | Remediate audit findings (17, 18) — SKETCH | ws2-routing / ws3-conflict | Opus / high | 4 | 17, 18 |
| 24 | WS-3.5 conflict scoreboard view on real observations + shadow-soak enablement prep (no flag-on) — SKETCH | ws3-gazetteer | Opus / high | 5 | 19 |
| 25 | Docs sync: OPEN-TASKS statuses, CURRENT-STATE, AGENTS snapshot, successor-plan cross-refs, program log — SKETCH | gov | Sonnet / medium | 1.5 | all merged |
| 34 | WS-7.4 ICD 203 likelihood band + corroboration-derived confidence as a versioned presentation mapping — SKETCH | ws7-tradecraft | Opus / high | 4 | 29, 32 merged; **T3 signed** |

**Checkpoint 4 (≈H44).** Merge 23–25; freeze the window's `main`; paste 26.

### Wave 5 — H44 → H48

| Step | Title | Worktree | Model / effort / mode | Agent h | Depends |
|---|---|---|---|---|---|
| 26 | Final adversarial audit of everything merged in the window; per-PR deploy go/no-go; next-48h handoff — SKETCH | audit-ws2 | Fable / max / ultracode | 3 | 23–25 |
| 27 | Operator: deploy per `docs/RELEASE-CHECKLIST.md` only PRs with go + no new cap env; observation window; decision-log entries | release clone | — | 1 | 26 |

Total agent time ≈ 80 h across twelve worktrees (plus ≈3 h for side session 28); wall clock
fits 48 h with 3–5 sessions in flight and one reviewer.

---

## 6. Checkpoint protocol (what the operator does each time)

1. Read every closing report produced since the last checkpoint (`docs/reviews/*-2026-09-0*.md`
   named in the step prompts). Each report ends with a **"Handoff"** section: what changed,
   test numbers, decisions needed, and what the next prompt must know.
2. Merge PRs in the order the wave says. Migration PRs: rebase, `npm run db:generate` check,
   then merge. Never merge a PR whose report shows red tests or missing fork-itest numbers.
3. Sign decisions: step 01 drafts decision-log entries in `docs/reviews/DECISION-ENTRIES-DRAFT-2026-09-05.md`;
   copy the signed ones to AGENTS.md's decision log (append at end of file until step 15
   lands, then under `## Decision log`). Spend / production-write / dataset-write items are
   never implied: an unsigned draft means "not authorized".
4. Rewrite the next wave's SKETCH prompts from the reports (the sketch says which report).
5. Append a program-log line to §10 (time, merged, decided, rewritten, blocked).

---

## 7. Closing-report contract (every step)

`docs/reviews/<STEP-SLUG>-2026-09-0X.md`, sections in this order: **Scope** (prompt file,
branch, base commit) · **Built** (files, PRs with titles) · **Tests** (unit count before/after,
typecheck, lint, fork itests with the branch name, `$0` / spend line) · **Rulings touched and
how each is satisfied** · **Citations re-verified** (file:line the step relied on, with any
correction) · **Decisions needed** (IDs from §2 or new) · **Debt and risks** · **Handoff**
(what the next step must know; which prompt to rewrite and how) · **Proposed AGENTS.md
changes** (standing-text corrections and the decision-log entry, for step 25 to apply — the
write-lock in §4). Plus a PROGRESS.md execution block under the session's plan block.

---

## 8. Linux-isms / WSL2 → macOS: findings and disposition

Scanned: `scripts/**`, `.githooks`, `.github`, `package.json`, vitest/drizzle/next configs,
`src/integration/**`, every `child_process` site, `CLAUDE.md`, `AGENTS.md`, `README.md`, all
`*.sh`. The runtime and test toolchain are portable today (§1.14). Debt, all handled by step 02:

- `scripts/llm-redigest.sh` — hard-codes `/home/go/code/bnow.net`, uses GNU `date -d`;
  dead (nothing references it). **Delete** under the CLAUDE.md scoped delete exception.
- `scripts/pin-dns.cjs` — WSL2 NAT-resolver workaround; harmless on macOS (falls back) but
  unneeded and mildly undesirable (bypasses the system/VPN resolver, drops IPv6). Keep the
  file for now, rewrite its header, and remove the "this box needs the DNS pin" instruction
  from `CLAUDE.md:36-38`, `CURRENT-STATE.md:630-636`, and the living runbooks
  (`OPENSANCTIONS-RESCORE-RUNBOOK.md:93-97`, `X-GAP-RECOVERY-RUNBOOK-2026-07-13.md:83,187,216`,
  `scripts/opensanctions-rescore.ts:35-36`, `scripts/ab-mapreduce.ts:13`). Archives
  (PROGRESS, DECISIONS, dated reviews, AGENTS decision log) stay verbatim.
- `/home/go` paths in live text: `scripts/telegram-login.ts:154` (error message),
  `docs/NEXT-SESSION-PROMPT.md:3` (relic — delete the file), `docs/BLOCKERS.md:7`,
  `docs/SETUP-NEXT-WEEK.md:170` (`cd ~/code/bnow.net`).
- GNU `date -d` in a living runbook: `docs/SETUP-NEXT-WEEK.md:188-189` → `date -u -v-1d +%F`.
- Code comments asserting "this box cannot reach api.openai.com" (`scripts/map-remap.ts:3-6`,
  `scripts/map-backfill.ts:4-6`, `scripts/telegram-getme.ts:14`, `scripts/isw-refresh.ts:40-42`
  unpooled-credential note) — reword; they route WS-0.3/WS-2.3 decisions.
- OPEN-TASKS #74 (dev-mode hydration dead on WSL2) — re-test `npm run dev` on the Mac; close
  if it hydrates.
- No Node version pin (`engines` / `.nvmrc`): CI runs Node 22, the Mac runs 24.19. Add
  `engines.node >=22` and a `.nvmrc` of 22 (decision-free; note in the PR).
- `data/` cache did not travel with git: `isw-refresh.ts` refetches politely (≈2.1 s/host);
  the #79 drain works but needs network.
- **New convention** (handoff §4.6; nowhere in the repo yet): worktrees are created only from a
  native session on the Mac — a Cowork/remote-mount worktree writes `/sessions/…` gitdir paths
  git cannot resolve later. Step 02 adds it to `CLAUDE.md` § Commands & setup and AGENTS.md
  § Conventions, together with the plain-release-clone deploy rule.

---

## 9. Deliberately not in this window

- Label-gated evaluation steps 2–4 (WS-1.3–1.5); heldout admission; any candidate run.
- Conflict shadow soak flag-on and cutover (WS-3.6/3.7) — this window prepares them.
- Multi-theater tagging N:M (#37, WS-3.8); #85 fence migration (WS-4.3); ASK_SESSIONS #97
  residuals; Ask on Anthropic (a second `GenerationProvider`, ≈8 h) unless D2 = B and time
  remains after step 20.
- WS-0.2 reviewer instrument (workbooks, guide, fold-back script): its inputs live outside the
  repo in the blinded packet folder, which also holds forbidden files; it needs a native Mac
  session with the operator present to hand over ONLY the three blinded files. Run it as a
  side session whenever the operator is free (prompt: `…-48h-28-reviewer-instrument.md`, not
  on the critical path).
- Any deploy before step 26's go/no-go.

---

## 10. Program log (append-only; operator writes)

- 2026-09-08 16:27 – 21:53 ET — **Stage 3 row A + row B(18) + 34 launched, delivered and landed
  (CP5); the queue is empty.** Launched through `scripts/launch/launch.sh` with its claim ledger.
  **Launch incident, fixed:** the first four unattended launches (20, 21, 32, 33 at 20:27Z) ran
  without a permission mode (`CLAUDE_LAUNCH_OPTS` unset), so each did a read-only pass and exited
  with no PR; their logs were kept as `logs/step<n>.readonly-20260908.log`, the claims released
  with a ledger note, and all four relaunched with `--dangerously-skip-permissions` (COMMON
  §4.10); each relaunch prompt got a handoff pointer to its read-only log. `launch.sh` now
  refuses an unattended `--go` without a permission mode. **Delivered:** 18 → #78 (register: 21
  findings, 2 major, 0 blocker, ran on Fable — disclosed; step 26 must use a different model),
  21 → #79, 32 → #80, 33 → #77, 20 → #75 #76 #81 #82 #83, 34 → #84. **Merged in order:** #78
  `c32213a` · #79 `95d6aa7` · #80 `48a9d4e` · #77 `c2e384d` · #84 `f8c9a96` · #76 `c6f0a6c` ·
  #75 `9f11619` · #81 `be591a1` · **#85 `4eccb06`** (see below) · test fix `25bdd27`.
  **Conflicts:** every GitHub CONFLICTING flag was the `merge=union` artifact on `PROGRESS.md` /
  `OPEN-TASKS.md`, resolved by rebasing in each PR's own worktree, except two real overlaps
  resolved by hand with both sides kept: #77 × #80 on the digest page's SELECT list and row
  type (+ #80's two new tests needed #77's fifth mock result), and #84 × #77 on
  `METHODOLOGY-TRADECRAFT.md` (two ICD 203 rows, the "what is missing" paragraph merged to name
  all three shipped items) and `page.test.tsx` (#84's narrowed assertion + its new test). Nothing
  in `drizzle/` or `src/db/schema.ts` moved; `ls drizzle | tail -4` unchanged.
  **Stack-merge incident, corrected:** #82 and #83 were stack PRs whose GitHub base was the
  previous branch, not `main`; `gh pr merge` landed them into `anthropic-wiring` /
  `anthropic-price` and `main` never received them. Re-landed as **#85** (two cherry-picks,
  `b27e2d2` price rows + `c091f6a` eval seam); the three stale stack branches deleted. **Rule
  for every future stack: `gh pr edit <n> --base main` before merging each PR above the
  bottom one.** **Gate on `main` `25bdd27`:** typecheck clean · lint 0 errors, 3 pre-existing
  warnings · unit **4,082 / 270 → 4,332 / 282** · `npm run build` PASS (blanked keys, dummy
  `DATABASE_URL`). One gate failure between #85 and `25bdd27`: `hardening-cli.test.ts`'s
  subprocess env blanked four secrets but not `ANTHROPIC_API_KEY`, so on a checkout whose
  `.env.local` holds a real key the `--provider anthropic` "key not set" refusal was unreachable
  (dotenv refills an ABSENT name — #112 class); green in every worktree only because none had
  the key. Fixed by blanking it (`25bdd27`). Carry to step 26: the gate's verdict on subprocess
  tests depends on the runner's `.env.local`; run it once on the full-secret checkout.
  **(f13) gate verified on `main`:** `mapreduceProviderTag()` is provider-aware
  (`mapreduceTagFrom(dispatchedIdentity("map"), dispatchedIdentity("reduce"))`), the second model
  authority `anthropicModel()` is gone, and `pricing.ts` carries `claude-haiku-4-5-20251001`
  $1/$5 and `claude-sonnet-5` $2/$10 with `provider: "anthropic"`. The Anthropic digest path is
  on `main` **dormant** (provider allowlist widened for `digest` only; nothing enabled).
  **New OPEN-TASKS from this batch:** #117 (digest carries no record of which extractor
  versions), #118 (`digest-persist.ts` literal NUL hazard). This session merged only: launched
  the six steps through the launcher, created no worktree, deployed nothing, touched no
  database, spent $0. **Stage 3 remaining:** 19 (launchable now), 23 (gated on your
  accept/defer/fix marks over #74's 71 + #78's 21 findings), 24 (after 19), 25 (after C).

- 2026-09-07 12:45–13:20 ET — **CP4 merge queue landed; the queue is empty.** Merged in order,
  each pulled before the next: **#74** WS-2 + WS-4 audit finding register (`0924ed9`), **#70**
  series-aware edition discovery (`72da938`), **#71** conflict-validate route (`5f30cd3`).
  **#70 was recorded by GitHub as a real merge, not auto-closed** — despite `515dbca` ⊆ `7566fd4`,
  merging it first gave it its own merge commit and kept the two step-14 reports separable.
  Before merging #74, one authorized edit on its branch (`8cf78ba`, (f4)): the register's "For
  step 27 (deploy)" bullet said "the only PR whose verdict is not merge-stands is #64"; it now
  names **#62, #64 and #67** as the fix-before-deploy set, keeping the existing #64 enablement
  sentence and adding one clause each for #62 (WS2-F04 missing manual-migration step + WS2-F02
  `JOIN` argument) and #67 (WS2-F07's vacuous exact-cache pin, the only evidence that a degraded
  provider is never cached) taken from the register's own verdict table.
  **Conflicts: none anywhere.** GitHub's DIRTY/UNKNOWN state was the `.gitattributes merge=union`
  artifact again — `git merge-tree <merge-base> origin/main origin/<branch>` emitted **zero**
  conflict markers for all three, checked locally before believing GitHub. The #70/#71 stack was
  rebased once from the top in `48h-ws3-gazetteer-20260905` (`git rebase --update-refs
  origin/main`, `a821695` → post-#74 `main`) and **completed with no conflict at all**: nothing was
  hand-resolved. `src/lib/usage/cron-run.ts` **auto-merged as predicted** — step 14 adds one
  `"conflict-validate": 300` line to `JOB_MAX_DURATION_SEC` while step 13b's optional `runId`
  callback argument sits at `:190`, disjoint hunks; `AGENTS.md` auto-merged (the `src/lib/isw/`
  directory-map line, additive, surviving the `3f09757` restructure); `docs/PROGRESS.md` was
  resolved by the union driver with no hand-editing. Nothing in `drizzle/` or `src/db/schema.ts`
  was touched by either PR. Both branches force-pushed with `--force-with-lease` (`9303047` /
  `9db3fcf`), #71 retargeted to `main` after #70 merged. Two procedural notes: GitHub fired **no**
  `pull_request` run for #71's rebased tip, so CI was triggered by a close/reopen of the PR (no
  code or branch change) and then ran green; and at 17:01Z, one minute before the #70 merge, the
  operator committed `11bcce0` (docs-only, the CP4 plan file) to the main checkout, diverging local
  `main` — resolved on the operator's instruction with `git pull --rebase` (zero file overlap with
  anything merged; content verified byte-identical, replayed as `2d74518`).
  **Gate on post-merge `main`:** typecheck clean · lint 0 errors, 3 pre-existing warnings · unit
  **4,015 / 267 files → 4,082 / 270 files** · `ls drizzle | tail -4` **unchanged**
  (`0029_runtime_logs.sql`, `0030_conflict_observations.sql`, `9999_claim_source_trigger.sql`,
  `meta` — neither #70 nor #71 adds a migration) · `git worktree list | grep -c mut` = **0** (step
  17's scratch worktrees are already gone; none was removed by this session).
  **Register gap G4 is CLOSED: `npm run build` PASSES.** Run twice with blanked provider keys,
  `LLM_DISABLE=1` and a dummy `DATABASE_URL` — as the baseline at `a512fc8` **before** any merge
  (so a failure would have been attributable to step 23, not to this queue) and again at post-merge
  `5f30cd3`. Both exit 0. This is the **first tree-wide build of the entire window**; #64's App
  Router route and #71's new `/api/cron/conflict-validate` both compile and register.
  **`docs/OPEN-TASKS.md` #110 filed** — WS2-F06, the `ASK_PIPELINE=legacy` rollback path that
  dispatches a paid completion with no SpendGuard, `[Tier 1 — spend safety]`, citing the register
  and (f2), pre-existing since `cea8cac` and owned by step 23 (A1's first half; its second half is
  the fix).
  **Four 2026-09-07 operator actions this log had not recorded, entered here for the record:**
  `a5caeb7` — the CP1-batch decision entries signed into `AGENTS.md`'s log (32 entries; section
  (c)'s O1 correctly skipped as superseded); `3f09757` — the **eighth archive pass**, i.e. step
  15's compaction, run directly on `main` rather than as a PR: `AGENTS.md` 189,651 → 98,038
  characters against the 150k ceiling, 29 pre-2026-08-31 entries moved verbatim to
  `docs/DECISIONS.md`, OPEN-TASKS #92 closed, `scripts/check-decision-log-move.sh` added and PASS
  (183 entries before, 183 after) — and **the end-of-file append convention is retired: new
  decision entries append at the end of `## Decision log`, in date order**; `901078a` — every open
  decision answered and drafted as (f1)–(f15), after which INDEX §2.1 reads "NOTHING IS WAITING ON
  AN OPERATOR ANSWER"; and **Stage 0** — `lsof` clean (both detached sessions confirmed gone),
  `git fetch --prune`, `gh pr list` read, `main` pushed, and **#51 closed** (its tip is an ancestor
  of `main`; the content landed at CP1 as `6eaef5d`). This session merged only: it launched
  nothing, created no worktree, deleted no branch, deployed nothing, touched no database, and spent
  $0. Open PRs after CP4: **#48 and #47 only** (Stage 2 operator work).
- 2026-09-06 21:50 ET — CP3 merge queue: five of the seven queued PRs landed on `main`, in order,
  each pulled before the next: **#73** migration 0030 conflict observations (`3e7cfdd`), **#67**
  Auto scorecard gate (`6b52c2c`), **#68** tradecraft crosswalk (`9c32fb7`), **#69** public
  `/methodology` (`a485c80`), **#72** retention policy + no-delete scan (`c101737`). After #73 the
  journal read `28 0028_lumpy_dragon_lord | 29 0029_runtime_logs | 30 0030_conflict_observations`
  and `ls drizzle | tail -4` ended `0029_runtime_logs.sql, 0030_conflict_observations.sql,
  9999_claim_source_trigger.sql, meta` — the step-1 check passed as written. Every conflict GitHub
  reported was the `.gitattributes` union artifact (GitHub does not honour `merge=union`, so a
  `docs/PROGRESS.md` tail reads DIRTY there while `git merge-tree` locally is clean): #67 and the
  ws7 stack were rebased in their own worktrees with `git rebase --update-refs origin/main` from the
  top of the stack, the union driver resolved PROGRESS.md with no hand-editing, gate re-run green
  (typecheck + unit) and force-pushed with `--force-with-lease` before CI and merge. The ws7 stack
  was rebased TWICE — once onto `main` with #73, then again after #67 landed and moved the tail —
  and #69/#72 were retargeted to `main` with `gh pr edit --base main` only after their parent had
  merged. #67 also touches `src/db/schema.ts`, but the hunk is a comment-only edit on
  `askUsage.provider` disjoint from #73's new table: it auto-merged and no code, `drizzle/` or
  schema conflict was resolved by this session. **#70 (step 14 discovery) and #71 (route, stacked on
  it) are NOT merged and are left open.** Both are DIRTY on GitHub for the same PROGRESS.md/AGENTS.md
  tails, so both need the rebase, and that rebase must happen in `48h-ws3-gazetteer-20260905`, where
  `lsof -a -d cwd -c claude` shows a live session (PID 55035, suspended, started 20:58 ET) —
  COMMON §4.11 forbids moving HEAD under it, so the queue stopped there rather than touching the
  worktree; nothing was checked out, reset or force-pushed on either branch. Step 17's finding
  register (item 8) has no open PR — audit-ws2 is still running — so it carries to CP4. Post-merge
  `main` `c101737`: typecheck clean, lint 0 errors (the same 3 pre-existing warnings), unit **4,015 /
  267 files** (from 3,914 / 263). Migrations on `main`: 0000–0030 + `9999_claim_source_trigger.sql`
  last. `git worktree list | grep -c mut` = **12** (step 17's mutation worktrees, left in place).
  No deploy, no env change, no DB, $0. This session merged only; it launched nothing and deleted no
  branch.
- 2026-09-06 21:35 ET — Wave 3 delivered: 11b (#67), 13b (#73, migration 0030, chain 0028→0029→0030
  verified), 14 (#70 discovery, #71 route), 30 (#68, #69), 31 (#72); 17 (Fable audit of `98294c5`)
  running in audit-ws2. Incident: the Wave 3 launch list was issued twice (CTO error — the post-CP2b
  message repeated 14/11b/30-31 as unstarted because nothing was on origin yet); ws3-gazetteer,
  ws2-routing and ws7-docs each ran a second session and took a `checkout -B` under a live one; no
  work lost (every session had pushed); the duplicates verified and stopped. Rule → COMMON §4.11.
  New decisions recorded: C5-m, C15, R14 (§2.1), R15 (§2.3). CP3 queue → `2026-09-06-48h-CP3-merge.md`.
- 2026-09-05 — program authored; awaiting H0 kickoff.
- 2026-09-06 20:50 ET — CP2b: the three PRs CP2 stopped on landed on `main`, in order, each pulled
  before the next: **#60** provider dimension (`fed1d03`), **#61** eval identity decouple
  (`ef0bba8`), **#64** log-drain receiver (`1e06112`). The `src/lib/llm/pricing.ts` add/add was
  resolved by keeping BOTH end-of-file blocks — `main`'s #59 embed block (`embedPriced`,
  `embedPricePerMtok`, `estimateEmbedCostUsd`) byte-unchanged and closed with its own brace, then
  the branch's `pricedFor` closed with its own — with the branch's `ModelPrice`/`PriceTable` types
  and the `PRICES_PER_MTOK: PriceTable` retyping kept; no other hunk in the file conflicted, and
  `--update-refs` moved both ws2-provider tips together. #64's migration was REGENERATED, not
  renumbered: the branch's `0030_snapshot.json` had `prevId` = the **0027** snapshot (the branch was
  cut before 0028 existed), so the generated files were dropped and `drizzle-kit generate` re-emitted
  them on top of 0028 — **0030 regenerated as 0029, SQL identical** to the branch's original
  (`diff` clean), journal tail `28 0028_lumpy_dragon_lord | 29 0029_runtime_logs`, **chain
  0028→0029** (`0029_snapshot.prevId` == `0028_snapshot.id`); the itest, report, OPEN-TASKS and PR
  title were renumbered in one commit and **step 13b takes 0030** (D10 amended). Post-merge `main`
  `1e06112`: typecheck clean, lint 0 errors (3 pre-existing warnings), unit **3,914 / 263 files**
  (from 3,808/258), `runtime-logs.itest.ts` **15/15** on disposable fork `br-morning-bar-atomf9hb`
  (created and deleted by the harness; `applying 0028_…` then `applying 0029_runtime_logs.sql`).
  Migrations on `main`: 0000–0029 + `9999_claim_source_trigger.sql` last. No deploy, no env change,
  $0. This session merged only; it launched nothing.
- 2026-09-06 19:45 ET — CP2 merge queue: seven of ten queued PRs landed on `main`, in order, each
  pulled before the next: #58 injection cases + exposure ledger (`8b6d9d7`), #57 Ask attribution
  report (`8ac41d2`), #59 embed pricing (`8a00ea2`), #63 migration 0028 (`c728b2b`), #62 log-drain
  design (`c75bd99`), #65 remap runbook + `--base-ack` (`7f267bd`), #66 PLAN-WS-7 (`2a51f91`).
  #63, #62, #65 and #66 each needed a rebase onto the moving `main` in their own worktree
  (`git rebase --update-refs origin/main`, force-pushed, CI re-run green before merge); every
  conflict there was `docs/PROGRESS.md` / `docs/OPEN-TASKS.md` resolved by the union driver keeping
  both blocks — no code, migration or schema conflict was resolved by this session. THREE PRs are
  NOT merged and are left open for the operator: **#60** (provider dimension) stops on a genuine
  code conflict in `src/lib/llm/pricing.ts` — #59's embed-pricing block and #60's `pricedFor` are an
  add/add at end-of-file whose union also needs a closing brace, which the CP2 prompt forbids
  guessing; **#61** is stacked on #60 and cannot be retargeted until it merges (its base is still
  #60's branch, untouched); **#64** (log-drain receiver, migration 0030) was retargeted to `main`
  per the queue but stops on a `drizzle/meta/_journal.json` conflict — `main` now holds idx 28
  (`0028_lumpy_dragon_lord`) where the branch writes idx 30 (`0030_runtime_logs`), a `drizzle/`
  conflict the prompt also reserves to the operator (D10: renumber to 0029, or keep 0030 and let
  step 13b take the next free number). Both aborted rebases left their worktrees clean on their
  original branches with no ref moved. Post-merge `main` `2a51f91`: typecheck clean, lint 0 errors
  (3 pre-existing warnings), unit 3,723 → 3,808 (255 → 258 files). Migrations on `main`: 0000–0028
  + `9999_claim_source_trigger.sql` last — 0030 is NOT on `main` (it rides #64). This session
  merged only; it launched nothing.
- 2026-09-06 18:30 ET — CP1 merge queue landed on `main`, in order, each pushed and pulled before
  the next: #50 port hygiene (`b62538d`), #51 release checklist (`6eaef5d`), #52 anthropic seam
  hardening (`d74d588`), #53 val-typ-005 memo (`544b475`), #54 PLAN-WS-2 (`ba968ce`), #55 WS-3.0
  memo + PLAN-WS-3 (`b5ca557`), #56 Iran/Levant gazetteer (`2203150`). #52 was rebased onto
  post-#51 `main` first: only `SETUP-NEXT-WEEK.md` conflicted and #52's version was kept per the
  16:30 entry; `BLOCKERS.md`, `HUMAN-SETUP-TODO.md` and `CURRENT-STATE.md` auto-merged to #52's
  corrected text with no #49 residue, and the rebased branch was force-pushed so origin matches
  what merged. Pre-push gate green on every push; unit 3,612 → 3,723 (255 files). Operator answers
  to T1–T3 recorded in §2 (`d18e961`). Wave 2 launched unattended (Opus / high / `-p`, detached,
  logs in `bnow-net-worktrees/logs/step<NN>.log`): step 12 `ws2-provider`, 11 `ws2-routing`, 22
  `ws2-remap`, 13 `ws3-conflict`, 16 `ws4-ops` — all five on their LANE branch, fast-forwarded to
  `2203150`, lockfile churn reverted, and `.env.local` copied into `ws2-remap` / `ws3-conflict` /
  `ws4-ops`. Steps 14 and 15 deliberately not started (14 waits on 13's schema, 15 on step 10).
  Step 07 has still pushed no branch — it is carried to the next pass. No CP1 decision was
  answered or inferred by this pass.
- 2026-09-06 18:00 ET — WS-7 (tradecraft legibility) admitted as a parallel docs/render lane from
  `docs/prompts/2026-09-06-ws7-tradecraft-legibility-addendum.md`: steps 29–34, worktrees
  `ws7-tradecraft` and `ws7-docs`, decisions T1–T3. Program ruling: WS-7.5's reserved columns do
  NOT ride step 13's migration (own additive migration later); the addendum's §3 sentences for
  WS-3.0/WS-3.1 are not applied. No WS-7 step touches prompts, datasets, registry, env, or scorer.
- 2026-09-06 17:30 ET — step 06 done (PR #56: versioned gazetteers, `iran-levant-v1` 106
  toponyms / 164 variants, rung-level `insufficient_data`; unit 3,612 → 3,704; rung-only per
  E5; two self-found fail-open holes fixed — carried to step 18's audit). Step 07 in progress
  (slow model quota; ETA ≈1 h). CP1 merge queue now ends … → #55 → #56.
- 2026-09-06 17:15 ET — steps 04 (PR #54) and 05 (PR #55) done; #50/#51 rebased onto #49 by an
  agent session in `gov2`. CP1 merge queue: #50 → #51 → #52 (rebase) → #53 → #54 → #55 → 06.
  Added `.gitattributes` (`merge=union` for PROGRESS.md/BLOCKERS.md) and COMMON §2.5 (plan
  Handoff rewrites are part of the step prompt).
- 2026-09-06 16:30 ET — step 09 done (PR #52; also corrected the Anthropic-fallback claim in
  SETUP-NEXT-WEEK/BLOCKERS/HUMAN-SETUP-TODO/CURRENT-STATE — overlaps #49's §1 edit: rebase #52
  after #49, keep #52's version). Step 08 done (PR #53; it ran in the `ws1-injection`
  worktree by mistake — harmless, branch correct; reset that worktree before step 07). Step 05
  plan approved as written; step 06 told to land the rung-level diagnostic only (E5 added).
  COMMON §4.8 corrected: the Anthropic path did honour `LLM_DISABLE`.
- 2026-09-06 (later) — worktrees renamed from numbered to descriptive (`ws2-routing`,
  `ws2-provider`, `ws2-remap`, `ws3-conflict`, `ws3-gazetteer`, `ws1-injection`,
  `ws1-adjudication`, `ws4-ops`, `audit-ws2`, `audit-ws3`; `gov`/`gov2` kept — their PRs are
  open); step prompts now carry a single `Worktree` row and no lane letters.
- 2026-09-06 14:10 ET — operator answered the H0 decisions in §2 (D2 = B: Anthropic in scope,
  key added to `.env.local`; C deferred beyond this round; D5 adds a ≤150k-character ceiling
  for AGENTS.md; D6 $0.50–$2.00; D8 all keys work, `VERCEL_TOKEN` valid and project-scoped;
  R5 = branch `claude/local-model-ask-eval-20260817`; O1 Vercel supports drains). Steps 01,
  02, 03 done as PRs #49, #50, #51 (merge in that order at CP1). Step branches use the dash
  form. Because an Anthropic key now exists locally and the dormant provider has no guard,
  step 09 runs FIRST in Wave 1 and every local run blanks `ANTHROPIC_API_KEY` until it merges.
