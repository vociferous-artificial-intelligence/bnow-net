# Step 02 — Port hygiene: WSL2 → macOS (Wave 1)

| | |
|---|---|
| Model / effort / mode | Sonnet / medium / plain session (same session continues into step 03) |
| Worktree | `/Users/go/code/bnow-net-worktrees/48h-gov2-20260905` (step 01 runs concurrently in `48h-gov`) |
| Window | H0 → H3 |
| Depends on | — (rebase onto 01 before merge; 01 merges first) |
| Decisions | none (the Node pin is decision-free; say so in the PR) |
| Spend | $0 |
| Closing report | `docs/reviews/PORT-HYGIENE-MACOS-2026-09-05.md` |

Read `docs/prompts/2026-09-05-48h-COMMON.md` first.

## Goal

Remove every Linux/WSL2 assumption that is wrong on the Mac, delete the one dead script that
breaks, record the two new host conventions, and pin Node. One PR, `scripts+docs: retire
WSL2 assumptions after the macOS port`. The scan that found these is summarized in INDEX §8;
re-verify each line before editing.

## Findings to act on (verify each; cite the line you changed)

1. `scripts/llm-redigest.sh` — hard-codes `/home/go/code/bnow.net`, uses GNU `date -d`,
   referenced by nothing (`git grep llm-redigest`). Delete it (CLAUDE.md scoped exception;
   say why in the commit).
2. `scripts/pin-dns.cjs` — WSL2 NAT-resolver workaround. Keep the file; rewrite its header
   comment (lines 2-7) to say it is a WSL2 relic, harmless but unnecessary on macOS, and not to
   be prescribed in new docs. Remove the "this WSL2 box needs the DNS pin" instruction from:
   `CLAUDE.md:36-38`; `docs/CURRENT-STATE.md:630-636` (rewrite the bullet for the Mac: no DNS
   pin, api.openai.com reachable locally — the 2026-09-03/05 local eval runs prove it, github.com
   DNS not flaky); `docs/reviews/OPENSANCTIONS-RESCORE-RUNBOOK.md:93-97`;
   `docs/reviews/X-GAP-RECOVERY-RUNBOOK-2026-07-13.md:83-84,187,216`;
   `scripts/opensanctions-rescore.ts:35-36`; `scripts/ab-mapreduce.ts:13`;
   `docs/prompts/2026-07-10-mtproto.md:26`; `docs/prompts/BUILD-mirror-trade.md:18`. Living
   runbooks get a one-line "(macOS: no DNS pin needed)" where a command is quoted; do NOT edit
   archives (`docs/PROGRESS.md`, `docs/DECISIONS.md`, dated review reports other than the two
   living runbooks named, AGENTS.md decision log).
3. `/home/go` paths in live text: `scripts/telegram-login.ts:151-155` (error message →
   `/Users/go/code/bnow-net`); `docs/BLOCKERS.md:7` (`~/code/scenefiend/.env.local` — add a
   dated note, do not edit the 2026-07-04 line); `docs/SETUP-NEXT-WEEK.md:170` (`cd ~/code/bnow.net`);
   `docs/NEXT-SESSION-PROMPT.md` (2026-07-06 relic — delete the file; grep for links first).
4. GNU `date -d` in a living runbook: `docs/SETUP-NEXT-WEEK.md:188-189` →
   `$(date -u -v-1d +%F)` (BSD) — or show both forms.
5. Code comments that justify routing all LLM work through the deployed route "because this
   box cannot reach api.openai.com": `scripts/map-remap.ts:3-6`, `scripts/map-backfill.ts:4-6`,
   `scripts/telegram-getme.ts:14-15`. Reword to state the real reason (production metering and
   env live on Vercel) and note that a local `next start` against a disposable Neon branch is
   now a valid non-production target — step 22 relies on this. `scripts/isw-refresh.ts:40-42`:
   the "unpooled credentials are stale" note is a WSL2-`.env.local` fact — check the Mac
   `.env.local` has both URLs (names only; never print values) and reword.
6. `docs/OPEN-TASKS.md` #74 (dev-mode hydration dead on WSL2): run `npm run dev` for 60 s and
   load `/` once in a browser or with `curl -s localhost:3000/ | head -c 400`; if it serves,
   record "hydration works on macOS — re-test in browser" under #74 and leave closure to the
   operator. Do not spend more than 10 minutes here.
7. Node pin: add `"engines": { "node": ">=22" }` to `package.json` and a `.nvmrc` containing
   `22` (CI uses 22; the Mac runs 24 — both satisfy the range). Mention in the PR that no
   behaviour changes.
8. **Conventions** (handoff §4.6; nowhere in the repo yet). Add to `CLAUDE.md` § Commands &
   setup and to AGENTS.md § Conventions (AGENTS.md:962-972), in place:
   - Worktrees are created only from a native session on the Mac; a worktree created through
     a Cowork/remote mount writes `/sessions/…` gitdir paths git cannot resolve later (the
     2026-09-05 cleanup found one such orphan — cite `docs/prompts/2026-09-05-cto-roadmap-handoff.md`
     §4.6 as the source).
   - Deploys come only from the plain release clone `/Users/go/code/bnow-net-rel-20260823`
     (never a worktree — OPEN-TASKS #78), via `npx vercel@latest deploy --prod --yes`; step 03
     writes the checklist this line points to.
   Also correct `CLAUDE.md:34-35` to say "from the plain release clone" and `README.md:41-42`
   (old one-liner).
9. Case sensitivity: `tsconfig.json` relies on TypeScript's default
   `forceConsistentCasingInFileNames`; add it explicitly (`true`) so the pre-push gate on the
   case-insensitive Mac keeps catching import-case drift that Linux CI would reject.

## Tests / acceptance

`npm run typecheck && npm run lint && npm test` green (unit count unchanged; note the number).
`git grep -n "WSL2"` afterwards returns only archives and the pin-dns header. `git grep -n
"/home/go"` returns only archives. Report the 60-second `npm run dev` result.

## Report

Per COMMON §5. Then continue in the same session with step 03 (new step branch).
