# STATE — BNOW.NET reconciliation (recon 2026-07-10)

**Compiled 2026-07-11 ~04:36 UTC** (recon prompt authored 2026-07-10, executed 07-11;
prompt archived verbatim at `docs/prompts/2026-07-10-recon.md`).
**Method:** read-only. Six parallel investigators against git history, the filesystem,
the live Neon DB (`scripts/sqlq.ts`, SELECT-only), and local `vitest`/`npm test`.
**Every number below carries provenance** (SQL / `file:line` / git ref).
**Actual product-LLM / paid-API spend this session: $0.00** — no deployed `/api` route,
no OpenAI/Anthropic/OpenSanctions/X call, no spend-incurring script was run.

> **Clock note (affects every windowed claim).** Truth: `SELECT now()` = **2026-07-11
> T04:36:12Z** (timestamptz). The convenience form `now() AT TIME ZONE 'UTC'` renders as
> `08:36Z` through `sqlq.ts` — a **+4h artifact**: it yields a `timestamp` *without* tz,
> which the Neon HTTP driver then localizes to the box's America/New_York (UTC−4 in July)
> before re-emitting as "Z". Confirmed: `raw_now` 04:36Z vs `naive_utc` 08:36Z, same row.
> The system is **live and current** — last cron 04:30:30Z, last doc 04:30:39Z, last digest
> 04:06:37Z — there is **no outage**. (Tooling nit: `sqlq` on `timestamptz` is correct;
> avoid `AT TIME ZONE 'UTC'`.)

---

## Executive summary (≤20 lines)

1. **MR Sprint 3 SHIPPED and is live-healthy.** reduce+synthesize+engine on disk, 59/59
   targeted tests green; A/B gate K=3 FAILED variance → K=5 PASSED all three criteria
   (coverage 24.97 vs 21.14, SD 6.94 vs 8.02, unsupported 0.296 vs 0.408); prod is serving
   `openai:gpt-4o-mini+mapreduce` on ru/ua/ir with `votes=5, failedVotes=0, truncationRetries=0`.
2. **AGENTS/CLAUDE restructure SHIPPED.** DECISIONS.md archive (298 ln), AGENTS.md snapshot+rulings
   (323 ln — ~23 over its own 300 budget, archive pass due), CLAUDE.md commands block, case rename.
3. **All July-6 hardening debt SHIPPED** (CI+pre-push, /ask caps+`ask_usage`, entity-canon 3 layers
   propose-only, integration tests, per-theater takeaway filter, IRAN_MILITARY_PROMPT, reliability
   weighting, ME zombies → 0 per-theater). `npm test` = **471/41 green** (matches AGENTS.md exactly).
4. **92 commits since 07-07 — zero orphans**; HEAD == origin/main (`2884f50`).
5. **Live spine healthy & current:** every cron 0-failed/0-killed over its 38h of history; map
   coverage **99.87%** (43,525/43,582); digests current all theaters; persist guard *observed firing*
   (2 ir thin-regen refusals). gulf/il correctly on legacy fallback.
6. **Live regression — X ingestion FROZEN ~32h.** `X_SPRINT_USD_CAP` exhausted at exactly $5.0001;
   `ingest:x` has run 39× since all `ok=true` but `fetched:0` — **green crons masking a dead adapter.**
   Docs still call X "live." OpenSanctions enrichment likewise frozen at its 300-call lifetime cap.
7. **Scoreboard instrument trustworthy, product far from target.** k=5 majority matcher, real
   claim→doc graph. Coverage ru 18.4 / ua 15.6 / ir 20.7% vs **≥80%** (59–64 pts short); unsupported
   (thin-sourced proxy, not literal) 45–56% vs **<2%**; info-lead **+15h** (favorable, but outside the
   symmetric ±6h band). X = ~27–29% of cited docs = single-platform dependency (and it's the frozen one).
8. **Spend fully contained:** all-time paid total **$40.63** (opensanctions $33 + x $5 + OpenAI LLM
   $2.63). No provider trending toward a *daily* cap; the two *all-time* caps (X, OpenSanctions) are
   already hit — intentional fail-closed, but freezing coverage.
9. **Next session → (b) MTProto ingest sprint** (justification at the end): coverage is the sole
   failing product metric, its documented cause is corpus scarcity (#19), and MTProto is the largest
   untapped corpus *and* reduces the 27–29% X dependency that is currently frozen. Primed (`bc3­e2c`
   session-mint + creds in env); gated only on a one-time operator login. In parallel, un-freeze X (#38).

---

## TASK 1.1 — MR Sprint 3 (reduce + A/B + cutover)

**Verdict: SHIPPED.** All five sub-systems (reduce core, K-voted synthesis, `DIGEST_ENGINE`
dispatch, new cron cadence, #29 Lebanese retag + map catch-up) are on disk, tested green, and the
map-reduce engine is verifiably running in prod (`openai_reduce` billing + `digest:finalize`/
`digest:intraday` cron rows, 2026-07-09→11). Two items cannot be closed from a read-only seat: the
deployed `DIGEST_ENGINE` value (Vercel Sensitive env is write-only) and the disposable A/B Neon
branch deletion (needs the Neon API) — both are documented and strongly corroborated (see Gaps).

**1. reduce.ts / synthesize.ts / engine.ts + tests — SHIPPED.** All exist under `src/lib/analysis/`
(plus `reduce-io.ts`, `map-versions.ts`, `quote-verify.ts`). `npx vitest run reduce synthesize engine
quote-verify map-versions` = **5 files, 59 tests, all pass** (reduce 28, synthesize 16, engine 7,
quote-verify 4, map-versions 4; 407 ms). Design points verified in source:
- **Star clustering, not union-find** — `reduce.ts:208-282` (`clusterClaims`): each claim joins the
  best-scoring anchor or founds a group; non-transitive (the union-find percolation that chained 519
  claims / 30% of ru 07-08 into one group was the fix motivation, `reduce.ts:210-213`).
- **Threshold 0.35** — `REDUCE_THRESHOLD = 0.35` (`reduce.ts:108`), tuned in `scripts/reduce-tune.ts`
  on 28 pos / 170 neg labelled prod pairs (precision 1.000, recall 0.714 — highest zero-FP point).
- **Corroboration promotion** — `reduce.ts:354-375`: `hedging='claimed'` factual claims with
  `independentSourceCount(...) >= 2` (domain-diverse, mirror-excluded) → `confirmed`, `promoted=true`.
- **Entity canonicalization** — `canonicalKey`/`junkReason` (`reduce.ts:21`, applied `:377-386`).
- **K=5 synthesis (ruling 18)** — `reduceVotes()` default 5 (`synthesize.ts:48-54`); `majority =
  floor(k/2)+1 = 3-of-5` (`:214-215`); events survive only with majority support.
- **Majority-gid fill (ruling 18)** — `finalizeEvents` (`synthesize.ts:332-343`): the exact mechanism
  that fixed the round-1 variance failure.

**2. A/B gate — SHIPPED, PASSED at K=5.** `MR3-CHECKPOINT.md` + `MR3-REDUCE-RESULTS.md` present; raw
data in `MR3-AB-RESULTS.jsonl` (round 1) + `MR3-AB-K5.jsonl` (round 2).
- **Round 1 (K=3): FAILED variance** (`MR3-REDUCE-RESULTS.md:142-153`), 180/180: coverage 24.9 vs
  21.1 PASS, unsupported 0.31 vs 0.41 PASS, **within-cell coverage SD 10.5 vs 8.0 FAIL** (repro was
  better, 0.73 vs 0.55). Diagnosed as marginal-event vote-flips at 2-of-3 (worst ru 07-07: 100→33→0).
- **Round 2 (K=5 + majority-gid fill): PASSED all three** (`:155-191`, 90 samples vs untouched round-1
  legacy baseline):

  | metric | legacy | mapreduce K=5 | gate |
  |---|---|---|---|
  | coverage mean % | 21.14 | **24.97** | PASS |
  | coverage within-cell SD | 8.02 | **6.94** | PASS |
  | unsupported-claim rate | 0.408 | **0.296** | PASS |
  | distinct docs cited / digest | 9.5 | 24.9 | — |
  | claim-level reproducibility (#28) | 0.549 | 0.745 | — |

  Per-theater paired coverage: ru −0.06 (p=1.00, parity), ir +15.11 (p=0.067), **ua −3.57 (p=0.45,
  noise-scale — the standing "watch ua" caveat).** Verdict text: "cut over" (`:184`). No drift vs the
  AGENTS.md 2026-07-09 log.

**3. `DIGEST_ENGINE` flag — SHIPPED (prod value unverifiable by design; corroborated live).**
`engine.ts:19-21`: `process.env.DIGEST_ENGINE === "mapreduce" ? "mapreduce" : "legacy"` — **code
default is legacy** (the rollback), exact-string compare; dispatch `:50-57` falls back to legacy on
`null` (gulf, no doc_claims). Prod value is a Vercel **Sensitive** (write-only) var — inferred, not
read: git `f5d994f` + `2829b38`; first `openai_reduce` row 2026-07-09 (5 req = the K=5 votes,
$0.005384), then **07-10: 120 req $0.173**, **07-11: 60 req $0.067** (to 04:00Z), all < `REDUCE_USD_CAP
_DAILY`; `openai_digest` still increments for the gulf legacy fallback — exactly as designed.

**4. Cron cadence in `vercel.json` — SHIPPED.** `digest?mode=finalize` @ `0 2 * * *`;
`?mode=intraday&slot=kyiv-morning` @ `0 4`; `eu-midday` @ `0 10`; `us-afternoon` @ `30 19`. Live:
`cron_runs` shows `digest:finalize`@02:01 + `digest:intraday`@04:01/10:00/19:30, all `ok=true`.

**5. Disposable A/B Neon branch deletion — UNKNOWN (documented, not verifiable read-only).** Branch
`br-proud-sun-atn3fch0`, documented deleted (`MR3-CHECKPOINT.md:20`, `MR3-REDUCE-RESULTS.md:132`;
PROGRESS.md "Branch deleted after the report committed"). `ab-mapreduce.ts:42-50` neither creates nor
deletes branches (consumes `AB_DATABASE_URL`, refuses prod host); lifecycle was manual. Confirmation
needs the Neon API — out of scope (see Gaps).

**6. OPEN-TASKS #29 (Lebanese channels → ir) + map catch-up — SHIPPED (DB-verified).**
- Channels route to ir: sources `mmirleb` (9157), `mtvlebanonews` (10321), `sameralhajali` (10378);
  `raw_documents` for those source_ids are **857 docs entirely `country_iso2='ir'`, zero ru**
  (`SELECT source_id,country_iso2,count(*) … WHERE source_id IN (9157,10321,10378)`). git `ed8481a` +
  `77bd071`; ruling 11 corrected in place.
- ir map backlog drained: naive `processed=false` = 1,107, but the map filter (`map-worker.ts:262-267`:
  `content≥40 AND NOT stub AND day≥MAP_EPOCH 2026-07-04`) makes **only 23 eligible-unmapped** (658
  short, 426 pre-epoch). Those 23 are the fresh intra-hour tail; `doc_map_state` max `mapped_at`
  07-11 03:40. ir `doc_claims` at current versions = **3,886** (mil 2,791 / elite 654 / nuclear 441).
  Catch-up itself (07-09): 620 selected → 100% disposition, 41 claims, $0.0041.

**Drift flags:** (a) retag count wobble is cosmetic — #29 title "635", log "651 moved", 857 today =
651 + post-retag ingestion; the prompt's "~586" matches none, the sourced catch-up figure is **620
selected**. (b) "1,107 unprocessed ir docs" is misleading without the eligibility filter — real
backlog is 23; not drift, just worth stating so nobody re-opens a non-issue.

---

## TASK 1.2 — AGENTS/CLAUDE restructure

All four restructure commits are ancestors of `main` (`192aa28`, `8366f15`, `7939a4d`, `7f3d892`).

- **`docs/DECISIONS.md` — SHIPPED.** 298 lines; titled the append-only archive; holds 2026-07-04 →
  07-09 entries verbatim (two passes: `8366f15` archived the tail, `192aa28` the MR-sprint-2 entries).
  *Minor:* the archive spans 07-04→07-09, not "07-04→07-08" as the recon brief guessed — brief undercounted.
- **AGENTS.md — SHIPPED, budget PARTIAL.** 323 lines; shape = Charter · Architecture · **Current-state
  snapshot** · **Standing rulings (18)** · **Decision log (current cycle only)** · Conventions ·
  Credentials · Next steps · Protocol. This is a brain, not a journal. **Drift:** its own rule says
  "under ~300 lines" and `8366f15` claimed "476→301"; it has since grown to **323** (sprint-3/cutover
  entries) — next archive pass is due → OPEN-TASKS #43.
- **CLAUDE.md — SHIPPED.** 50 lines; `## Commands & setup` present (`:23-39`: install, hooksPath,
  test/typecheck/lint/dev, integration, db, deploy, DNS pin). git `7939a4d`.
- **Filename case — SHIPPED.** `git ls-files | grep -i claude` → exactly `CLAUDE.md` (lowercase),
  no stray `CLAUDE.MD`. git `7f3d892`.

---

## TASK 1.3 — July-6 hardening debt

Every item verified against code/DB/git (not the docs). **`npm test` → 471 passed / 41 files
(0 fail, 1.6s) — the AGENTS.md claim is exact.**

| # | Item | Status | Evidence |
|---|---|---|---|
| a | CI + pre-push gate | **SHIPPED** | `.github/workflows/ci.yml` gate = `tsc --noEmit` + `npm run lint` + `npm test` (push+PR); `integration` job runs only with `NEON_API_KEY`. `.githooks/pre-push` (`-rwxr-xr-x`, `set -e`, same 3). `git config core.hooksPath`=`.githooks`. |
| b | /ask caps + `ask_usage` logging | **SHIPPED** | `ask/limits.ts`: 20/user/UTC-day + $1/day global; `askWithLimits` INSERTs `ask_usage`; `route.ts:16` → 429. `limits.test.ts` 6 green. DB: `ask_usage` = **4 rows, 1 user, all 2026-07-08 20:46–21:19Z** (0 in last 48h). |
| c | Entity canonicalization (3 layers, propose-only) | **SHIPPED** | rules `canonicalize.ts` (`junkReason`/`planCleanup`); LLM audit `entity-audit/route.ts:131-132` **returns** proposals, never writes (ruling 6); extraction lexicon `tracks.ts:81 ENTITY_RULES` joined in `map-prompts.ts:129,136,139`. (LLM cleanup pass is propose-only, never run in prod — no `openai_entity_audit` usage rows.) |
| d | Integration tests: trigger / digest path / scoreDigest | **SHIPPED** (not run here) | `src/integration/hardening.itest.ts` (claim→source trigger deferred-COMMIT ×3; `generateDigest` e2e; /ask reliability ordering; `scoreDigest` fixture) + `reduce.itest.ts` (#32 overwrite guards + trigger on shared path). Not executed — forks a Neon branch. |
| e | Per-theater ISW takeaway filtering | **SHIPPED** | `validation/run.ts:98-119`: ru/ua takeaways filtered via `classifyTakeawayTheater` to both\|own-theater, re-indexed, `details.takeawaysFiltered` recorded; full set still persisted on `isw_reports`; ir validates its own Iran Update. |
| f | Iran military prompt variant | **SHIPPED** | `tracks.ts:135 IRAN_MILITARY_PROMPT` + `:113 IRAN_MILITARY_LEXICON`, wired `systemPromptByCountry/lexiconByCountry {ir}` (`:207-208`); `NUCLEAR_PROMPT :175`. |
| g | Reliability weighting in ranking/retrieval | **SHIPPED** | legacy `digest.ts:89,100 ORDER BY COALESCE(reliability_score,0.3) DESC`; mapreduce `reduce.ts:467 scoreGroup` uses `maxReliability`, `rankGroups :471`; /ask orders by confidence (`hardening.itest.ts:185`). |
| h | ME zero-citation "zombie" rows | **SHIPPED / resolved** | `source_theater_stats` has **0 zero-citation rows** (ru 6,985 / ir 3,598 totals). `sources.country_id` is NULL for all 11,131 rows (theater lives in `source_theater_stats`), so the "1,574 ME zombies" figure is stale; the residual **1,167 global-zero rows** are UI-hidden via `citation_count>0` (`registry/page.tsx:28`, `app/page.tsx:27`) — truth-in-UI ruling 3 upheld. |

---

## TASK 1.4 — Commit accounting

**92 commits since 2026-07-07** (`git rev-list --count --since="2026-07-07 00:00" HEAD` = 92; 13 on
07-07, 5 on 07-08, 74 on 07-09). **HEAD == origin/main = `2884f5095f2f9210488d7b13089541f49bde78b0`**
(`git rev-parse`, single clean attempt — no unpushed work). **All 92 map to a known workstream —
zero orphans.**

| Workstream | commits | | Workstream | commits |
|---|---|---|---|---|
| MR sprint 3 (reduce/A-B/cutover) | 44 | | AGENTS/CLAUDE restructure | 5 |
| Map stage (MR sprint 2) | 9 | | Ingest/feeds/routing | 5 |
| MR sprint 1 (metering/guards/cron_runs) | 12 | | Source-mix quota | 4 |
| Nav restructure | 6 | | Coverage sprint | 3 |
| #29 retag | 2 | Auth/email 2 · Mobile-nav 1 · MTProto 1 · i18n 1 · pipeline-audit/ops ~10 | |

**Prompt provenance gap (systemic):** `docs/prompts/` holds only the five 2026-07-06 `BUILD-*.md` +
README. **Zero** of these 92 commits' workstreams has a saved prompt; their provenance lives only in
decision-log/review docs. (This recon's prompt is now added at `docs/prompts/2026-07-10-recon.md`.)

**Findings (recent auth/nav/MTProto cluster):**
- **A — test-count churn is self-consistent, not drift.** `0731a41` set 470 (after MTProto+Postmark),
  `28d4efc` +1 mobile-nav test, `2884f50` set 471; live `npm test` = 471/41. No correction needed.
- **B — the Postmark fix (`9b5b368`) did NOT fix the reported login failure.**
  `docs/reviews/AUTH-EMAIL-2026-07-09.md`: `track.pstmrk.it` link-rewriting was a *real, secondary*
  defect (fixed), but both tracked and untracked links authenticated — the perceived breakage fits a
  **single-use token consumed by the first open** (phone prefetch/scanner) then reopened on a second
  device → `/api/auth/error?error=Verification`. Standing debt left: sender still
  `no-reply@scenefiend.app` (SETUP #3); `TrackOpens:false` "doesn't take" at Postmark; **`git push`
  does not deploy** (no GitHub→Vercel integration — prod served the stale build ~20 min). → #40, #39.
- **C — mobile-nav fix (`28d4efc`) verified only structurally.** jsdom computes no layout; the new
  test asserts DOM nesting (sheet is a sibling of `<header>`), not the visual clip it fixes.
- **D — MTProto login (`bc30e2c`) enables an integration that is still unwired.** Ships
  `scripts/telegram-login.ts` (mints a GramJS StringSession to gitignored `.telegram.session`), adds
  `gramjs` (+791 lock lines). The **adapter remains a fixture stub** not in prod ingest; **no
  `.telegram.session` exists yet** (needs a phone+TTY login). The session-mint half exists; the
  adapter wiring is still open. → #38-adjacent / next-session.

---

## TASK 2 — Live-system health

**As of now()=2026-07-11 04:36Z.** `cron_runs` history spans only 2026-07-09 14:15Z → now (~38h — the
table was populated from the cutover redeploy forward), so "last 48h" ≈ the whole table. Pre-14:15
runs survive only in their output tables (e.g. the 07-09 07:00 validate → `validation_runs`).

**1. Per-cron health — 0 unfinished (`finished_at IS NULL`), 0 failed (`ok=false`) across all 12 job
types.** No killed/timed-out runs.

| job | runs | ok | unfin | verdict | avg_s | max_s |
|---|---|---|---|---|---|---|
| ingest:fast (`*/15`) | 153 | 153 | 0 | every slot fired (153/153) | 141 | 162 |
| ingest:telegram (`:10`) | 39 | 39 | 0 | ok | 130 | 137 |
| ingest:x (`:20`) | 39 | 39 | 0 | **ok but no-op — §5** | 13 | 84 |
| map (`:40`) | 38 | 38 | 0 | every slot fired | 33 | 102 |
| digest:finalize (02:00) | 2 | 2 | 0 | ok | 356 | 423 |
| digest:intraday (04/10/19:30) | 6 | 6 | 0 | ok | 248 | 349 |
| validate (07:00) | 1 | 1 | 0 | ok | 13 | 13 |
| enrich (08:00) | 1 | 1 | 0 | **ok but 0.29s no-op — §7** | 0 | 0.3 |
| datadark (09:00) | 1 | 1 | 0 | ok | 29 | 29 |

(`digest`/`digest:core`/`digest:gulf`/`map:backfill` rows = 07-09 cutover-day manual/backfill runs,
superseded. `trade`/`materials` monthly — correctly absent.)

**2. Runtime vs `maxDuration` — no job near its ceiling.** ingest 162/300 (54%, the one to watch),
digest 423/800 (53%), map 102/800 (13%), validate 13/300, datadark 29/300. Grep of
`src/app/api/cron/*/route.ts`.

**3. Digest currency & engine — all live theaters current on the intended engine.**
- **ru(1)/ua(3)/ir(5): current**, latest `digest_date`=2026-07-11, provider `openai:gpt-4o-mini+mapreduce`.
- **il(4)+gulf sa(6)/ae(7)/qa(8)/om(9): current on legacy fallback** (`openai:gpt-4o-mini`) — matches
  the "no doc_claims → legacy" ruling. bh/kw/cn: zero digests (scaffolded/deferred).
- 48h provider mix: mapreduce 18, legacy 26. Every mapreduce digest: `engine=mapreduce, votes=5,
  failedVotes=0, truncationRetries=0`, `reduce.claims` 13→1251 — none zero.

**4. Validation currency — current for its cadence.** Last `run_at`=2026-07-10T07:00Z (scoring the
07-09 finalized mapreduce digests: coverage 25.0/40.0/40.0, unsupported ≤0.50, lead +20–22.7h); next
validate (07-11 07:00) is in the future. `llm_match` 07-10 = 15 req (k=5 × 3) — LLM path, not keyword.
*(One 07-09 run scored 0.0 coverage — see TASK 4.)*

**5. X ingestion — SILENT DEGRADATION (key finding).** `ingest:x` fires hourly, returns `ok=true`,
but is a **no-op**: `counts={fetched:0,inserted:0,errors:0}` in ~180 ms every run. Last `x_api` doc
`fetched_at`=**2026-07-09T20:21Z → ~32h of zero new X docs.** `provider_usage.x_api` total =
**$5.0001 / 3,063 req**, last row 07-09. Diagnosis: cumulative spend flat-lined at exactly ~$5.00 =
`X_SPRINT_USD_CAP` exhausted → `SpendGuard.tryReserve()` fails closed → adapter returns 0 fetched with
no error (ruling 4 as designed). **Correct fail-closed, but X coverage is frozen while docs still list
X as "live" — DRIFT.** (X ≈ 27–29% of digest citations — TASK 4 — so this is material.)

**6. Map coverage — 99.87% (target ≥95%: PASS).** Eligibility (verbatim `map-worker.ts:262-267`):
`processed=false AND country_iso2 ∈ {ru,ua,ir} AND length(content)≥40 AND NOT '[STUB FIXTURE]%' AND
COALESCE(published_at,fetched_at)::date ≥ 2026-07-04`. Denominator uses `raw_documents.processed`
(ruling 13 — the faithful disposition signal; a raw `doc_map_state` join would undercount mirrors +
no-track docs).

| theater | eligible | disposed | backlog | coverage |
|---|---|---|---|---|
| ru | 18,153 | 18,126 | 27 | 99.85% |
| ua | 5,706 | 5,701 | 5 | 99.91% |
| ir | 19,723 | 19,698 | 25 | 99.87% |
| **all** | **43,582** | **43,525** | **57** | **99.869%** |

The 57-doc backlog is entirely fresh (oldest unprocessed `fetched_at` 07-11 03:45, after the last
03:40 run) — normal intra-hour lag, drained by the next `:40`. Superseded pre-07-09 extractor versions
persist as history and are filtered out by the `map-versions` accessor (rulings 13/18).

**7. Silent-failure class — guards confirmed firing, not silently absent.**
- **Persist guard IS firing:** `digest:intraday` 07-10 19:30 and `digest:finalize` 07-11 02:00 both
  recorded `overwriteRefusals:1, refusalDetails:["ir thin-regen"]` in `cron_runs.counts` (ruling 17).
- **0-claim / empty digests: NONE** — all 44 digests in 48h have `rendered_md ≥ 433 chars`; every
  mapreduce `stats.reduce.claims ≥ 13`.
- **Truncation / refusal spikes: none** — every digest `truncationRetries=0, failedVotes=0`.
- **Thin-but-nonzero WATCH:** ua military 07-10 (id 504) rendered 998 chars / `survivingEvents=2` (vs
  4 on 07-11) — aligns with the open ua coverage watch.
- **Secondary — enrich idle (cause resolved in TASK 3):** enrich 07-10 ran 0.29s, no OpenSanctions
  rows since 07-09 → the 300-call cap (TASK 3 §4).
- **Secondary — gdelt staleness:** last gdelt fetch 07-10 23:16 (~5h) — documented upstream flakiness,
  not a cron failure.

**Overall:** ingest→map→digest→validate spine healthy and current; cutover holding; one live
degradation (X frozen on an exhausted cap while docs say "live").

---

## TASK 3 — Spend audit

All from `provider_usage` (queried 2026-07-11 04:36Z), `src/lib/usage/*`, `enrich/run.ts`, `x-api.ts`,
`cron_runs`. No paid call made. Prod cap envs are Vercel **Sensitive** (write-only) — stated prod
values are inferred from runtime behavior or read out of a cron error string, and flagged as such.

**Full table (19 rows, lifetime 07-07 → 07-11):** per-provider 7-day totals (= table lifetime):

| provider | requests | est_usd | days |
|---|---:|---:|---|
| llm_match | 415 | $0.0518 | 07-07..07-10 |
| openai_digest | 65 | $0.0821 | 07-09..07-11 |
| openai_map | 2,212 | $2.2524 | 07-09..07-11 |
| openai_reduce | 185 | $0.2454 | 07-09..07-11 |
| **opensanctions** | 300 | **$33.00** | 07-07..07-09 |
| **x_api** | 3,063 | **$5.0001** | 07-07..07-09 |

**Category totals:** OpenAI product LLM (map+reduce+digest+match) = **$2.6317**; x_api $5.0001;
opensanctions $33.00; **grand total all paid providers = $40.63.** The two non-LLM providers are 93.5%
of all spend and both already sit at a hard cap.

**Caps (env value / code default / prod verifiability):** guard fails closed when both `totalCapUsd`
and `totalRequestCap` are unset (`spend-guard.ts:72`), and when the resolved daily USD cap is null;
per-day USD caps resolve `envCap(…) ?? (isProduction() ? null : DEFAULT)`.

| Cap env | code default | `.env.local` | prod |
|---|---|---|---|
| `LLM_SPRINT_USD_CAP` (all-time backstop) | none → **fail-closed** | **absent** (by design) | unverifiable |
| `LLM_DIGEST_USD_CAP` (daily) | 2 (non-prod only) | absent | unverifiable |
| `MAP_USD_CAP_DAILY` (daily) | 3 (non-prod only) | absent | ≥$2.086 (07-09 backfill didn't stop) |
| `REDUCE_USD_CAP_DAILY` (daily) | 2 (non-prod only) | **=2** | =2 (consistent w/ $0.17/day burn) |
| `X_SPRINT_USD_CAP` (all-time) | none → **fail-closed** | absent | **≈$5.00** (spend stalled at $5.0001; `x-backfill.ts:8` example uses 5) |
| `X_DAILY_USD_CAP` (daily) | 1.5 | absent | **>$1.877** (07-07 exceeded the default) |
| `OPENSANCTIONS_CALL_CAP` (all-time req count) | 300 | absent | **=300 CONFIRMED** (cron error string) |

**Burn rate & days-to-cap — no provider trending toward a *daily* cap within 7 days.** Steady-state
(07-10, first full mapreduce day): `openai_reduce` $0.173/day = **8.7% of $2** (inside the AGENTS.md
$0.10–0.30 expectation); `openai_map` $0.159/day steady (the 07-09 $2.086 was one-off Lebanese-retag
catch-up + `map:backfill`, held under cap); `openai_digest` $0.017/day (gulf fallback only); `llm_match`
~$0.002/day. **Two all-time caps ALREADY HIT** — this is the headline:
- **x_api STALLED at ≈$5.00 sprint cap** (`>=` refuses, `spend-guard.ts:82`); 39 green `fetched:0`
  crons since = green crons masking a budget-frozen adapter (~32h dead).
- **opensanctions STALLED at 300/300 call cap** (below).

**OpenSanctions follow-up (the MR1 "$33 in 3 days / 2,000-call quota" flag):**
- The $33 window is fully captured, *not* pre-table: opensanctions = 07-07 (200 req/$22) + 07-08
  (91/$10.01) + 07-09 (9/$0.99) = **300 req = $33** at `OS_EST_USD_PER_MATCH=0.11` (`enrich/run.ts:17`).
- **Budget IS fixed and enforcing.** `OPENSANCTIONS_CALL_CAP` is a **total (all-time) request cap**
  (`enrich/run.ts:24`), not monthly; it resolved to 300 in prod. The 07-10 enrich cron (`cron_runs`
  id 253, `ok=true`, `scanned=200`) recorded `sanctions.budgetStopped="opensanctions: total requests
  300 >= cap 300"` and did **0** checks.
- **Monthly trajectory: trivially inside 2,000** — the 300 lifetime backstop structurally prevents it;
  worst case $220/mo (2,000×$0.11) can't occur unless the operator raises the cap.
- **Side effect:** sanctions enrichment is now **permanently frozen at 300 lifetime calls** — zero new
  coverage since 07-09 (intended fail-closed; raising it is licensing-gated). → #41.

---

## TASK 4 — Scoreboard truth

**§8.7 targets (`PRODUCT-BRIEF.md:315`):** "Phase 2: event coverage ≥80% of ISW-reported events
same-day; unsupported-claim rate <2%; timeliness within ±6h of ISW publication."

**Measured (all 49 `validation_runs`, 2026-07-05→07-10; `validation_runs v JOIN digests d`):**

| Theater | n | Coverage mean±SD | Nonzero-day (n) | Zero-cov days | Unsupported mean | Info-lead median |
|---|---|---|---|---|---|---|
| ru (1) | 20 | **18.4% ± 24.2** | 33.4% (11) | 9/20 | 0.564 | +15.2h |
| ua (3) | 20 | **15.6% ± 19.4** | 31.2% (10) | 10/20 | 0.454 | +14.5h |
| ir (5) | 9 | **20.7% ± 16.7** | 31.1% (6) | 3/9 | 0.491 | +15.2h |

Weighted overall coverage 17.7% (nonzero-day ~32%) — confirms AGENTS' "~17.5% / ~31%" snapshot. **22 of
49 runs (45%) scored exactly 0%.** SD ≈ mean (ruling-17 extraction variance). Post-cutover subset
(`run_at ≥ 07-09`, n=**6**, tiny): coverage mean 32.6% (nonzero 39.1%), unsupported 0.383 (ru 20.0 /
ua 48.6 / ir 29.2) — directionally consistent with the A/B, but n=6 is a leading indicator only.

**Distance from each §8.7 target (no spin):**

| Theater | Coverage → vs ≥80% | Unsupported → vs <2% | Info-lead → vs ±6h |
|---|---|---|---|
| ru | 18.4% → **61.6 pts short** (best day 33.4%) | 56.4% → **~28× over** | +15.2h → 9.2h outside (early) |
| ua | 15.6% → **64.4 pts short** (best day 31.2%) | 45.4% → **~23× over** | +14.5h → 8.5h outside (early) |
| ir | 20.7% → **59.3 pts short** (best day 31.1%) | 49.1% → **~25× over** | +15.2h → 9.2h outside (early) |

- **Coverage:** the best theater's best-case (~39% post-cutover / ~33% all-time) is less than half of 80%.
- **Unsupported — definitional caveat (important):** the DB column is the **thin-sourced proxy**, not a
  literal hallucination rate. `score.ts:14-15,101-103`: `thin = claims with docCount<2 AND hedging ∈
  {claimed,unverified}`. The schema guarantees ≥1 source (ruling 2), so *nothing is truly unsupported* —
  this counts single-sourced hedged claims. It is the KPI §8.7 names, and by that definition we are 23–28×
  over, but the label overstates hallucination risk. Real fix = corroboration (≥2 sources). → #45.
- **Timeliness:** `timelinessHours = median(ISW publish − earliest supporting doc)` (`score.ts:82-85`).
  All 27 non-null runs positive (min +0.5h, max +24.7h; 0 negative); the other 22 are NULL (zero-match
  days have no pair to time). We consistently *lead* ISW ~15h — favorable for a monitoring product, but
  outside the symmetric ±6h window by ~9h, and conditional on matching at all.

**Matcher — k=5 majority-vote LLM (current).** From `validation_runs.details`: **35 runs**
`matcher=llm-majority, voteRounds=5` (ruling 18, since 07-07T20:07); **14** older `matcher=llm`
single-round (07-05→07-07 04:05); **zero keyword-fallback** runs. All 9 ir runs are majority-vote.
*(The 07-09 id-279 0.0-coverage run is a genuine zero-match day, not a matcher failure.)*

**X-share of citations ≈ 27–29% (single-platform dependency).** From the persisted citation graph
`claims → claim_sources → raw_documents` grouped by `adapter`: since cutover (created ≥07-09) x_api =
**206/710 distinct cited docs (29.0%)** (rss 291, tg 185, gdelt 28); all-time **340/1,279 (26.6%)**. So
~1 in 3.4 cited docs is from X — and X is currently the *frozen* adapter (TASK 2/3). Loads §8.6 risk-1
(X pricing/breadth) and risk-2 (validation contamination via ISW's own X sources). → #42. **Distinct
docs cited / digest: mapreduce 39.0 (18 digests) vs legacy 10.1 (26)** — corroborates and exceeds the
A/B's 24.9 vs 9.5: the new engine cites ~4× more distinct docs, even as coverage-vs-ISW stays low.

**Bottom line:** trustworthy instrument, product nowhere near §8.7. Coverage is the headline gap.

---

## TASK 5 — OPEN-TASKS reconciliation

Full changes applied in `docs/OPEN-TASKS.md`. Existing item numbers are **preserved** (the decision
log and standing rulings cite them by number — #18/#28/#29/#32/#33/#35/#37 etc.); newly-surfaced items
continue the sequence at #38+.

**Closed this recon (were open, proved shipped):**
- **#1 CI pipeline** → `.github/workflows/ci.yml` + `.githooks/pre-push` (TASK 1.3a). *(was still
  unstruck in Tier 1 — stale-open.)*
- **#2 /ask rate limit** → `ask/limits.ts` + `ask_usage` + 429 (TASK 1.3b).
- **#3 Entity canonicalization** → 3 layers present, propose-only (TASK 1.3c); note the LLM cleanup
  pass has never been *run* against prod (operator-gated, ruling 6).
- Re-confirmed already-struck closures with evidence: #10(sa) #15 #16 #18 #28 #29 #32 #34 #35.

**Updated in place:**
- **#11 (§8.7 targets)** — current honest numbers folded in (coverage ru 18.4/ua 15.6/ir 20.7%,
  unsupported ~45–56% *thin-proxy*, lead +15h); the *surface-it-on-/scoreboard* build task stays open.
- **#30 (measure digest LLM cost)** — now **measurable**: reduce $0.173/day, map $0.159/day steady,
  digest(legacy gulf) $0.017/day (`provider_usage` 07-10). The modelled $0.158/day is replaced by data.
- **#36 (map `maxDuration` sizing)** — **answered:** measured steady max 102s vs 800s ceiling (13%),
  hourly cadence keeping pace (backlog 57, all <1h old). Sizing question resolved; kept as a WATCH only.

**New (from this recon), tiered:**
- **#38 [Tier 1] X ingestion frozen + green-cron blind spot.** `X_SPRINT_USD_CAP` exhausted at $5.0001;
  `ingest:x` 39× `ok=true`/`fetched:0` (~32h). Operator: raise the cap to resume, or mark X paused; and
  add a monitor so a budget-frozen ingest cron does not read as healthy.
- **#39 [Tier 1] No git→Vercel deploy integration.** `git push` ≠ deploy (prod served the stale build
  ~20 min after the 07-09 auth fix). Wire the Vercel Git integration or codify "push then `vercel
  deploy --prod`" in the release checklist.
- **#40 [Tier 1] Magic-link cross-device UX.** Single-use token consumed by first open/prefetch →
  `error=Verification` on the second device (`AUTH-EMAIL-2026-07-09.md`); the 07-09 tracking fix does
  not address it. Decide token model or document the constraint.
- **#41 [Tier 2] OpenSanctions enrichment frozen at 300 lifetime calls.** Fail-closed working; zero new
  sanctions coverage since 07-09. Raising `OPENSANCTIONS_CALL_CAP` is licensing-gated (relates #17).
- **#42 [Tier 2] X single-platform citation dependency ~27–29%.** Concentration + contamination risk
  (§8.6). Diversify corpus (MTProto, more RSS/TG) — also the coverage fix (#19).
- **#43 [maintenance] AGENTS.md 323 > 300-line budget** — archive pass due (move the oldest current-cycle
  log entries to DECISIONS.md verbatim).
- **#44 [maintenance] `X_DAILY_USD_CAP` prod > 1.5 code default** — reconcile the default/comment.
- **#45 [Tier 2] "unsupported-claim rate" KPI is a thin-sourced proxy mislabeled as literal.** Clarify
  the metric name / add a true-corroboration (≥2-source) metric (relates #14 calibration).
- **#46 [WATCH] ingest:fast runtime creep** (54% of 300s) and **ua coverage** (A/B −3.6 pts; 07-10 ua
  thin) — both watch-only, no action yet.

---

## Recommended next session — (b) MTProto ingest sprint

**Pick: (b).** (a) MR Sprint 3 is SHIPPED and healthy — not applicable. (c) the hardening sweep is
essentially **done** — every July-6 item verified shipped; only 1-hour nits remain (#25 gate.ts
comment, #26 `error.tsx`, #27 skip-to-content), not a session.

**Justification, tied to what this recon found:** coverage is the *sole* failing product metric
(15–21% vs the 80% §8.7 target, TASK 4), and its documented root cause is **corpus scarcity, not
tuning** (OPEN-TASKS #19: "the fix is more feeds, not quota tuning"). MTProto is the largest untapped
corpus and simultaneously **reduces the 27–29% X single-platform dependency (#42) — which is itself
currently frozen** (TASK 2/3). The sprint is **primed**: `TELEGRAM_API_ID/HASH` are in the env and the
session-mint tool shipped in `bc30e2c`. Build the adapter behind the existing stub seam
(`src/lib/adapters/stubs.ts` → live, mirroring `telegram-web.ts`), fixture-test it, wire it into
`src/lib/ingest/run.ts` behind a flag — all doable at $0 with the map/dedup/digest pipeline unchanged.

**One prerequisite, one parallel action (both operator, called out honestly):**
1. **MTProto live activation needs a one-time operator login** — `.telegram.session` does **not** exist
   yet (`scripts/telegram-login.ts` needs a phone + TTY + 2FA). Build+test can proceed without it; live
   ingestion can't. The sprint should ship operator-activatable, not block on the key mid-session.
2. **Un-freeze X in parallel (#38)** — raising `X_SPRINT_USD_CAP` instantly restores ~27% of the
   citation corpus that is silently dark today; cheaper than any build and it feeds the same coverage gap.

---

## Gaps — not verifiable at $0 / read-only

- **Deployed `DIGEST_ENGINE` value** — Vercel Sensitive (write-only); inferred from `openai_reduce`
  billing + mapreduce cron behavior, not read. Testing it live would incur LLM spend (forbidden).
- **Disposable A/B Neon branch `br-proud-sun-atn3fch0` deletion** — documented deleted; confirmation
  needs the Neon API (out of scope).
- **Prod cap env values** (`LLM_SPRINT_USD_CAP`, `LLM_DIGEST_USD_CAP`, `MAP_USD_CAP_DAILY`,
  `X_SPRINT_USD_CAP`, `X_DAILY_USD_CAP`) — Sensitive/write-only; only `OPENSANCTIONS_CALL_CAP=300`
  (cron error string) and `REDUCE_USD_CAP_DAILY=2` (.env.local + decision log) are confirmed; the rest
  are inferred from runtime behavior.
- **X freeze vs genuine zero-tweets** — 39 consecutive `fetched:0` runs after spend hit exactly the cap
  is unambiguous, but not disambiguated by a live X call (skipped per $0).
- **A/B numbers** are read from the committed report/JSONL, not re-reproduced (the driver hits OpenAI).
- **Integration suite** not executed (forks a Neon branch) — verified by test-title inventory.
- **Neon branch / Postmark server-state / MTProto login / entity-audit LLM** — all require paid or
  interactive calls; verified from disk/DB/docs only.

**Session-end product-LLM / paid-API spend: $0.00.**
