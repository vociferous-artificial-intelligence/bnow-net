# analyst-trust sprint checkpoint (2026-07-12)

Prompt: `docs/prompts/2026-07-12-analyst-trust.md`. Branch `20260712-analyst-trust`,
tag `pre-analyst-trust-20260712`. Rollback deployment: **bnow-jihmibgm6** (READY, recorded
pre-sprint). Baseline gate: 996 tests / 79 files, typecheck+lint clean, HEAD `1234300`
== origin/main (one docs-only commit past deployed merge `4482669` — no functional drift).

## Status

- [x] §1 preconditions — PASS (see above)
- [x] Setup: branch + tag + prompt committed + this checkpoint
- [x] W0 diagnosis readback written (rn bigint-string bug = root cause; W4 verdict: implement $0 dual coverage)
- [x] W1 time-model truth (src/lib/time/*, TIME-MODEL.md, cadence-aware panel; 1022 tests)
- [x] W2 signed-in home reorder + redirect (R3 order, stretched-link cards, signin lands on /; 1036 tests)
- [x] W3 scoreboard explainer (explainer block + 4 how-to-read lines + home-tiles caption; 1042 tests)
- [x] W4: dual coverage SHIPPED (at-publish evidence metric, $0, no migration; prod backfill 15/18 runs, 3 honest skips); window restructure PARKED with docs/designs/ISW-CUTOFF-SCORING.md; 1053 tests
- [x] W5 registry gating (requireAdminOr404, links removed, mailto moved; 1027 tests)
- [ ] W6 docs ride-along
- [ ] Assembled gate + local drive
- [ ] Merge + deploy + prod smoke
- [ ] Morning note

## Ledger (reversible defaults taken)

- W5: registry links removed UNCONDITIONALLY (admins navigate by URL) rather than
  per-role nav rendering — simplest R5-compliant reading, avoids role-aware nav.
- W5 flag for operator: /pricing full-tier copy still promises "Source-registry
  explorer" — paid subscribers won't see it without an admin grant (morning note ⑦).
- W5: orphaned i18n keys (nav.item.registry, nav.item.me_registry,
  home.quicklinks.registry, home.features.reliability.link) left in catalogs.
- W2: headline styling (text-2xl/3xl, py-6) chosen as the compact reading of R3;
  overlay link rendered LAST in card DOM to keep existing link-index tests stable.
- W2: home.cta.digest / home.cta.coverage / home.live_label keys orphaned, left in
  catalogs (same posture as W5's orphans).
- W5: registry/[id]/page.test.tsx deleted with its only subject (sourceFlagSubject
  helper) — scoped delete exception.

- HEAD is `1234300` (docs-only) vs deployed `4482669`: treated as "main == deployed" for
  precondition 1 — the delta is docs only, verified by `git diff 4482669..1234300 --stat`.
- `data/embed-backfill-checkpoint.json` untracked leftover from 07-12 embed backfill: left
  in place, not evidence of an active session.
- Stale worktrees `.workstream/20260711-*` (both branches merged): left in place.

## W4 backfill record (prod, 2026-07-12 ~13:15 ET)

Branch-tested on br-small-tooth-atqg0c7z (deleted after green), then prod --apply:
15 of 18 runs in the 7-day window gained details.atPublish (backfilled:true);
skipped honestly: ir 07-07, ir 07-08, ua 07-08 (digests regenerated after scoring,
matched claims gone). NO headline number changed (jsonb-additive only). Notable:
ir 07-11 final 100% vs at-publish 0% (evidence arrived after ISW's 20:10Z publish);
ru 07-11 57.1% at publish == final.

## Spend

OpenAI: $0.00 so far (cap $5, all reserved for W4).
