# Orphan scratch scripts (preserved 2026-09-05)

These were untracked one-off measurement scripts left in the `87-digest-unicode-20260827`
and `97-reduce-wellformed-20260827` worktrees (`scripts/_measure-8[79]-baseline.ts`), which
were removed in the 2026-09-05 worktree estate cleanup.

They are untested, unreviewed, one-off baseline-measurement scripts kept here as `.txt` so
`tsc`/`eslint` never see them. They are not part of the build and are not expected to run.
