# BNOW.NET Quality Foundation — Independent Final Audit: SAFETY/OPERATIONS review

- **Reviewer role:** Fresh, independent safety/operations adversarial reviewer (no authoring context; commissioned by the independent final audit).
- **Model / effort:** claude-fable-5 (verified from my own system context: "You are powered by the model named Fable 5. The exact model ID is claude-fable-5."). Effort: the spawn config states xhigh inherited from the parent session; I cannot independently read my own sampling-effort parameter, so I report it as configured-by-spawner, not self-verified.
- **Review date:** 2026-08-18.
- **SHA reviewed (git rev-parse HEAD in my worktree):** `7150b494d1399dddada6e7f917b1c0e76114d458`
- **Base SHA:** `05fdd2c` (merge of origin/main `9c5e9cb` + reviewed routing tip `0e469f7`). Program diff: `05fdd2c..7150b49` (66 files, 20,727 insertions, 119 deletions — verified).

**Process order attestation:** This attack plan was written after reading only: the audit commission prompt, the governing program prompt (`docs/prompts/2026-08-17-quality-foundation-fable-ultracode.md`), CLAUDE.md/AGENTS.md project instructions, `git rev-parse HEAD`, `git log`, and `git diff --stat 05fdd2c..7150b49` (file names/counts only). No file under `docs/reviews/` and nothing in the evidence package (`/Users/go/code/bnow-net-audit-evidence-20260818`) was read before this plan was committed to the draft. The program's own reports and the evidence package were opened only afterward.

## Initial attack plan (pre-report-reading)

Numbered hypotheses I will attempt to prove, each an attack on a claimed safety property:

1. **Routing dependency (9c5e9cb..0e469f7 as consumed):** the approved-model registry or map activation hard lock can be bypassed from QF code paths — e.g., `scripts/map-remap.ts` or the eval live runner constructs a dispatch identity that skips the approval check, or the "activation lock" only guards the cron route but not the remap route/driver. Also: dispatch identity (model/effort/route-version) recorded in eval results could diverge from what is actually dispatched.
2. **SpendGuard ordering:** find a paid path in map-worker/remap/live-runner where the provider call happens before `tryReserve`, or where an unset cap env resolves to "unlimited" instead of fail-closed (especially the new `openai_eval` guard: check NaN/0/empty-string cap parsing). Check the remap driver's estimate mode cannot construct a client.
3. **Metering-before-parse:** find a billed response (including truncated/length-stopped, schema-invalid JSON, or refusal) that is discarded or throws before its usage is recorded. Check retry paths: a 429 or truncation-split that dispatches two physical attempts under one reservation (per-attempt cardinality violation), and `maxRetries: 0` on every reachable OpenAI client construction (grep all `new OpenAI(`).
4. **Lease (map-lease.ts):** construct from the exact SQL: (a) split-brain — two owners after clock skew between app clocks vs DB clock (is expiry compared against DB `now()` or a JS Date?); (b) ABA — release+reacquire by another owner between a worker's renew check and its write, with a write-inert fence (fence token checked but not enforced on doc_claims/doc_map_state writes → a zombie owner past a stalled statement can still write); (c) renew-vs-takeover race — renew uses owner-token CAS or a blind UPDATE?; (d) pooled-connection — does anything still rely on session state (advisory lock removed as *primary* — is it removed entirely)?; (e) the documented residual: a single statement stalling ≥TTL lets a takeover happen while the old owner's INSERT is in flight — is the claimed bound honest (statement_timeout? idempotent unique constraints as backstop?); (f) expired-owner release: stale owner releasing someone else's lease.
5. **Remap (scripts/map-remap.ts):** (a) eligibility anti-join wrong — superseded-version rows satisfy it, or final no-claims dispositions are perpetually re-billed; (b) checkpoint keyed without version digest → resume after a version bump silently skips docs or rebills; (c) `--execute` vs dry-run: dry-run constructs an OpenAI client or reserves; (d) budget parse: `--budget-usd=NaN/0/-1` fails open; (e) route capability handshake spoofable / older deployed route silently ignores remap semantics and marks `processed`; (f) processed immutability: any path where remap resets or sets `raw_documents.processed`; (g) activation lock relaxed for remap targets.
6. **Eval control-plane isolation:** make validate/estimate/offline/report modes touch DB or network: (a) module-load-time side effects — does `scripts/analysis-eval.ts` import anything that transitively imports `@/db` (which binds `DATABASE_URL` at import)? Is the `EVAL_DATABASE_URL` overwrite ordered before every such import edge (static import hoisting beats a later assignment)?; (b) the "lazy import one refactor away" risk — how brittle is the ordering?; (c) isolation source scans: do the globs actually cover the tree, and are filename exemptions (e.g. `scripts/ask-eval-harvest.ts`) wildcards that exempt more than intended?; (d) live-runner reachable without explicit flag + key + caps + non-prod DB guard — check what "non-production DB guard" actually tests (string match on hostname is spoofable, but the fail direction matters).
7. **Invariants sweep:** (a) drizzle/ untouched in the diff (no migrations claimed); (b) claim-source trigger + app-layer transaction untouched; (c) digest-persist.ts: `guardPublishedEvents` still runs before overwrite verdict; evidence-recency computed AFTER guard AND after overwrite verdict (else refused overwrites still mutate stats, or recency measures pre-guard population); (d) K=5 / REDUCE_VOTES untouched; (e) no new routes → ruling 21 surface unchanged (check `src/app` diff: only cron/map route changed — verify it stays CRON_SECRET-gated); (f) ISW prose / source full text not committed in the eval datasets (ruling 1) — scan the 5 dataset JSONs + offline results for prose leakage; provenance of committed offline artifacts (are they generated from fixtures, deterministic, no paid output?).
8. **Environment contracts:** `.env.example` diff vs code reads — every new env documented and every documented env actually read with matching semantics; `MAP_LEASE_TTL_SEC` clamp (floor/ceiling — can an operator set TTL 1s and thrash, or 0/NaN and never expire?); `EVAL_*` caps documented vs read; deployment-default equivalence — deploying 7150b49 with today's prod env: what changes? (lease replaces advisory lock — behavior change is claimed; recency stats start persisting; funnel is read-only script; evals inert). Look for anything else that silently activates (new cron behavior, new counts keys breaking dashboards is fine; anything paid or destructive is not).
9. **Secrets/hygiene:** scan the full diff for keys/tokens/connection strings/emails; check `.gitignore` changes don't ignore something that should be reviewed (or un-ignore secrets); confirm `docs/evals/analysis/results/*.json` are offline-fixture outputs, not paid-run outputs misrepresented.
10. **Test honesty:** spot-check the highest-leverage tests (map-lease concurrency, map-worker-spend, map-remap, isolation.test, live-runner) for weakened assertions, mocked-away guards, or tests that assert the mock rather than the code. Run targeted vitest files to confirm green.

## Files and runtime paths inspected

All paths under `/Users/go/code/bnow-net-worktrees/qf-audit-reviewer-safety-20260818` unless noted.

Runtime code (read in full or in the relevant regions):
- `src/lib/llm/model-config.ts` (routing authority incl. MAP activation hard lock), `src/lib/llm/analysis-registry.ts` (approval registry), `src/lib/analysis/openai-client.ts` (client factory)
- `src/lib/analysis/map-lease.ts` (full), `src/lib/analysis/map-worker.ts` (full, 868 lines), `src/app/api/cron/map/route.ts` (diff + gate region)
- `scripts/map-remap.ts` (full, 493 lines), `scripts/map-backfill.ts` (diff + callMap/category plumbing)
- `src/lib/usage/spend-guard.ts` (full: tryReserve/record/envNum/envCap/stopCategoryOfCode/pgUsageStore), `src/lib/usage/llm-guard.ts` (full)
- `scripts/analysis-eval.ts` (full, 584 lines), `src/lib/evals/live-runner.ts` (full, 446 lines), `src/lib/evals/eval-guard.ts` (full), `src/lib/evals/runner.ts` (header + import graph), `src/lib/evals/contracts.ts` (import graph)
- `src/lib/analysis/digest-persist.ts` (diff + NUL byte region), `src/lib/analysis/digest.ts` + `src/lib/analysis/synthesize.ts` (diffs), `src/lib/analysis/evidence-recency.ts` (contract half), `src/lib/analysis/quality-funnel.ts` (SQL parameterization scan), `scripts/quality-funnel-report.ts` (header + write/provider scan)
- `src/lib/validation/llm-match.ts` (full QF diff), `src/db/index.ts`, `src/db/schema.ts` (doc_claims/doc_map_state constraints), `drizzle/0011_past_squirrel_girl.sql` (unique indexes)

Tests (read):
- `src/lib/evals/isolation.test.ts` (full), `src/lib/analysis/openai-client.test.ts` (full), `src/lib/evals/live-runner.test.ts` (preflight sections), `src/lib/analysis/map-worker-spend.test.ts` (assertions), `src/integration/map-lease.itest.ts` (full), `src/integration/map-remap.itest.ts` + `src/integration/reduce.itest.ts` (case inventory)

Config/docs:
- `.env.example`, `.gitignore`, `AGENTS.md` (diffs); `docs/evals/analysis/*.json` (datasets + committed offline artifacts, programmatic inspection); `docs/reviews/QUALITY-FOUNDATION-INTEGRATION-2026-08-17.md` (full); `docs/reviews/MAP-RELIABILITY-REMAP-2026-08-17.md` (chimera/failure-table regions); governing prompt `/Users/go/code/bnow-net/docs/prompts/2026-08-17-quality-foundation-fable-ultracode.md` (full, before the attack plan)
- Evidence package (after attack plan): `gap-audit__quality-foundation-program__a4c3e82713270a342.md` (full), `qf-reviewers-recovered-20260818/final-safety-operations-reviewer__*.jsonl` (head, provenance check), package listing

## Commands run with results

1. `git rev-parse HEAD` → `7150b494d1399dddada6e7f917b1c0e76114d458`; `git status --porcelain` → clean at start.
2. `git diff --stat 05fdd2c..7150b49` → 66 files, 20,727 insertions, 119 deletions (matches commission).
3. `git diff --stat 9c5e9cb..0e469f7` → routing dependency: 35 files, incl. model-config/analysis-registry/openai-client (read as consumed).
4. `npm install --no-audit --no-fund` → OK (later produced benign `"peer": true` lockfile drift from npm's version; reverted with `git checkout -- package-lock.json`).
5. `npx vitest run` on 8 targeted files (map-lease, map-worker-spend, map-remap, isolation, live-runner, cron-map route, digest-persist, evidence-recency) → **126/126 pass**.
6. `npm test` → **2,402/2,402 (185 files)**; `npm run typecheck` → clean. Matches the report's §11 claims exactly.
7. `grep -rn 'from "@/db"' …` over every module reachable from `scripts/analysis-eval.ts` → zero static edges; only lazy `import("@/db")` in `map-lease.ts:55` and `spend-guard.ts:11-13`.
8. `grep -rn "new OpenAI("` over src/ + scripts/ (non-test) → only the two `maxRetries: 0` factories plus pre-existing `scripts/ask-eval-harvest.ts:190` (bare, exempted by filename).
9. Secret scan (perl regex over the full diff: `sk-…`, DSNs with credentials, AKIA, private keys, tokens) → only dummy test/build DSNs.
10. `git diff --stat 05fdd2c..7150b49 -- drizzle/ src/db/ src/lib/analysis/publication-guard.ts src/lib/gate.ts src/app/` → ONLY `src/app/api/cron/map/route.{ts,test.ts}`; drizzle/, db schema, publication guard, gate untouched.
11. `git diff e5757ea..HEAD -- ':!*.md' ':!.env.example'` → **empty (0 lines)**, verifying the report's docs-only-after-final-review claim; the `.env.example` delta is comment-only.
12. NUL byte scan: `perl -ne 'print if /\x00/'` on `digest-persist.ts` → **literal NUL found at line 286** (`cat -v` shows `${kind}^@${ck}`); `map-worker.ts` → clean (uses the ` ` escape). Independently confirms the gap audit's G2.
13. Dataset provenance: programmatic JSON inspection of all 4 committed offline artifact files → every result has `promptTokens/completionTokens/estUsd = null`, meter `{attempts:0, reservations:0, meterings:0, erroredAttempts:0}`, scope `full` — consistent with $0/no-provider provenance. "ISW" hits in `validation-v1.json` are the field name `iswPublishedAt` plus synthetic takeaway text; case text is authored/fictional (provenance `authored-2026-08-17`).
14. `git ls-files docs/prompts/ | grep 2026-08` → only `2026-08-15-iran-validation-recovery.md`; the governing QF prompt is NOT tracked at the review SHA (confirms G1).
15. `find scripts -type d` → flat; 47/47 script .ts files covered by the isolation scan's non-recursive readdir.
16. End state: `git checkout -- package-lock.json`; `git status --porcelain` → **empty (clean)**; `git rev-parse HEAD` → `7150b494d1399dddada6e7f917b1c0e76114d458`.

Process note: I read AGENTS.md/CLAUDE.md, the commission, the governing prompt, and ran only SHA/stat/log commands before writing the attack plan above; docs/reviews/* and the evidence package were opened strictly afterward, as required.

## Findings by severity

### BLOCKER
None.

### HIGH (MAJOR)
None.

### MEDIUM

**M1 — Factually false gate assertion in the integration report: "zero NUL bytes in changed .ts files".**
Evidence: `docs/reviews/QUALITY-FOUNDATION-INTEGRATION-2026-08-17.md` §11 (source-scans row) vs `src/lib/analysis/digest-persist.ts:286`, which contains a LITERAL 0x00 byte inside the entity-cache-key template (`${kind}<NUL>${ck}`) — and digest-persist.ts is a file this program changed (+53/−5). The byte is pre-existing on the base and functionally harmless (a working separator), and the program even fixed the identical pattern in `map-worker.ts` (`c40060e` rewrote it as the ` ` escape) — which makes the unqualified "clean" claim in a gate line demonstrably false rather than merely stale. No runtime consequence; the finding is gate-record integrity in a program whose whole value is its review chain. Fix: correct the report line (state the pre-existing exemption) or escape the byte in a follow-up commit. (Independently found by the gap audit as G2; I verified it at the byte level.)

**M2 — The governing program prompt is not tracked at the review SHA while AGENTS.md cites it as a repo path.**
Evidence: `git ls-files docs/prompts/` at `7150b49` contains 22+ sibling prompts but NOT `2026-08-17-quality-foundation-fable-ultracode.md`; the new AGENTS.md decision-log entry (appended in this diff) cites `docs/prompts/2026-08-17-quality-foundation-fable-ultracode.md` as though resolvable in-repo. The prompt survives only as an untracked file in the operator's dirty checkout — the very checkout the prompt forbade the program from using. Failure scenario: a future operator (or auditor) at this SHA cannot retrieve the spec the entire review chain grades against, defeating the program's own completion criterion ("decide what to merge … without relying on chat history") and leaving the decision log with a dangling citation. Fix: `git add` the prompt on the integration branch (or correct the citation). (Matches gap-audit G1.)

### MINOR

**m1 — The final review verdicts bind `e5757ea`, not the tip `7150b49`.**
Evidence: integration report §2 (final-reviewed-SHA row) and §12; commits `6b52a27..7150b49` post-date both PASS-WITH-MINORS verdicts. Mitigation verified by me: `git diff e5757ea..HEAD -- ':!*.md' ':!.env.example'` is empty and the `.env.example` delta is comment-only, so no runtime byte is unreviewed; the report discloses this precisely. Strictly, though, the program prompt says a final PASS applies only to the exact final SHA. This audit (a fresh full review of `7150b49`) cures the gap going forward.

**m2 — The eval CLI's DB isolation rests on an un-pinned lazy-import invariant.**
Evidence: `scripts/analysis-eval.ts:93` statically imports `llm-guard` → `spend-guard`, and `runner.ts:22` statically imports `map-worker` → `map-lease`; both reach `@/db` ONLY through lazy `import("@/db")` (`spend-guard.ts:11-13`, `map-lease.ts:54-55`), and `@/db` binds `DATABASE_URL` eagerly at module load (`src/db/index.ts:5-8`). `scripts/env.ts` loads `.env.local` (production DSN) into `process.env` for every mode, and the live path's `process.env.DATABASE_URL = evalDatabaseUrl` overwrite (`analysis-eval.ts:444`) is safe today only because every `@/db` execution comes after it. One innocent refactor (making either lazy import static) silently rebinds ALL eval modes — including "no DB" validate/estimate/offline/report — to the production URL, and moves the live run's `openai_eval` spend ledger onto production. No test pins the laziness or the overwrite ordering (`isolation.test.ts` scans eval-lib imports and SDK reachability, not `@/db` reachability). Self-disclosed by the program (§13 residuals, added by its final safety review) with the right fix (a regression test); I concur it must land before the first paid eval and would prefer before merge.

**m3 — `--execute-live`'s DB acknowledgment is a speed bump, not a production-host refusal.**
Evidence: `live-runner.ts:179-183` — the refusal message itself echoes the exact `--db-ack <host>` string to paste, and nothing compares the EVAL host against the production `DATABASE_URL` host that is sitting in the same env (loaded from `.env.local`). An operator who sets `EVAL_DATABASE_URL` to the production DSN (copy-paste slip across the two vars) gets one error message containing the correct bypass and then writes `openai_eval` provider_usage rows to production. Cheap hardening: refuse when `new URL(EVAL_DATABASE_URL).host === new URL(DATABASE_URL).host`. Not blocking: the mode has never run, requires four deliberate operator acts, and the writes at stake are spend-ledger rows, not analysis data.

**m4 — A remap checkpoint lacking `versionsDigest` is trusted without the version-reset guard.**
Evidence: `scripts/map-remap.ts:265` — `if (cp.versionsDigest !== undefined && cp.versionsDigest !== versionsDigest)` resets day states; a checkpoint file WITHOUT the field (hand-edited, corrupted, or from a hypothetical older tool) skips the reset and its `complete: true` days suppress sweeps after a version bump. Bounded: `doc_map_state` remains the authoritative no-rebill record, so the worst case is a stale "complete" skip (under-remapping, loudly resumable with a fresh checkpoint), never a rebill. Disclosed in the report's §13 residuals ("legacy checkpoint trusted once").

### NOTE

**n1 — "never a second writer" in the map-lease header is locally overstated.**
`map-lease.ts:44-47` claims the worst case of a >TTL provider call is duplicate billing, "never a second writer (writes are token-gated)". The token gate is a check-then-write (`stillOwner()` renew at `map-worker.ts:349-368`, then the persist transaction), so a ≥TTL single-STATEMENT stall between renew-success and commit does admit a second writer — the documented "chimera" residual, which can interleave mixed-generation `doc_claims` ordinals for one (doc, track, version) when two responses differ in claim count (doc_map_state PK + `doc_claims_doc_track_version_ordinal_idx` make it duplicate-proof but not interleave-proof; drizzle/0011). The honest qualification exists 300 lines away (`map-worker.ts:345-348` docstring) and in the B report (§2 residual b; review finding 6 names the "mixed-generation claim set"), and the integration report §7 separates the two residuals correctly — so this is a wording nit in one header sentence, not a concealment. The residual's bound is real: ≥120s single-statement stall at exactly the wrong instant, no traceability or publication-safety consequence (interleaved claims are still genuinely extracted from that document), shadow-map tables only.

**n2 — `scripts/ask-eval-harvest.ts:190` constructs a bare `new OpenAI()` (SDK default auto-retries) and is exempted by exact filename in `isolation.test.ts:119`.** Pre-existing, outside this diff, separately authorized, and disclosed; the exemption is name-tight (a rename would trip the scan) but normalizes the one retry-enabled client in the tree.

**n3 — The isolation scans are syntactic and their scripts/ coverage is non-recursive.** `readdirSync(SCRIPTS_DIR)` (isolation.test.ts:45) covers all 47 script files today because scripts/ is flat; a future subdirectory would escape. Computed/concatenated import specifiers would also evade any of these regexes. Acceptable as defense-in-depth beside the structural rule (live-runner dynamic-import-only), worth a `walk()` when scripts/ ever nests.

**n4 — Bounded cap overshoot under concurrency (documented).** `map-worker.ts:616-618`: concurrent in-flight batches can overshoot the daily/total cap by at most concurrency−1 batches, since caps are checked per reservation, not fenced across workers. Pre-existing pattern, explicitly documented in the code comment.

**n5 — The remap driver's lease-busy wait is unbounded** (`map-remap.ts:329-334`: 60s sleep loop while the hourly worker holds the lease, cursor unadvanced). Operator-attended CLI, interrupt-and-resume safe; worth a `--max-wait` eventually.

**n6 — `cycle()` constructs the OpenAI client even for a zero-batch run** (`map-worker.ts:610` runs unconditionally after the dry-run return). Construction is not contact, but with `OPENAI_API_KEY` absent the SDK constructor throws and a no-work cycle records an error instead of a clean zero. Production always has the key; cosmetic.

## Categories explicitly checked with NO finding (clean)

1. **Routing dependency as consumed (9c5e9cb..0e469f7):** the approval registry is per-(workload, model, effort) with no production bypass; the MAP activation hard lock has no env override, sits inside `resolveWorkloadModel` before pricing/approval messaging, and the QF remap path dispatches through the same server-side `workloadDispatchConfig("map")` — remap is NOT an activation bypass (today it can only re-extract under the baseline model, exactly as documented). Dispatch identity is built from the same config object the billed call used, never re-read from env.
2. **SpendGuard ordering on every paid path in the diff:** map worker: lease → `assertLlmEnabled` → `workloadDispatchConfig` (typed fail-closed) → guard.init → per-attempt `reserve()` → dispatch → `guard.record` BEFORE finish_reason/parse. Eval live runner: preflight (flag, kill-switch, stub refusal, EVAL_DATABASE_URL+ack, key, both caps) → DATABASE_URL overwrite → guard/client construction → per-attempt reserve → meter-before-parse. Fail-closed cap parsing verified (`envCap`: unset/invalid/≤0 → null → refuse; `envNum`: NaN → default; `evalGuardFromEnv` has deliberately NO out-of-production daily default).
3. **Retry/metering cardinality:** `maxRetries: 0` on both factories, source-scan-pinned including live-runner; the only retries are the explicit 65s 429 loops, each behind a FRESH reservation; truncation-split recursion reserves per physical attempt (3:3:3 test-pinned); truncated/unparseable/refusal responses metered before discard; lease-lost stops before the next reservation (all verified in map-worker-spend.test.ts and live-runner.test.ts assertions, 126 targeted tests re-run green).
4. **Lease atomicity:** single-statement `INSERT … ON CONFLICT DO UPDATE … WHERE (free|expired) RETURNING` CAS; expiry compared only against DB `now()` (no app clock in any authorization decision); tokens are per-acquisition UUIDs never reused (structural ABA protection); renew/release are token-CAS single statements; renew-vs-takeover races serialize on the row with exactly one winner; release preserves the fence; driver failure on acquire fails safe (no work, no writes); no session state remains (advisory lock fully removed from the worker — verified by reading the full file); real-Postgres itest covers the genuine two-racer race, expired takeover with fence monotonicity, and stale renew/release refusal.
5. **Remap resumability/rollback:** eligibility ignores `processed` and anti-joins `doc_map_state` at explicitly resolved current versions (superseded rows re-selected; current no-claims dispositions excluded — never perpetually rebilled); remap NEVER writes `processed` (line-verified `!opts.remap` guard; the mirrors write path is structurally unreachable in remap mode); old version rows never deleted or mutated; `--execute` requires a finite positive `--budget` (NaN/0/negative throw); estimate-over-budget aborts before any paid call; checkpoint spend counts against the budget on resume; the version digest resets stale `complete` flags (with `doc_map_state` as the real record); route capability handshake enforced in phase 1 on EVERY day (empty days included) plus a live-response re-check; typed stop categories with correct cursor semantics (cursor advances only on clean calls; benign run_cap loops converge via the server-side anti-join, test-pinned).
6. **Eval control-plane isolation:** validate/estimate/offline/report construct no client, reach no DB (no static `@/db` edge anywhere in the reachable graph — grep-verified), and live-runner is dynamic-import-only (scan-pinned); the registry bypass (`evalDispatchConfig`) is confined to live-runner, stamps `approval: "evaluation_candidate"` into every artifact, and no production module can import the eval library (scan + my read of the scan's regexes); live results are gitignored (`docs/evals/analysis/results/live-*`); committed offline artifacts carry zero-token/zero-cost/zero-meter provenance (programmatically verified).
7. **Migrations and traceability:** `drizzle/` and `src/db/` byte-untouched by the program diff; the claim-source trigger and app-layer transaction untouched; `9999_claim_source_trigger.sql` undisturbed; no new migration (none claimed).
8. **Publication safety and K=5:** `guardPublishedEvents` still runs before the overwrite verdict in `digest-persist.ts` (position unchanged); evidence recency is computed AFTER the guard AND after the overwrite verdict, on the exact persisted post-guard population, stub-excluded at query level, and fail-OPEN (a stats failure warns and omits the key — it can never block a publishable digest); no pre-existing `structured.stats` key is altered; REDUCE_VOTES/K=5 untouched (synthesize diff adds only pure vote-gid counters); the live digest eval refuses any K≠5.
9. **Authorization surface (ruling 21):** no new routes; the only `src/app` change is the CRON_SECRET-gated map cron route (gate verified first in the handler, before parsing); no page/layout/gate file touched.
10. **Environment contracts and deployment-default equivalence:** every new env is both documented and read with matching semantics (`MAP_LEASE_TTL_SEC` clamped [30,600] vs the route's 800s maxDuration; `EVAL_USD_CAP_DAILY`/`EVAL_DAILY_REQUEST_CAP`/`EVAL_RUN_REQUEST_CAP`/`EVAL_DATABASE_URL`). Deploying this tree with today's production env changes exactly what the reports claim: the map lease replaces the advisory lock immediately (the #77 fix), digest persists begin writing additive `evidenceRecency` + reduce-stat keys, and cron counts gain lease/dispatch observability; remap, funnel, and the whole eval plane stay inert without deliberate operator action; no cap or flag semantics change.
11. **Secrets/data hygiene:** no secrets, no `.env` files (only `.env.example`), no production data, no paid-result artifacts in the diff; datasets are synthetic/fictional with no ISW prose (ruling 1 clean — "ISW" occurrences are a field name plus authored takeaway text).
12. **Test honesty:** the targeted suites assert real cardinalities and orderings (not mock echoes); the full unit suite and typecheck reproduce the report's exact numbers (2,402/185, clean).

## Cross-check against the program's own review chain

After completing my code review I read the integration report, the B report's residual sections, and the evidence package. My independent findings converge with the recovered final reviews and the gap audit (G1↔M2, G2↔M1, G3↔m1, G4↔n1, G5↔m2, G6↔n2), and I found no material issue they missed and none they overstated. The recovered reviewer journals confirm the two final reviews were genuinely commissioned fresh agents bound to the correct base/tip. The report's §11 gate table reproduced exactly on every gate I could re-run; the one false line in it is M1.

## Verdict rationale

No blocking or major finding. The two MEDIUMs are documentation/gate-record integrity items (one false scan claim, one untracked governing spec) with zero runtime consequence; both are curable in a docs commit and should be dispositioned at merge time. The MINORs are either disclosed residuals with agreed pre-paid-eval fixes (m2, m4), a cheap hardening (m3), or cured by this very audit (m1). The runtime diff itself — spend, retries, lease, remap, isolation, invariants — withstood every attack I constructed.

VERDICT: PASS-WITH-MINORS (on 7150b494d1399dddada6e7f917b1c0e76114d458)
