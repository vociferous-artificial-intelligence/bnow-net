# <<PROJECT>> — rollout project file

Copy this to `docs/rollout/PROJECT.md` in the project and fill it in once. Every agent prompt
begins by reading this file, so the placeholders in `COMMON.md` §1 resolve here instead of
being edited into each prompt.

| Placeholder | Value |
|---|---|
| `<<PROJECT>>` | |
| `<<REPO>>` | |
| `<<CATEGORY>>` | |
| `<<POSITION>>` (current line; may be provisional) | |
| `<<ICP_LIST>>` (ranked; mark GATED ones) | |
| `<<COMPETITORS>>` (grouped) | |
| `<<STRATEGY_DOCS>>` (paths) | |
| `<<OUT>>` | `docs/rollout/` |
| `<<OPERATOR>>` | |
| Live site (signed-out URL) | |
| Test sign-in available to agents? | yes / no — how |
| Analytics stack and consent posture | |
| Decision log path (append-only) | |
| Open-task register path | |
| Claims / outreach rules register, if any | |
| Locales served; which are native-reviewed | |
| Paid-call budget for research steps | default: none |

## Project-specific notes for agents

(Anything an agent must know before starting: repo rulings that bind copy or design,
folders that are scratch, deploy constraints, names not to use.)
